# Pre-Review Self-Audit — 10-6-4

**Generated:** 2026-07-13 by claude-opus-4-8
**Story file:** 10-6-4-cheap-lane-latency-usable-tool-call-turn.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- **AC-1** (keep_alive on every chat dispatch, env `OLLAMA_KEEP_ALIVE` default `-1`): **MATCH** — `models.py` `__init__` param `keep_alive: int | str = -1`, passed top-level on `call` (`:535`) + `call_with_tools` chat_kwargs; `registry.py` reads env via `_parse_keep_alive`. Tests assert both sites + default + env passthrough.
- **AC-2** (Ollama timeout env-configurable, default 120, Anthropic unchanged): **MATCH** — both Ollama sites `float(get_secret_optional("OLLAMA_TIMEOUT_SECONDS","120.0"))`; Anthropic `60.0` untouched; tests cover default/override/Anthropic-60.
- **AC-3** (Seam B trim+prefix+retry-tame): **PARTIAL/MATCH-with-honest-defer** — the ONE dev-codeable in-repo lever (MCP `timeout: 30→120`) applied; B1 tool-trim + B3 retry-tame proven Hermes-runtime-owned (not config-exposed; `dispatch_tool_call` already single-attempt `retryable=False`) → deferred to AC-6 walk + flagged. Story contract explicitly permits this defer ("if not touchable from this repo, record locus + defer to AC-6"). Not drift — a documented scope-boundary. B2 stable-prefix holds via static persona files.
- **AC-4** (num_ctx NOT set + guard comment): **MATCH** — no `num_ctx` anywhere; guard comment added at `call_with_tools` options citing F-10-6-1-W1 Finding 2.
- **AC-5** (MANDATORY-CR ≠ dev + 4 gates green): **MATCH (CR pending)** — gates green (ruff/mypy-strict-134/pytest 1923+3+3); CR is the next step (sonnet-5 ≠ opus-4-8).
- **AC-6** (live Discord re-walk): **N/A — Adam-hands-on Phase 3.5** (closes done-flip clause 3; not dischargeable autonomously).

No AC text needed correction.

## 2. File-List-vs-git diff check

`git status --porcelain`:
- `mailbot_api/router/models.py` — **TRACKED (M)** ✓
- `mailbot_api/router/registry.py` — **TRACKED (M)** ✓
- `hermes-config/config.yaml` — **TRACKED (M)** ✓
- `tests/unit/router/test_ollama_adapter.py` — **TRACKED (M)** ✓
- `tests/unit/router/test_registry.py` — **TRACKED (M)** ✓
- `10-6-4-...md` (story) — UNTRACKED (new artifact, expected) — staged at Step 2.6
- `F-10-6-1-W1-diagnosis-2026-07-13.md` — UNTRACKED (new diagnosis artifact, expected) — staged at 2.6

All File-List source/test paths are tracked. No untracked source files. PASS.

## 3. Adversarial self-review

- [LOW] `hermes-config/config.yaml` — MCP `timeout: 120` addresses the tool-EXECUTION call, which is a DIFFERENT timeout from the inference dispatch the diagnosis measured. It's a defensible belt-and-suspenders alignment, but a reviewer could argue it's not strictly the F-10-6-1-W1 mechanism. Documented as such in the comment + Completion Notes (honest, not overclaimed).
- [MED] `registry.py:_parse_keep_alive` — an empty/whitespace `OLLAMA_KEEP_ALIVE` value would `int("")`→ValueError→return the stripped `""` string, which Ollama would reject at dispatch. But `get_secret_optional` returns the default `"-1"` for unset/empty env, so `""` never reaches the parser in practice. Edge is unreachable via the real call path; noted not fixed.
- [MED] `OLLAMA_TIMEOUT_SECONDS` malformed value (e.g. `"abc"`) → `float()` raises at `init_default_adapters()` boot. This is fail-loud-at-startup (intended per Dev Notes) — a misconfigured deploy fails fast rather than silently defaulting. Acceptable; documented.
- [LOW] `keep_alive: int | str` union — a caller could pass a nonsense string; the adapter forwards it verbatim to Ollama which validates. No adapter-side validation added (matches the existing pattern — the adapter is a thin pass-through). Intentional.
- [LOW] nomic embeddings adapter also gets `keep_alive=-1` (pins ~fixed small model). Harmless; env-tunable; noted in Dev Notes as a deliberate symmetry choice.
- [LOW] The 4 new adapter tests use the existing `_FakeAsyncClient.last_kwargs` capture — they assert the kwarg is PRESENT and correct, but a real Ollama server ignoring an unknown `keep_alive` kwarg is not exercised by unit tests (that's the AC-6 live walk's job; and keep_alive was live-confirmed in the diagnosis before this story). Coverage boundary is honest.

## 4. Self-caught issues remediated this audit

- [LOW] config.yaml MCP-timeout semantics → **ACCEPT WITH RATIONALE**: documented in-comment + Completion Notes as belt-and-suspenders, not the load-bearing fix. No overclaim.
- [MED] `_parse_keep_alive` empty-string edge → **ACCEPT WITH RATIONALE**: unreachable via `get_secret_optional` default; adding a guard would be dead code.
- [MED] `OLLAMA_TIMEOUT_SECONDS` malformed → fail-loud → **ACCEPT WITH RATIONALE**: intended fail-fast-at-boot; documented.
- [LOW] keep_alive union no validation → **ACCEPT WITH RATIONALE**: matches thin-adapter pattern; Ollama validates.
- [LOW] nomic keep_alive pin → **ACCEPT WITH RATIONALE**: deliberate symmetry, env-tunable.
- [LOW] unit-test coverage boundary → **ESCALATE TO REVIEWER** (for a second opinion) + real-server behavior is AC-6's job. Live keep_alive effect was already confirmed in the diagnosis (25.3s→3.4s).

## 5. Posture Audit

- **5.1 Tests written & passing** — `.venv/Scripts/python.exe -m pytest -q` → **1923 passed, 3 skipped, 3 deselected** (+10 net vs 10-6-3 baseline 1913). PASS.
- **5.2 Lint clean** — `ruff check mailbot_api tests` → `All checks passed!`. PASS.
- **5.3 Types clean** — `mypy --strict mailbot_api` → `Success: no issues found in 134 source files`. PASS.
- **5.4 Boundary/architecture** — enforced by ruff arch rules (part of 5.2); no new cross-boundary imports (adapter-internal + registry env-read + config YAML). PASS.
- **5.5 No secrets committed** — `grep -rnE "sk-…|Bearer …|password="` on the 3 changed source/config files → empty (only the pre-existing `${MAILBOT_ROUTER_KEY}` env-substitution placeholder). PASS.
- **5.6 No debug/print leftover** — changes are param threading + env reads + comments; no `print`/`breakpoint`. PASS.
- **5.7 Error handling** — `_parse_keep_alive` catches `ValueError` → string fallback; malformed timeout fail-loud-at-boot (intended). PASS.
- **5.8 Migrations** — N/A, no schema change.
- **5.9 Docs/comments** — diagnosis-referencing comments at every locus (keep_alive rationale, num_ctx guard, MCP-timeout rationale). PASS.
- **5.10 Backward compat** — new params/env all have safe defaults (`keep_alive=-1`, timeout `120.0`); existing adapter constructions without the kwargs still work (adapter default; registry sets it). Anthropic path byte-unchanged. PASS.
- **5.11 Perf/resource** — `keep_alive=-1` pins qwen ~1.9GB resident (documented RAM cost + env-tunable). This is the INTENDED effect (the fix). PASS.
- **5.12 §5.12 self-audit cadence** — this artifact IS the cadence discharge; MANDATORY-CR reviewer≠dev criterion-6 satisfied at the CR step (load-bearing adapter dispatch seam). PASS.
