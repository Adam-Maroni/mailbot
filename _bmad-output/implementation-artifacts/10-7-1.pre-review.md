# Pre-Review Self-Audit — 10-7-1

**Generated:** 2026-07-16 by claude-opus-4-8[1m]
**Story file:** 10-7-1-tool-call-as-text-rescue-parser.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- **AC-1 (well-formed block promoted): MATCH** — `_rescue_text_tool_call` (models.py) promotes a clean `name`+`arguments` (dict, JSON-string, or absent→`{}`) block to an `OpenAIToolCall`; call-site sets `finish_reason="tool_calls"`. Byte-shape identical to structured path (same `json.dumps(...,separators=(",",":"))`, same `OpenAIToolCall`/`Function` construction, synthesized `id="call_0"`).
- **AC-2 (malformed declined + logged): MATCH** — stray-sibling-key block (the walk's `memory` shape) returns `None` → not promoted, `finish_reason="stop"`, `event="tool_call.rescue.declined"` emitted.
- **AC-3 (no over-trigger): MATCH** — the rescue branch is guarded by `"<tool_call>" in text`; a plain "I can't do that." never enters it. Existing `test_call_with_tools_text_only_response` stays green.
- **AC-4 (structured wins, fallback only): MATCH** — rescue branch guarded by `if not tool_calls`; content is not scanned when the structured array yielded any call. No double-count.
- **AC-5 (temp-0 fidelity): MATCH** — rescue is a pure post-response text parse; it never touches `temperature`/`options`/`chat_kwargs`. Long-Graph-id round-trips exact (test asserts).
- **AC-6 (promote + decline both log structured events): MATCH** — `tool_call.rescue.promoted` (info) + `tool_call.rescue.declined` (warning) both greppable, both asserted.
- **AC-7 (scope fence, clause 3 NOT closed): MATCH** — Completion Notes state FORMAT-channel only; clause 3 owed 10.7.3 + Adam re-walk.

No DRIFT — no AC text required correction.

## 2. File-List-vs-git diff check

`git status --porcelain` (relevant paths):
```
 M mailbot_api/router/models.py            MODIFIED (in File List)   ✅
 M tests/unit/router/test_ollama_adapter.py MODIFIED (in File List)  ✅
?? _bmad-output/implementation-artifacts/10-7-1-tool-call-as-text-rescue-parser.md  story artifact ✅
?? _bmad-output/implementation-artifacts/10-7-1.pre-review.md  this audit ✅
 M _bmad-output/implementation-artifacts/sprint-status.yaml  expected status-flip ✅
```
Unrelated pre-existing (NOT this story, will NOT be staged at Step 2.6): ` M .claude/settings.json`, ` M _bmad-output/implementation-artifacts/10-7-0-spike-finding.md`, `?? .autonomous-run-active.json` (run-state, teardown at Phase 3.5).

Both File-List source paths are TRACKED (`M`, not untracked). No UNTRACKED source file needing `git add` before review.

**Test-to-prod ratio (§5.11.b):** testAdded=291, prodAddedExcludingDocs=133 (all of models.py's added lines are production), ratio 291/133 = 2.19 ≥ 0.30 ✅.

## 3. Adversarial self-review

- [LOW] models.py `_rescue_text_tool_call` — a `<tool_call>` substring with no closing `</tool_call>` (truncated/`length` finish) makes the regex miss → helper returns `None` → a decline log fires even though there was no complete block. Behaviorally correct (no promotion, which is the safe direction) but the decline log conflates "malformed JSON" with "truncated block." Acceptable — the decline event is telemetry, not a contract; a truncated block IS a declined block.
- [LOW] models.py multi-block: AC-1 task says "promote first, log the rest"; impl promotes the first and emits the promote log but does NOT emit a separate per-extra decline/extra event. A walk can see a rescue happened but not that N extra blocks were dropped. Flagged below (§4) as ACCEPT-WITH-RATIONALE — single-call semantics is intentional; extra blocks are qwen over-emitting, and surfacing them is not load-bearing for the format-channel fix.
- [LOW] models.py `_sanitize_text` duplicates the four-regex redaction body of `sanitize_error` (errors.py:281-284) rather than sharing a helper. Chosen because `sanitize_error` is exception-typed and `observability._redaction` exposes only the regex constants (no string helper). A future refactor could hoist a shared `redact_str` into `_redaction`. ACCEPT — mirrors the existing pattern; extraction is out of scope for a format-channel story.
- [INFO] The `"<tool_call>" in text` cheap pre-guard avoids running the regex on every plain-text turn — a small hot-path win on the common no-tool-call path. Confirmed the guard and the helper's internal guard agree (helper also checks the substring), so no divergence.
- [INFO] `id="call_0"` is a fixed literal (single-call rescue → only ever one call). Matches the structured path's `call_{i}` for i=0. No collision risk since rescue produces exactly one call.
- [INFO] Verified the rescue does not alter `text` — `ToolCallAdapterResponse.text` still carries the raw content (incl. the `<tool_call>` block). Downstream that reads `.text` is unaffected; only `.tool_calls` gains the promoted call.

## 4. Self-caught issues remediated this audit

- §3 issue 1 (truncated-block decline-log conflation): **ACCEPT WITH RATIONALE** — the decline is the safe direction; the log is telemetry not contract.
- §3 issue 2 (multi-block "log the rest" not per-extra-logged): **ACCEPT WITH RATIONALE** — single-call semantics intentional; promote event already signals a rescue occurred; per-extra logging is not load-bearing for the FORMAT channel. Noted for reviewer visibility.
- §3 issue 3 (`_sanitize_text` duplicates `sanitize_error` redaction body): **ACCEPT WITH RATIONALE** — no shared string-redaction helper exists; duplication mirrors the canonical body in the same order; extraction out of scope.
- No FIX-NOW items surfaced. No ESCALATE-blocking items. (§3 is non-empty — audit is not shallow.)

## 5. Posture Audit

### 5.1 — Lockfile hygiene
`git diff --stat -- requirements.txt` → `(no output)`. Non-dep-change story (no `requirements.txt` touch). ✅ PASS.

### 5.2 — Cross-doc pair verification
**Cross-doc branch:** N/A — this story makes no claim that cross-references a canonical doc against another doc; it's a single-module adapter change plus tests. The story's line-number cites (`models.py:695-724` etc.) are self-referential to the file it edits, verified live in §5.9.
**§5.2.1 schema-touching:** N/A — File List contains no migrations paths (no `mailbot_api/db/migrations/*`).

### 5.3 — Lifecycle string-uniqueness
N/A — story added zero i18n keys (no graphical frontend; the log event strings are telemetry, not lifecycle-staged user strings).

### 5.4 — Multi-consumer impact scan
`call_with_tools` is the modified surface. It's an existing method on `OllamaAdapter`; the change is purely additive (a new fallback branch in the already-empty `tool_calls` path) and does not change the method's return type or the shape of a natively-structured call. Consumers of `ToolCallAdapterResponse.tool_calls` (`router.py:2514` `len(...)`, drain pipeline) see a rescued call identically to a structured one — that indistinguishability is the AC-1 design goal. ✅ PASS — no consumer needs a change; the seam is shape-preserving.

### 5.5 — Screenshot-based perception check
N/A — backend adapter parse; no user-visible painted surface. No AC uses "visible/appears/displays."

### 5.6 — Upstream-contract spec coverage
N/A — the story does not consume an upstream-stripped/role-gated projection field. It parses qwen's own response text; both the present case (well-formed block → promote) and absent case (no block / malformed → zero calls) are spec'd with full desired-output contracts (not just a fallback-to-default branch). The "malformed → decline, NOT fabricate" is precisely the desired-output contract for the absent-clean-args case.

### 5.7 — Module-level mutable container check
Modified `.py` files: `mailbot_api/router/models.py` (production), `tests/unit/router/test_ollama_adapter.py` (test). New module-level symbols in models.py: `_log = logging.getLogger(__name__)` (logger, not a mutable container), `_TEXT_TOOL_CALL_RE = re.compile(...)` (compiled-regex constant, immutable). No module-level `dict`/`list`/`set` counter or cache added; no `global` mutation. models.py runs in the long-lived Router process but neither new symbol holds per-request mutable state. ✅ PASS — `(no mutable-container output)`.

### 5.8 — Dev-fixture seed-vs-production-shape parity
The new test "fixtures" are inline `<tool_call>` text blocks. The malformed-block fixture is byte-faithful to the REAL producer shape recorded at WALK-10-7-5-F1 (`{"name":"memory","action":"add","target":"user","content":"unread_emails"}` — ground truth from `router_calls id=15022`). The well-formed fixtures are shape-faithful synthesis against the documented `<tool_call>{name,arguments}</tool_call>` contract (§Dev Notes real wire-shape). Producer is qwen-via-Hermes-template (a third-party model output the project does not own → pattern-1 recording is what WALK-10-7-5-F1 already did for the malformed case). ✅ PASS — malformed fixture is a recorded real shape; well-formed fixtures are shape-faithful to the documented contract.

### 5.9 — grep-verify-cited-figures
```
Cite: "pytest 1952 passed / 3 skipped / 3 deselected (+10 vs 1942 baseline)"
Command: .venv/Scripts/python.exe -m pytest -q  → "1952 passed, 3 skipped, 3 deselected, 1 warning in 257.37s"
Verdict: MATCH (baseline 1942 is 10-7-5's recorded suite count; +10 = the 10 new rescue tests).

Cite: "10 new rescue tests"
Command: .venv/Scripts/python.exe -m pytest tests/unit/router/test_ollama_adapter.py -q -k "rescue"  → "10 passed"
Verdict: MATCH.

Cite: models.py numstat "133 added", test numstat "291 added"
Command: rtk git diff --numstat -- mailbot_api/router/models.py tests/unit/router/test_ollama_adapter.py  → "133 0 …models.py / 291 0 …test_ollama_adapter.py"
Verdict: MATCH.
```
✅ PASS.

### 5.10 — Producer-boundary contract enforcement
**§5.10.a typed-column producers:** N/A — the rescue writes to no typed ORM column; it constructs an in-memory `OpenAIToolCall`. No `int(...)`/`Decimal(...)`/`datetime.fromisoformat(...)` on third-party input feeding a DB column. `json.loads` on qwen text is guarded by `try/except (TypeError, ValueError)` → returns `None` (errors-as-data, no raise across the adapter boundary, AR-PAT-4). ✅ PASS (input-shape guarded — malformed JSON declines, never raises).
**§5.10.b response-shape allow-list:** N/A — no ORM row returned to an HTTP client in this change; no `SELECT *`; no co-emitted sensitive type.

### 5.11 — Git-evidence consistency check
**5.11.a:** `git status --porcelain` cross-referenced in §2 — both File List source paths STAGED-ELIGIBLE + IN FILE LIST; no silent scope-creep among story paths; unrelated pre-existing files explicitly excluded from staging. ✅ PASS.
**5.11.b:** ratio 291/133 = 2.19 ≥ 0.30 ✅ PASS.
**5.11.c:** single-session dev pass; story flipped `in-progress`→`review` this session (baseline c45d0b0); no later commits under attribution. N/A (trivially clean).

### 5.12 — CR-cadence-mandatory surface classification
- Criterion 1 (boundary-introducing): NO — no new writer-monopoly / lint boundary / allowlist. (New import of `observability._redaction` regexes mirrors `errors.py`'s existing import; boundary checker exit 0.)
- Criterion 2 (dep-introducing): NO — no new external dep.
- Criterion 3 (dev-self-flagged): NO — §4 has no ESCALATE-TO-REVIEWER items (three ACCEPT-WITH-RATIONALE only).
- Criterion 4 (capstone): NO — 10-7-1 is not the last Epic-10.7 story (10-7-3 follows; clause 3 owed).
- Criterion 5 (privacy-invariant): NO — the parser touches no FR-2.3/2.5/5.7 / NFR-PRIV surface. (It does uphold the safety-net posture — STRICT decline avoids pushing ambiguous output toward action — which is authorization-adjacent, reinforcing MANDATORY-CR below.)
- Criterion 6 (load-bearing-orchestrator): **YES** — `OllamaAdapter.call_with_tools` is the primary tool-call dispatch surface for the entire local-lane cost thesis; its return shape feeds `router.py`'s `tool_calls_count` and the propose→grant→drain pipeline. The story spec itself pre-declares MANDATORY-CR (reviewer≠dev, adapter parse seam; [[feedback_reviewer_model_substitution]]).

**Cadence verdict: MANDATORY-CR** (criterion 6 fires; story spec pre-declares it).

## Posture Audit summary table

| Check | Status |
| --- | --- |
| 5.1 Lockfile hygiene | ✅ PASS |
| 5.2 Cross-doc pair verification | N/A — no cross-doc claim; no migrations path |
| 5.3 Lifecycle string-uniqueness | N/A — no i18n keys |
| 5.4 Multi-consumer impact scan | ✅ PASS — shape-preserving; consumers unchanged |
| 5.5 Screenshot-based perception check | N/A — backend, no visible surface |
| 5.6 Upstream-contract spec coverage | N/A — no upstream-stripped projection; present+absent both spec'd |
| 5.7 Module-level mutable container | ✅ PASS — only a logger + compiled-regex constant added |
| 5.8 Dev-fixture seed-vs-production-shape parity | ✅ PASS — malformed fixture is recorded real shape (WALK-10-7-5-F1) |
| 5.9 grep-verify-cited-figures | ✅ PASS — test/count/numstat cites verified against command output |
| 5.10 Producer-boundary contract enforcement | N/A/✅ — no typed-column/HTTP producer; json.loads guarded |
| 5.11 Git-evidence consistency check | ✅ PASS — File List matches tree; ratio 2.19; single-session |
| 5.12 CR-cadence-mandatory surface classification | MANDATORY-CR (criterion 6) |
