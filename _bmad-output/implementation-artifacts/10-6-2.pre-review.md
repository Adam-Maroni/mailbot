# Pre-Review Self-Audit — 10-6-2 (draft-pipeline reachability from chat)

**Generated:** 2026-07-13 by claude-opus-4-8[1m]
**Story file:** _bmad-output/implementation-artifacts/ai-2-draft-pipeline-reachability-from-chat.md
**Status at audit time:** review (post dev-story, pre code-review)

Scope reminder: this is a **Hermes-side persona-contract story** (boundary-honest per the story spec + Epic 10.5 discipline). The `draft_reply` MCP tool + the `handle_draft_reply` Opus pipeline already exist in `mailbot_api` (Story 10.5.3); the dev-codeable work here is AC-2 (persona reach) + AC-3 (sensitivity-gate no-regression) + AC-4 (MANDATORY-CR). AC-1/AC-5 (live Opus draft walk) are Adam-hands-on Phase 3.5. No `mailbot_api` source was changed.

## 1. AC-vs-code drift scan

- **AC-1** (real Discord draft turn → Opus `draft_reply` router_calls row): `N/A (this dev pass)` — live-walk AC, Adam-hands-on Phase 3.5. The dev pass makes it *reachable* (AC-2); it does not itself produce the live row.
- **AC-2** (persona/dispatch contract makes `draft_reply` reachable deterministically; persona issues the verb, not "isn't exposed"): `MATCH` — `hermes-config/skills/mailbot/SKILL.md` `draft_reply` section now carries a MUST-dispatch Reach contract + no-improvise / no-"isn't-exposed" prohibition naming F-10-5-11 in the F-10-5-6 false-narration family; Turn structure 2 step 5 rewritten to dispatch the `draft_reply` MCP verb (not `ask_router`). Verified by `test_draft_reply_reach_contract.py` (6/6 green).
- **AC-3** (privacy invariant preserved; no FR-2.3/F28 regression): `MATCH` — the reach edits did not touch the sensitivity-gate description (confidential refused; sensitive requires `mint_sensitivity_token`→`confirmation_token`). `test_skill_md_draft_reach_preserves_sensitivity_gate` asserts the gate survives; `test_dispatch_tool_call_sensitivity_gate_f28.py` + `test_draft_reply_orchestrator.py` still pass in the full suite (1911 passed).
- **AC-4** (MANDATORY-CR, reviewer ≠ dev): `MATCH (pending execution)` — dispatched at Step 2.4 with sonnet-5 ≠ opus-4-8.
- **AC-5** (live Opus draft walk, real spend, Console-authoritative): `N/A (this dev pass)` — Phase 3.5, Adam-hands-on.

No DRIFT requiring AC-text edits.

## 2. File-List-vs-git diff check

`git status --porcelain` (story-relevant paths):
```
 M _bmad-output/implementation-artifacts/ai-2-draft-pipeline-reachability-from-chat.md
 M _bmad-output/implementation-artifacts/sprint-status.yaml
 M hermes-config/skills/mailbot/SKILL.md
?? tests/integration/test_draft_reply_reach_contract.py
```
File List declared paths:
- `hermes-config/skills/mailbot/SKILL.md` → `M` ✅
- `tests/integration/test_draft_reply_reach_contract.py` → `??` (untracked, pending stage) ✅
- `_bmad-output/implementation-artifacts/ai-2-draft-pipeline-reachability-from-chat.md` → `M` ✅
- `_bmad-output/implementation-artifacts/sprint-status.yaml` → `M` ✅

All four File-List paths present in git output; all tracked-or-pending. No UNTRACKED-story-file left unstaged after staging (Step 2.6). Out-of-scope untracked entries (`.autonomous-run-active.json`, `10-6-4-*.md`, `F-10-6-1-W1-diagnosis-*.md`, `epic-10-6-retro-*PARTIAL.md`, `scratch/`) are pre-existing background work / other-story artifacts — NOT staged (see §5.11.a). `.claude/settings.json` is a pre-existing env-envelope modification, not this story's — NOT staged.

## 3. Adversarial self-review

- `[LOW] SKILL.md draft_reply Reach contract` — The prohibition says "NEVER hand-write / improvise the draft body yourself in your own chat turn." There is a legitimate inline-drafting variant (SKILL.md "Inline-drafting variant — F28 awareness") where the persona drafts inline through `/v1/chat/completions` with the F28 gate. Risk: the new "never hand-write" language could be read as contradicting that documented inline path. Mitigation: the inline variant is about the *sensitivity gate firing on inline drafts*, not about *bypassing the Opus pipeline for quality*. The F-10-5-11 failure is specifically improvising-instead-of-dispatching to dodge the (falsely-assumed-unreachable) verb. Left the inline-variant section intact; the new prohibition targets the "isn't exposed → improvise in haiku" failure, which is distinct. **Disposition: ACCEPT WITH RATIONALE (see §4).**
- `[LOW] Turn structure 2 step 4 rewrite` — I changed step 4 from "Call the Router with `task_type="tone_style_mirror"`" to "tone-mirroring is handled INSIDE the draft_reply pipeline." This is factually correct — `handle_draft_reply` (`mailbot_api/chat/orchestrator.py:187-244`) runs `tone_style_mirror` itself before `draft_reply`. Risk: a reader wanting the standalone tone verb loses the explicit call example. Mitigation: the pipeline-internal behavior is the *correct* contract for the chat surface (the persona should NOT double-dispatch tone_style_mirror separately); the `tone_signals_blob` param is documented for the pre-fetched case. **Disposition: FIX-verified-correct (no change needed); the rewrite matches orchestrator reality.**
- `[LOW] Drift test coupling to exact phrases` — `test_skill_md_turn_structure_uses_the_mcp_verb_not_only_ask_router` asserts the literal substring `` `draft_reply` MCP verb `` or `` `draft_reply` verb `` in the turn body. Risk: brittle to future rewording. Mitigation: this brittleness is *intended* (drift tests are meant to red-gate silent regression of the exact contract wording), matching the 10-5-6 `test_recognized_phrase_dispatch.py` posture. **Disposition: ACCEPT WITH RATIONALE — intentional drift-gate brittleness.**
- `[INFO] AGENTS.md not modified` — I chose not to add prose to AGENTS.md because Rule N already anchors "draft_reply routes to Opus per FR-4.4" (the "a draft isn't a cheap inline improvisation" invariant). `test_agents_md_draft_default_model_note_is_not_a_hand_write_license` passed on the first RED run, confirming the anchor pre-exists. Adding redundant prose would be churn. **Disposition: ACCEPT WITH RATIONALE.**

## 4. Self-caught issues remediated this audit

- §3 issue 1 (never-hand-write vs inline-variant): **ACCEPT WITH RATIONALE** — the two are distinct; the inline F28 path stays valid; the new prohibition targets the dodge-the-verb-in-haiku failure. Flagged to reviewer as a place to sanity-check the contract isn't self-contradictory.
- §3 issue 2 (tone step rewrite): **FIX NOW → verified correct at write time** — the rewrite matches `handle_draft_reply`'s actual tone-inside-pipeline behavior; no further change.
- §3 issue 3 (drift-test brittleness): **ACCEPT WITH RATIONALE** — intentional, mirrors 10-5-6.
- §3 issue 4 (AGENTS.md untouched): **ACCEPT WITH RATIONALE** — pre-existing anchor covers it.

One item flagged for reviewer attention: the never-hand-write vs inline-drafting-variant coherence (issue 1) — a load-bearing persona-contract consistency question.

## 5. Posture Audit

### 5.1 — Lockfile hygiene
`git diff --stat -- requirements.txt` → (no output). **N/A — non-dep-change story; requirements.txt untouched.**

### 5.2 — Cross-doc pair verification
Cross-doc branch: the story claims "`draft_reply` IS MCP-registered (Story 10.5.3, tool count 26)". Verified against canonical source:
```
$ Grep "_EXPECTED_TOOL_COUNT = 26" mailbot_api/mcp_server.py
1117:_EXPECTED_TOOL_COUNT = 26  # Story 10.5.3 added draft_reply (F-10-5-11 chat call site)
$ Grep '"draft_reply": draft_reply' mailbot_api/mcp_server.py  → line 959 (registration map)
$ Grep '"draft_reply": (' mailbot_api/mcp_server.py  → line 1104 (_TOOL_DESCRIPTIONS)
```
Verdict: **MATCH** — `draft_reply` is genuinely registered + described + counted. §5.2.1 schema branch: **N/A — File List contains no migrations paths.**

### 5.3 — Lifecycle string-uniqueness check
**N/A — story added no i18n keys (no graphical frontend; persona docs + tests only).**

### 5.4 — Multi-consumer impact scan
**N/A — story modified no shared source module.** The only source-adjacent change is a new offline test file; the persona `.md` is consumed only by Hermes's runtime loader (single consumer). No `mailbot_api` symbol changed.

### 5.5 — Screenshot-based perception check
**N/A — no graphical frontend; AC "visible" surfaces are Discord chat text, verified at Phase 3.5 live walk, not by screenshot.**

### 5.6 — Upstream-contract spec coverage check
**N/A — story consumes no upstream-stripped projection field.** The drift tests read the persona files directly; no server-side gate/projection is under test.

### 5.7 — Module-level mutable container check
**N/A — story modified zero `.py` source files under `mailbot_api/`.** The new test file declares only module-level `Final`-style path constants (`_REPO_ROOT`, `_SKILL`, `_AGENTS` — `Path` objects, never mutated) and no mutable dict/list/set/counter. Confirmed by read: no `= {}` / `= []` / `= set()` / mutable-global patterns.

### 5.8 — Dev-fixture seed-vs-production-shape parity check
**N/A — story added no test fixture consumed by code reading ORM output or pipeline payloads.** The new tests read live persona-doc files (`SKILL.md`, `AGENTS.md`) as their "fixture" — i.e., they assert against the *actual* production persona contract, which is the strongest possible parity (no synthesized shape).

### 5.9 — grep-verify-cited-figures
Numeric cites in the story/pre-review, verified at audit time:
```
Cite: "full suite 1911 passed + 3 skipped + 3 deselected"
  $ .venv/Scripts/python.exe -m pytest -q  → "1911 passed, 3 skipped, 3 deselected, 1 warning in 252.52s"
  Verdict: MATCH
Cite: "+6 net vs the 10-6-1 baseline of 1905"
  Inputs: 1911 (this run) − 1905 (10-6-1 baseline, from sprint-status 10-6-1 row) = 6
  Verdict: MATCH
Cite: "tool count 26" / "_EXPECTED_TOOL_COUNT = 26"
  $ Grep "_EXPECTED_TOOL_COUNT" mailbot_api/mcp_server.py → 1117:_EXPECTED_TOOL_COUNT = 26
  Verdict: MATCH
Cite: "6 drift tests"
  $ .venv/Scripts/python.exe -m pytest tests/integration/test_draft_reply_reach_contract.py -q → 6 passed (3 fail→pass across RED→GREEN; 6 total)
  Verdict: MATCH
Cite: "SKILL.md diff 48 added / 12 removed"
  $ git diff --numstat -- hermes-config/skills/mailbot/SKILL.md → 48	12
  Verdict: MATCH
```
Verdict: **PASS** — all cited figures anchored to command output at audit time.

### 5.10 — Producer-boundary contract enforcement
**N/A — story modifies no normalizer/DTO/service feeding a typed ORM column AND returns no ORM row to an HTTP client.** No `mailbot_api` source touched; persona docs + offline tests produce no runtime values.

### 5.11 — Git-evidence consistency check
**5.11.a — File-List-vs-working-tree:**
```
git status --porcelain (story paths): M SKILL.md, M story.md, M sprint-status.yaml, ?? test_draft_reply_reach_contract.py
File List declared: SKILL.md, test_draft_reply_reach_contract.py, story.md, sprint-status.yaml
Cross-reference:
  hermes-config/skills/mailbot/SKILL.md                              M + IN FILE LIST   ✅
  tests/integration/test_draft_reply_reach_contract.py              ?? + IN FILE LIST   ✅ (pending stage)
  _bmad-output/.../ai-2-draft-pipeline-reachability-from-chat.md     M + IN FILE LIST   ✅
  _bmad-output/.../sprint-status.yaml                                M + IN FILE LIST   ✅
  .claude/settings.json                                              M + NOT IN LIST    → pre-existing env-envelope work, NOT this story; will NOT stage
  10-6-4-*.md / F-10-6-1-W1-*.md / epic-10-6-retro-*PARTIAL.md / scratch/  ?? + NOT IN LIST → other-story artifacts, NOT this story; will NOT stage
Verdict: ✅ PASS — no silent scope-creep; all declared paths present; strays identified + excluded from staging.
```
**5.11.b — Production-only test-to-code ratio:**
```
Changed non-doc: SKILL.md (persona contract — classified as docs per §5.11.b: .md), test file (test).
  testAdded: 140 (tests/integration/test_draft_reply_reach_contract.py)
  docsAdded: 48 (SKILL.md .md added lines) + story/sprint-status .md
  prodAddedExcludingDocs: 0 (zero .py source under mailbot_api/ added)
  prodOnlyTestRatio: 140 / 0 = null (denominator 0)
Verdict: ✅ PASS — null ratio (no production code added; docs+test-only story). Threshold N/A when prodAddedExcludingDocs = 0.
```
**5.11.c — No-later-commits-under-attribution:** single-session dev pass; story flipped in-progress and completed in the same session on 2026-07-13. **N/A — no git log history under File-List paths between flip and pre-review.**

Verdict: **PASS** (5.11.a clean; 5.11.b docs+test-only; 5.11.c N/A single-session).

### 5.12 — CR-cadence-mandatory surface classification
```
Criterion 1 (boundary-introducing): NO — no new writer-monopoly, lint boundary, or check_boundaries allowlist. New drift test, no new invariant enforcement code.
Criterion 2 (dep-introducing): NO — requirements.txt untouched (§5.1).
Criterion 3 (dev-self-flagged): YES — §4 escalates the never-hand-write vs inline-drafting-variant coherence question to the reviewer (a load-bearing persona-contract consistency concern).
Criterion 4 (capstone): NO — 10-6-2 is not the last epic story (10-6-3, 10-6-4 remain) and is not a cross-story-collision integration point.
Criterion 5 (privacy-invariant): YES — the story TOUCHES the FR-2.3/F28 sensitivity-gate surface (AC-3 explicitly requires no-regression to the confidential/sensitive draft gate). Even though the intent is "preserve, don't change," the contract-surface edit sits directly on the privacy-invariant persona text.
Criterion 6 (load-bearing-orchestrator): YES — the persona draft-dispatch contract is the primary integration surface the charter-flagship "MailBot drafts in your voice via Opus" capability + the AC-1/AC-5 live walk lean on. The dispatch seam is load-bearing (retro's central lesson; sibling to 10-6-1's routing seam which ran MANDATORY-CR).

Cadence verdict: MANDATORY-CR (criteria 3 + 5 + 6 fire).
```

## Posture Audit summary table

| Check | Status |
| --- | --- |
| 5.1 Lockfile hygiene | N/A — non-dep-change; requirements.txt untouched |
| 5.2 Cross-doc pair verification | ✅ PASS — draft_reply MCP registration claim verified in mcp_server.py |
| 5.3 Lifecycle string-uniqueness | N/A — no i18n keys |
| 5.4 Multi-consumer impact scan | N/A — no shared source module modified |
| 5.5 Screenshot-based perception check | N/A — no graphical frontend |
| 5.6 Upstream-contract spec coverage | N/A — no upstream-stripped projection consumed |
| 5.7 Module-level mutable container | N/A — zero `.py` source under mailbot_api/ touched; test file has no mutable module state |
| 5.8 Dev-fixture seed-vs-production-shape parity | N/A — no ORM/pipeline fixture; tests read live persona files |
| 5.9 grep-verify-cited-figures | ✅ PASS — all figures anchored to command output |
| 5.10 Producer-boundary contract enforcement | N/A — no typed-column producer / HTTP-row-return touched |
| 5.11 Git-evidence consistency check | ✅ PASS — File List clean; docs+test-only; single-session |
| 5.12 CR-cadence-mandatory surface classification | **MANDATORY-CR** (criteria 3 + 5 + 6) |

No FLAGGED checks. One §4 escalation (never-hand-write vs inline-variant coherence) forwarded to the MANDATORY-CR reviewer.
