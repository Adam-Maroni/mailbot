# Pre-Review Self-Audit — 10-5-2

**Generated:** 2026-07-10 by claude-opus-4-8[1m]
**Story file:** _bmad-output/implementation-artifacts/10-5-2-authorization-below-the-persona-api-layer-enforcement.md
**Status at audit time:** review (post dev-story Tasks 1-4+7, pre code-review; Task 5 = Adam-hands-on HALT)

## 1. AC-vs-code drift scan

- **AC-1** (no agent self-mint of sensitivity tokens / Tier-2 grants): **MATCH** — `mint_sensitivity_token` (verbs/mint_sensitivity_token.py) + `mint_grant` (actions/authorization.py) now consume a `user_confirmations` row (created only at the chat boundary) and refuse `NEEDS_USER_CONFIRMATION` otherwise. Proven: `test_mint_requires_user_confirmation.py` (8).
- **AC-2** (skill-file not agent-writable): **MATCH (scoped honestly)** — the code portion is N/A in `mailbot_api` (the write vector is Hermes `file`/`skills` toolset on the RW bind-mount; `mailbot_api` has no skill-file-write verb). Story text + Dev Notes + Task 5 record this explicitly and defer the Hermes-config hardening + live-reject verification to Adam-hands-on Task 5. No fabricated fix — the honest boundary is the correct realization.
- **AC-3** (structured refusal envelope, no 502, no id leak, three shapes): **MATCH** — `SensitivityRefusal` on `RouterError.refusal_envelope`; boundary renders 200 four-beat message; `email_ref` one-way hash makes the Graph id structurally unprintable. Proven: `test_sensitivity_refusal_envelope.py` (9) + `test_sensitivity_refusal_envelope_boundary.py` (4).
- **AC-4** (genuine escalation, code portion): **MATCH** — session-independent handshake (pending_sensitive_refusal keyed on caller_origin, confirm→single-use confirmation→mint succeeds, no-brick). Proven: `test_sensitive_escalation_handshake.py` (13). Live end-to-end walk = Task 5 (Adam-hands-on) — correctly deferred, not claimed done.
- **AC-5** (MANDATORY-CR): **MATCH** — cadence verdict MANDATORY-CR (see §5.12); Task 6 pending.

No DRIFT requiring a story-text fix.

## 2. File-List-vs-git diff check

`git status --porcelain` (scratch/ excluded) — all File-List paths present as `M` (tracked) or `??` (new, pending-add). See §5.11.a for the full cross-reference. One addition beyond the story's original File List: `docs/DATABASE.md` (schema-doc entries added per §5.2.1) — added to the File List below. No UNTRACKED story-adjacent file is unaccounted for.

## 3. Adversarial self-review

- **[LOW] main.py** — the boundary escalation-detection runs `confirm_pending_escalation` (a DB write) on the request thread only when `is_escalation_confirmation` is true (exact-phrase). Non-confirmation turns incur no DB hit. Accepted — gated, cheap.
- **[MEDIUM] user_confirmation.py `_consume`** — uses `execute_write_returning(USER_CONFIRMATION_CONSUME + " RETURNING id")` for atomic single-use. Two concurrent mints for the same scope: only one UPDATE flips `consumed_at` (rowcount-1), the other returns None → refuses. Correct. But TTL is checked BEFORE the atomic consume (read-then-write), so a confirmation expiring in the microseconds between could be consumed — negligible (10-min TTL vs µs window) and fail-safe direction (a barely-expired confirmation succeeding once is harmless). Documented here.
- **[MEDIUM] router.py** — `record_pending_sensitive_refusal` is a DB write added to the sensitive-no-token refusal path (both dispatch_tool_call + not added to ask_router path). Reviewer: confirm the ask_router sensitive path does NOT also need the pending-refusal write — it does (Hermes chat uses dispatch_tool_call, but a direct ask_router sensitive refusal would not record a pending refusal). **ESCALATE** — see §4.
- **[LOW] sensitivity_refusal.py `email_ref_for`** — sha256[:8] gives a 32-bit space; collision risk across a user's concurrent refusals is negligible and the ref is display-only (never used as a key). Accepted.
- **[LOW] Confirmation scope for grant** — `consume_grant_confirmation` matches by `action_type` only (not email_ids), mirroring the existing grant-promotion semantics (`PENDING_GRANT_PROMOTE_FOR_ACTION_TYPE` also action_type-only). Consistent with the established contract; a tighter email-scoped confirmation is a future hardening. Accepted with rationale.
- **[LOW] Boundary phrase set** — `_ESCALATE_PHRASES` is a small fixed set; a user typing "yes please escalate" won't match. Deliberate (retro §8.7 recognized-phrase determinism over fuzzy interpretation). The envelope's `user_facing_guidance` tells the user the exact phrase. Accepted.

## 4. Self-caught issues remediated this audit

- **[MEDIUM] router.py ask_router sensitive path missing pending-refusal record** → **ESCALATE TO REVIEWER.** The `ask_router` sensitive-no-token refusal (router.py ~:570) carries the envelope but does NOT call `record_pending_sensitive_refusal`, whereas `dispatch_tool_call` does. In production the Hermes chat path is `dispatch_tool_call` (tools=[...]), so the escalation handshake works for the real chat surface. A direct `ask_router` sensitive refusal (e.g. a verb-internal draft call) would render the "yes, escalate" offer but leave no pending refusal for the boundary to correlate. This is a latent gap on a non-chat path; flagging for the reviewer to decide fix-now vs defer (the chat path — the walked surface — is covered). Fail-safe: worst case the user's "yes, escalate" is a no-op (no-brick holds), never an over-authorization.
- **[MEDIUM] TTL read-then-consume micro-window** → **ACCEPT WITH RATIONALE** (10-min TTL vs µs window; fail-safe direction).
- All other §3 issues → **ACCEPT WITH RATIONALE** (documented inline above).

§3 is not shallow (6 issues, 1 escalated).

## 5. Posture Audit

### 5.1 — Lockfile hygiene
Non-dep-change story (no `requirements.txt` touch).
```
$ git diff --stat -- requirements.txt
(no output)
```
Verdict: ✅ PASS (no dependency change).

### 5.2 — Cross-doc pair verification
Cross-doc branch: the story references epics.md § Story 10.5.2 + retro §8.5 as requirement source (read, not contradicted). Schema-touching branch fires (File List includes `mailbot_api/db/migrations/026_user_confirmations.sql`) → §5.2.1.

#### 5.2.1 — Schema-touching schema-doc verification
```
Grep "user_confirmations|pending_sensitive_refusal" docs/DATABASE.md → 8 matches
```
Verdict: **MATCH** — `docs/DATABASE.md` was updated this story with full table-reference sections (purpose / key columns / indexes / data-classification / lifecycle) for both new tables + inventory rows + migration-index row 026. (Pre-existing gap noted: migration index was already stale at 018–025 — back-fill owed, not introduced here.)

### 5.3 — Lifecycle string-uniqueness check
N/A — story added no i18n keys (no graphical frontend; the three refusal message shapes are distinct prose, verified distinct by `test_sensitivity_refusal_envelope.py` shape tests).

### 5.4 — Multi-consumer impact scan
Modified shared surface: `RouterError` (added optional `refusal_envelope` field — additive, every existing consumer unaffected; None default). Mint verbs gained a precondition (consumers: MCP tool wrappers + any caller of `mint_grant`/`mint_sensitivity_token` — all now correctly require a confirmation; the 32 setup-mint test sites were updated). `main.py` chat boundary is the sole HTTP consumer.
```
Grep "refusal_envelope" mailbot_api → errors.py (def), router.py (populate ×7), main.py (read)
```
Verdict: ✅ PASS — additive field; mint-gate consumers audited + tests updated; boundary is sole render site.

### 5.5 — Screenshot-based perception check
N/A — backend-only; no graphical frontend. The refusal "rendering" is text at the Discord boundary, asserted via `TestClient` (200-shape content), not a painted UI.

### 5.6 — Upstream-contract spec coverage check
Applies: the envelope path depends on the router's sensitivity classification (`emails.sensitivity` / `sensitivity_at`). Present + absent cases both tested: confidential (present) + not-classified (absent, NULL sensitivity_at) + sensitive-with/without-token — all covered in `test_sensitivity_refusal_envelope_boundary.py` + `test_mint_requires_user_confirmation.py` (expired-confirmation absent case). Verdict: ✅ PASS.

### 5.7 — Module-level mutable container check
New `.py` files: `user_confirmation.py` (module-level: only `CONFIRMATION_TTL: timedelta` constant + a `frozenset` `_ESCALATE_PHRASES` — both immutable), `sensitivity_refusal.py` (module-level: only the frozen Pydantic model + a pure builder; no mutable container). Router/main edits add no module-level mutable state. The existing `sensitivity_tokens._REGISTRY` (module dict, AR-D12-1) is untouched.
```
Grep for module-scope let/dict/set mutation in new files → none (frozenset + Final-style constants only)
```
Verdict: ✅ PASS.

### 5.8 — Dev-fixture seed-vs-production-shape parity check
N/A — no recorded/producer-shaped JSON fixtures introduced. Test seeds insert real `emails` rows via `execute_write` against the real migrated schema (the production shape itself), not hand-crafted producer payloads.

### 5.9 — grep-verify-cited-figures
Cited figures: baseline 1728, post 1762 (+34 net), mypy 131 files.
```
$ pytest -q → "1762 passed, 2 skipped, 3 deselected" (full-suite run bij32iqeu→b8qmbwu6g)
  baseline (pre-change run b1tld11y8): "1728 passed, 2 skipped, 3 deselected"
  Δ = 1762 − 1728 = 34 ✓
$ mypy --strict mailbot_api → "Success: no issues found in 131 source files" ✓
```
Verdict: ✅ PASS — test-count delta + mypy file count recomputed from actual command output.

### 5.10 — Producer-boundary contract enforcement
§5.10.a (typed-column producers): the new writes (`user_confirmations`, `pending_sensitive_refusal`) take already-validated Python values (`email_id`/`task_type`/`action_type` strings from the router + the boundary; `email_ids` via `json.dumps`). No unguarded coercion of third-party JSON into typed columns. §5.10.b (response-shape allow-list): the envelope is an explicit-field Pydantic model (`SensitivityRefusal`) — the Graph id is NOT a field, so the refusal response cannot widen to leak it. This is the allow-list, enforced at construction. Verdict: ✅ PASS.

### 5.11 — Git-evidence consistency check

#### 5.11.a — File-List-vs-working-tree
```
git status --porcelain (scratch/ excluded):
 M mailbot_api/actions/authorization.py
 M mailbot_api/db/queries.py
 M mailbot_api/main.py
 M mailbot_api/router/errors.py
 M mailbot_api/router/router.py
 M mailbot_api/verbs/mint_sensitivity_token.py
 M tests/... (10 updated test files)
 M docs/DATABASE.md
 M _bmad-output/... (sprint-status, epics.md — planning; story file untracked)
?? mailbot_api/actions/user_confirmation.py
?? mailbot_api/db/migrations/026_user_confirmations.sql
?? mailbot_api/router/sensitivity_refusal.py
?? tests/integration/test_mint_requires_user_confirmation.py
?? tests/integration/test_sensitive_escalation_handshake.py
?? tests/integration/test_sensitivity_refusal_envelope_boundary.py
?? tests/unit/router/test_sensitivity_refusal_envelope.py
```
All File-List paths present. `docs/DATABASE.md` added to File List (§5.2.1 schema-doc). `_bmad-output/planning-artifacts/epics.md` is a pre-existing working-tree M (not this story's edit) — left as-is, not staged by this story. Verdict: ✅ PASS.

#### 5.11.b — Production-only test-to-code ratio
Approx from `wc -l` (new) + `git diff --numstat` (edits):
```
testAdded  ≈ 688 (4 new test files) + ~146 (test-file edits) = ~834
prodAdded (excl. docs + migration SQL) ≈ 381 (2 new .py) + 310 (source edits) = ~691
  (migration SQL 60 lines classed as docs per §5.11.b MailBot rule; DATABASE.md classed as docs)
prodOnlyTestRatio ≈ 834 / 691 = 1.21
```
Threshold 0.3. Verdict: ✅ PASS (1.21 ≥ 0.30).

#### 5.11.c — No-later-commits-under-attribution
Single-session dev pass; status flipped in-progress → review this session (2026-07-10). No intervening commits.
```
git log --since="2026-07-10" --oneline -- <File-List paths> → (empty)
```
Verdict: ✅ PASS.

### 5.12 — CR-cadence-mandatory surface classification
- Criterion 1 (boundary-introducing): **YES** — new user-confirmation authorization primitive that mint verbs are now gated on; a new cross-module invariant ("mints require a user-gated confirmation").
- Criterion 2 (dep-introducing): NO — no new external dep.
- Criterion 3 (dev-self-flagged): **YES** — §4 escalated the ask_router pending-refusal gap to the reviewer.
- Criterion 4 (capstone): NO.
- Criterion 5 (privacy-invariant): **YES** — touches the FR-2.3/NFR-PRIV sensitivity-refusal enforcement + the AR-D12-1 sensitivity-token lifecycle.
- Criterion 6 (load-bearing-orchestrator): **YES** — the Router refusal path + `/v1/chat/completions` boundary + the token/grant mint seam are primary integration surfaces.

Cadence verdict: **MANDATORY-CR** (criteria 1 + 3 + 5 + 6). Reviewer model ≠ dev (sonnet-5 ≠ opus-4-8), full scope.

## Posture Audit summary table

| Check | Status |
| --- | --- |
| 5.1 Lockfile hygiene | ✅ PASS (no dep change) |
| 5.2 Cross-doc pair verification | ✅ PASS (5.2.1 MATCH — DATABASE.md updated) |
| 5.3 Lifecycle string-uniqueness | N/A — no i18n keys |
| 5.4 Multi-consumer impact scan | ✅ PASS (additive RouterError field; mint consumers audited) |
| 5.5 Screenshot-based perception check | N/A — backend-only, no GUI |
| 5.6 Upstream-contract spec coverage | ✅ PASS (present + absent sensitivity cases tested) |
| 5.7 Module-level mutable container | ✅ PASS (frozenset + constants only) |
| 5.8 Dev-fixture seed-vs-production-shape parity | N/A — no producer-shaped fixtures |
| 5.9 grep-verify-cited-figures | ✅ PASS (1762/+34/131 recomputed) |
| 5.10 Producer-boundary contract enforcement | ✅ PASS (allow-list envelope; no unguarded coercion) |
| 5.11 Git-evidence consistency check | ✅ PASS (a/b/c) |
| 5.12 CR-cadence-mandatory surface classification | MANDATORY-CR |
