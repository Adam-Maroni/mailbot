---
baseline_commit: 65d9bd73a44a95bc83b59558f6b51321b2681199
---

# Story 9.5.5: `policy.yaml` v0→v1 bump — evidence-cited change OR signed no-change verdict

Status: done

## Story

As Adam,
I want the `router/policy.yaml` `version` field bumped from `v0` to `v1` with either (a) at least one routing decision changed cited to the `benchmark_runs.run_id` from Story 9.5.3 AC-5, OR (b) an Adam-signed retro entry stating the verdict was reviewed and no change was warranted with the benchmark output cited,
So that Epic 9's purpose — *the router defends its choices with evidence* — is fulfilled, and Epic 9.5 (and Epic 9 by extension) closes at "the tool informed the system" rather than "the tool was built."

## Acceptance Criteria

### AC-1 — Verdict review determines the path

**Given** Story 9.5.3 AC-5's Haiku-vs-Opus verdict is captured in walk evidence
**When** the policy.yaml change is evaluated
**Then** Adam reads the rendered report from `python -m benchmark.report --run-id <ac-5-run-id>` and reviews the verdict for `(draft_reply, haiku-vs-opus)`:
  - If `PROMOTE-needed` (Haiku is sufficient → demote draft_reply from Opus to Haiku) → policy.yaml updated, draft_reply.model changed from `claude-opus-4-7` to `claude-haiku-4-5`
  - If `DEMOTE-invalid` (Opus is materially better → keep Opus) → policy.yaml unchanged in this cell
  - If `hold-steady` or `INSUFFICIENT_DATA` → policy.yaml unchanged, verdict captured as no-change rationale
  - If `DEMOTE-valid` (current model is already cheaper alternative) → already at cheaper alternative; policy.yaml unchanged in this cell

> **Route pre-resolved upstream (2026-07-03, Adam-signed in `9.5.3-walk-evidence.md` § "AC-5 verdict interpretation"):** the verdict token for the Haiku-vs-Opus draft_reply comparison is **`INSUFFICIENT_DATA`** (opus scoring `n=0`; report's literal `PROMOTE-needed` for haiku is absolute-threshold-only and misleading in isolation). Story 9.5.5 route = **(b) accept-with-rationale** → the no-change path (AC-3) applies; the change path (AC-2) is N/A.

### AC-2 — Change path (N/A this run; verdict implies no routing change)

**Given** the change path (verdict implies routing change)
**When** policy.yaml is edited
**Then** `router/policy.yaml`'s `version` field bumps from `v0` to `v1`
**And** the changed task block contains a `# v1: changed from claude-opus-4-7 to claude-haiku-4-5 per benchmark_runs.run_id=<id> verdict=PROMOTE-needed on 2026-XX-XX` comment (or analogous for whichever direction)
**And** the DEMOTION HYPOTHESIS comment above the changed cell is updated from hypothesis-shaped to evidence-shaped: `# EVIDENCE: benchmark_runs.run_id=<id> shows ...` (replacing the prior `# DEMOTION HYPOTHESIS:` annotation)
**And** the existing Story 9-1 watchfiles hot-reload picks up the policy.yaml change without restart (verified by running a test prompt post-edit and observing the new model in audit log)

### AC-3 — No-change path (ACTIVE path this run)

**Given** the no-change path (verdict implies no routing change)
**When** the no-change verdict is documented
**Then** `router/policy.yaml`'s `version` field STILL bumps from `v0` to `v1` (the version bump signals "this policy has been benchmark-reviewed" regardless of whether the review resulted in change)
**And** the DEMOTION HYPOTHESIS comments on Opus tasks are NOT removed but ARE augmented with `# v1-review (date): benchmark_runs.run_id=<id> verdict=<verdict>; hypothesis retained because <reason>` lines
**And** an Adam-signed retro entry exists in `epic-9-5-retro-{date}.md` stating: `"policy.yaml v0→v1 reviewed against benchmark_runs.run_id=<id> output (full report at <path>). No routing changes warranted because <specific reason citing report sections>. Hypothesis annotations retained for v2 re-review."`

### AC-4 — Regression test on the load-bearing config

**Given** the v0→v1 bump touches the load-bearing config
**When** the change is tested
**Then** a new regression test `tests/integration/test_policy_v1_loads_cleanly.py` verifies: (1) `load_policy()` succeeds on the new v1 file, (2) the version field is correctly read as `"v1"` (cohort_key composition depends on this per Story 9-6's `router_policy_version` 4-tuple component), (3) any user-overrides from Story 9-4 still merge cleanly via shallow-leaf semantics on top of v1

### AC-5 — cohort_key propagation

**Given** the cohort_key 4-tuple's `router_policy_version` component will now change from `v0` to `v1`
**When** new benchmark runs fire post-v1
**Then** Story 9-6's `benchmark_runs.cohort_key` for new runs uses the v1 component (verified by running a single corpus item through `benchmark.runner` post-v1 and inspecting the resulting `cohort_key` SHA-256)
**And** historical v0 runs remain queryable (the cohort_key change does NOT invalidate prior rows — Story 9-9's cross-cohort drift section handles this naturally)

### AC-6 — MANDATORY-CR full scope

**Given** this is the load-bearing closure story for Epic 9.5 + Epic 9
**When** CR cadence is evaluated per the 6 criteria
**Then** criterion 6 (load-bearing — this story converts hypothesis to evidence on the production routing policy) fires → **MANDATORY-CR per §5.12** with FULL scope (not reduced-scope) — the policy.yaml edit is reviewed for: correct evidence citation, correct version field bump, no accidental non-cited mutations, hypothesis annotations correctly augmented (not silently removed), and regression test correctness

## Tasks / Subtasks

- [x] Task 1 — Verify citation evidence + confirm the route (AC: 1) — run_id `db48480f-9692-4791-b3e2-4b3a2ab1fed8` + verdict `INSUFFICIENT_DATA` + route (b) confirmed in `9.5.3-walk-evidence.md` §§ Section 2 / AC-5 verdict interpretation; report artifact `9.5.3-report-db48480f.md` exists on disk; 3 honesty caveats recorded (opus n=0, calibration_mae contamination flag, single-evaluator-trusted v1 posture)
  - [x] Re-read `9.5.3-walk-evidence.md` §§ "Section 2 — AC-5" + "AC-5 verdict interpretation"; confirm `run_id=db48480f-9692-4791-b3e2-4b3a2ab1fed8`, verdict token `INSUFFICIENT_DATA`, route (b)
  - [x] Confirm the rendered report artifact exists at `_bmad-output/implementation-artifacts/9.5.3-report-db48480f.md` (the `benchmark.report` output copied from the container — Adam already read it during the 9.5.3 walk; no re-render needed)
  - [x] Record the honesty caveats that must appear in the rationale: (i) opus scoring `n=0` → no valid relative comparison; (ii) 9.5.3's `calibration_mae=0.0` retroactively flagged contaminated by F-ANCHOR-ANSWER-KEY-LEAK (9.5.4); (iii) eval pipeline is single-evaluator-trusted for v1 per the 9.5.4 Path B posture
- [x] Task 2 — RED: regression test `tests/integration/test_policy_v1_loads_cleanly.py` (AC: 4) — 5 tests authored (4 AC-4/no-change-guard + 1 AC-5 E2E); RED confirmed: exactly the 3 v1-dependent tests fail on `'policy-v0-2026-06-01' != 'policy-v1-2026-07-04'`, the 2 version-independent tests pass (loader + routing-unchanged guard)
  - [x] Test (1): `load_policy()` succeeds on the repo's real `router/policy.yaml`
  - [x] Test (2): the loaded `PolicyTable.version` is the v1 value (`policy-v1-2026-07-04`) — must FAIL while the file still says `policy-v0-2026-06-01`
  - [x] Test (3): a Story 9-4-style user-overrides file merges cleanly via shallow-leaf semantics on top of the real v1 baseline (`+overrides:` suffix appears; non-overridden fields preserved)
  - [x] Run the new test file; confirm test (2) fails RED for the right reason
- [x] Task 3 — GREEN: edit `router/policy.yaml` (AC: 3) — version bumped + 1 header comment block + 3 per-task v1-review comment blocks; `git diff` verified 16 insertions / 1 deletion, ALL comments except the version line itself; zero routing mutations; new test file 5/5 green
  - [x] Bump `version: "policy-v0-2026-06-01"` → `version: "policy-v1-2026-07-04"`
  - [x] Augment `draft_reply`'s `demotion_hypothesis` with a comment line: `# v1-review (2026-07-04): benchmark_runs.run_id=db48480f-9692-4791-b3e2-4b3a2ab1fed8 verdict=INSUFFICIENT_DATA; hypothesis retained because opus scoring returned n=0 (no valid Haiku-vs-Opus comparison; see 9.5.3-walk-evidence.md)` + haiku-below-threshold secondary reason
  - [x] Augment the other two Opus-task `demotion_hypothesis` blocks (`tone_style_mirror`, `multi_turn_refinement`) with honest v1-review lines stating they were NOT covered by the run (draft_reply-only benchmark) and are retained unreviewed for v2 — do NOT cite the run_id as if it reviewed them
  - [x] NO other mutations — no model changes, no lane/token/prompt_version edits, hypothesis fields themselves untouched
  - [x] Re-run the new test file; all green
- [x] Task 4 — cohort_key propagation check (AC: 5) — `test_cohort_key_uses_v1_component_and_v0_rows_stay_queryable` runs the 5-item canary through the REAL `benchmark.runner` twice (v0-snapshot "historical" run, then real repo v1 policy) with scripted qwen adapter + `--cost-mock` ($0): v1 rows carry `router_policy_version=policy-v1-2026-07-04` with stored cohort_key recomputable via `compute_cohort_key` from row-frozen components; v0 rows stay fully queryable post-v1 with a disjoint cohort_key
  - [x] Add test(s) asserting: `compute_cohort_key(..., router_policy_version="policy-v1-2026-07-04")` differs from the same tuple with the v0 component, and a single corpus item pushed through the `benchmark.runner` path (E2E canary pattern from `tests/integration/test_benchmark_e2e_canary.py`, mock cost env — $0 spend) records `router_policy_version` = the freshly-loaded v1 value in `benchmark_runs`
  - [x] Assert historical-v0-queryability: rows with the old cohort_key remain readable alongside v1 rows (no invalidation)
- [x] Task 5 — Draft the route-(b) retro entry (AC: 3, third clause) — section inserted before "Other retro seeds" in the retro seed doc; 3-reason rationale (INSUFFICIENT_DATA relative comparison / haiku-below-threshold absolute signal / evaluator-trust caveats) + hypothesis-retention sentence; explicitly marked PENDING-ADAM-SIGNATURE
  - [x] Append to `_bmad-output/implementation-artifacts/epic-9-5-retro-2026-07-04.md` a section: `## policy.yaml v0→v1 — route (b) no-change verdict (Story 9.5.5)` containing the AC-mandated sentence citing `benchmark_runs.run_id=db48480f-9692-4791-b3e2-4b3a2ab1fed8`, the report path, the specific no-change reasons (opus n=0 / INSUFFICIENT_DATA / single-evaluator-trusted posture), and "Hypothesis annotations retained for v2 re-review."
  - [x] Mark the entry `**Signature status: PENDING-ADAM-SIGNATURE**` — the autonomous run may NOT self-sign; signature is requested at the run's Phase 3.5 manual-verification gate (PASS verdict = signature granted, entry then edited to Adam-signed)
- [x] Task 6 — Gates + Dev Agent Record (AC: 4, 6)
  - [x] ruff full repo clean; mypy --strict `mailbot_api` clean; full pytest suite green (baseline 1680 passed + 2 skipped + 3 deselected; expect +net-new tests from Tasks 2/4) — result: 1685+2+3 (+5 net). Ruff caveat: 2 pre-existing T201 errors in UNTRACKED `scratch/walk_bootstrap.py` (9.5.3/9.5.4 walk leftover, outside story scope + outside git index — flagged, not fixed)
  - [x] Fill Dev Agent Record (model, debug log, completion notes per AC, file list, change log)

## Dev Notes

### Route + citation facts (pre-resolved upstream — do not re-litigate)

- **Route (b) accept-with-rationale** was Adam-decided at 9.5.3 close (2026-07-03, Adam-signed) and re-confirmed by the 9.5.4 close ("Story 9.5.5 gate: Path-B-accept-with-rationale"). The dependency gate (9.5.4 α≥0.6 OR accept-with-rationale) is satisfied via the Adam-signed Path B entry in `epic-9-5-retro-2026-07-04.md`.
- **Citation source:** `benchmark_runs.run_id=db48480f-9692-4791-b3e2-4b3a2ab1fed8` (AC-5 run, 186/186 dispatched ok, 93 items × 2 models on draft_reply). Report artifact: `_bmad-output/implementation-artifacts/9.5.3-report-db48480f.md`.
- **Why no change is warranted (rationale skeleton):** report's `(draft_reply)` cell shows haiku `subjective_overall=2.6176 (n=34)` with `PROMOTE-needed` fired on haiku's ABSOLUTE quality threshold only; opus has `n=0` scored rows (scoring-pipeline defect, investigation deferred) → no valid RELATIVE Haiku-vs-Opus comparison → `INSUFFICIENT_DATA` → per AC-1 bullet 3, policy.yaml unchanged, verdict captured as no-change rationale. Additionally: haiku's 2.62/5 quality is BELOW the ~3.0 hold-steady threshold, so even the absolute signal argues AGAINST demoting draft_reply to haiku.
- **Honesty caveats to carry into the rationale:** 9.5.3's `calibration_mae=0.0` was retroactively flagged contaminated (F-ANCHOR-ANSWER-KEY-LEAK, fixed in 9.5.4); the eval pipeline is **single-evaluator-trusted (opus-only) for v1** per the Adam-signed 9.5.4 Path B entry; α≥0.6 is a v2 gate.

### Technical requirements

- **`router/policy.yaml` current state:** `version: "policy-v0-2026-06-01"` (line 12). The "v0→v1 bump" preserves the field's `policy-vN-YYYY-MM-DD` format → new value `"policy-v1-2026-07-04"`.
- **Hypothesis annotations are structured fields, not comments:** `draft_reply` (line 41), `tone_style_mirror` (line 145), `multi_turn_refinement` (line 155) carry `demotion_hypothesis:` Pydantic fields (`mailbot_api/router/policy.py` `PolicyEntry`). The AC's "DEMOTION HYPOTHESIS comments … augmented" maps to: add `# v1-review (…)` YAML comment lines adjacent to those fields; never delete or rewrite the field values.
- **Only `draft_reply` was benchmark-reviewed.** The run was draft_reply-only; v1-review comments on `tone_style_mirror` / `multi_turn_refinement` must say "not covered by this run", NOT cite the run_id as evidence about them.
- **Loader:** `mailbot_api.router.policy.load_policy(path, *, overrides_path=None) -> PolicyTable`; validation-or-no-swap; overrides merge per Story 9-4 shallow-leaf semantics with `+overrides:<sha256[:8]>` version suffix (`_compute_merged_version`). YAML comments are invisible to the parser — the version string and comment additions are the ONLY semantic+cosmetic deltas.
- **cohort_key:** `benchmark/cohort.py::compute_cohort_key(prompt_version, scorer_model, anchors_version, router_policy_version)` — SHA-256[:16] over pipe-joined 4-tuple; `router_policy_version` = `PolicyTable.version` at run-start (passed through `benchmark/runner.py` → `benchmark/db.py` `benchmark_runs.router_policy_version` + `cohort_key` columns). Pure function — historical rows are untouched by a new version value.
- **Watchfiles hot-reload:** version-only edits fire `policy.reloaded` (Story 9-1); validation-or-no-swap protects the live container. No restart needed; live verification is optional at Phase 3.5 (AC-2's audit-log check is change-path-only, N/A here).

### File structure requirements

- Modified: `router/policy.yaml` (version line + comment lines only)
- New: `tests/integration/test_policy_v1_loads_cleanly.py` (AC-4; may also host the AC-5 cohort_key assertions or those may extend `tests/integration/test_benchmark_e2e_canary.py` — dev's choice, keep placement consistent with existing patterns)
- Modified: `_bmad-output/implementation-artifacts/epic-9-5-retro-2026-07-04.md` (route-(b) entry, PENDING-ADAM-SIGNATURE)

### Testing requirements

- Framework: pytest; integration tests live in `tests/integration/`; policy-test patterns in `test_policy_user_overrides_lifespan.py` (module-state reset fixture `_reset_policy_module`, tmp_path YAML fixtures) and E2E runner pattern in `test_benchmark_e2e_canary.py` (BENCHMARK_COST_MOCK env, $0 spend).
- Full-suite baseline at story start: **1680 passed + 2 skipped + 3 deselected** (live marker auto-excluded via `addopts`).
- Gates: `ruff check .` full repo; `mypy --strict mailbot_api`; full pytest.
- **Zero real spend:** all AC-5 verification via mock/fake adapters — no Anthropic dispatch. Spend-truth rule (`feedback_anthropic_spend_source_of_truth.md`) not triggered.

### References

- `_bmad-output/planning-artifacts/epics.md` § "Story 9.5.5" (lines 3697-3739) — AC source of truth
- `_bmad-output/implementation-artifacts/9.5.3-walk-evidence.md` §§ "Section 2 — AC-5" + "AC-5 verdict interpretation" (route decision, run_id, rationale skeleton)
- `_bmad-output/implementation-artifacts/epic-9-5-retro-2026-07-04.md` (Path B Adam-signed entry; filing target for the route-(b) entry)
- `mailbot_api/router/policy.py` (loader/merge/version semantics), `benchmark/cohort.py` (cohort_key), `benchmark/db.py` (benchmark_runs columns)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` lines 259-265 (Epic 9.5 state + dot-notation convention)

## Dev Agent Record

### Agent Model Used

claude-fable-5 (dev, via autonomous-story-run inline walk); review model: sonnet (claude-sonnet-5) — story-spec'd sonnet-4-6 substituted per 9.5.4 D2 reviewer-model-substitution blessing precedent

### Debug Log References

- Route (b) no-change path pre-resolved upstream (9.5.3 walk evidence, Adam-signed 2026-07-03; 9.5.4 close gate token `Path-B-accept-with-rationale`) — AC-2 change path N/A by construction; no re-litigation.
- RED discrimination was clean: exactly the 3 v1-dependent tests failed on the version string; the 2 version-independent tests (loader validity, routing-unchanged guard) passed pre-edit — proving the guard tests don't depend on the edit under test.
- AC-5's "run a single corpus item through benchmark.runner post-v1" satisfied at L2 via the 9-8 E2E canary pattern (real `runner_main`, real Router precondition/lane/cost path, scripted qwen adapter at the registry boundary, `--cost-mock`, $0 spend). The "historical v0 run" is simulated by `model_copy(update={"version": v0})` on the freshly-loaded real v1 table — all other cohort components byte-identical, isolating the version component exactly.
- `demotion_hypothesis` is a structured Pydantic field, not a comment — AC-3's "augment the DEMOTION HYPOTHESIS comments" implemented as adjacent `# v1-review (2026-07-04)` YAML comment blocks; field values untouched (YAML comments invisible to the parser; `git diff` shows 16 insertions of which 15 are comment lines + 1 version-value change, 1 deletion = old version line).
- `tone_style_mirror` + `multi_turn_refinement` v1-review lines deliberately do NOT cite the run_id — the run was draft_reply-only; citing it as evidence about uncovered cells would be false citation.
- Retro entry drafted PENDING-ADAM-SIGNATURE — autonomous run cannot self-sign; signature requested at Phase 3.5 (PASS verdict = granted).
- Pre-existing ruff T201 ×2 in untracked `scratch/walk_bootstrap.py` (9.5.3/9.5.4 walk leftover) — outside story scope and outside the git index; flagged in run-flags, not fixed.

### Completion Notes List

- AC-1: Verdict reviewed against the rendered report artifact `9.5.3-report-db48480f.md` (+ walk-evidence interpretation): `(draft_reply, haiku-vs-opus)` = `INSUFFICIENT_DATA` (opus n=0) → bullet-3 path: policy.yaml routing unchanged, verdict captured as no-change rationale.
- AC-2: N/A — change path not taken (no routing change; hot-reload audit-log check is change-path-only).
- AC-3: `router/policy.yaml` version bumped `policy-v0-2026-06-01` → `policy-v1-2026-07-04`; hypothesis annotations on all 3 Opus tasks augmented (never removed) with `# v1-review (2026-07-04)` lines; route-(b) retro entry drafted in `epic-9-5-retro-2026-07-04.md` § "policy.yaml v0→v1 — route (b) no-change verdict (Story 9.5.5)" with run_id + report-path citation and 3-reason rationale — **PENDING-ADAM-SIGNATURE** (granted at Phase 3.5 PASS).
- AC-4: `tests/integration/test_policy_v1_loads_cleanly.py` — 4 tests: real-file `load_policy()` success; `version == "policy-v1-2026-07-04"`; Story 9-4 shallow-leaf overrides merge on v1 (`+overrides:` suffix, sibling leaves + untouched tasks preserved); route-(b) routing-unchanged guard (draft_reply stays Opus, hypotheses non-null).
- AC-5: `test_cohort_key_uses_v1_component_and_v0_rows_stay_queryable` — real runner E2E ×2 (v0-snapshot historical run + real v1 policy run): v1 rows freeze `router_policy_version=policy-v1-2026-07-04` with stored `cohort_key` recomputable via `compute_cohort_key`; v0 rows remain queryable post-v1 with disjoint cohort_key.
- AC-6: MANDATORY-CR FULL scope EXECUTED — round 1 under claude-sonnet-5 (dev claude-fable-5; different-model contract honored; sonnet-4-6→sonnet-5 substitution per 9.5.4 D2 blessing): 3 findings (1 MEDIUM + 2 LOW), 2 Patches applied + 1 Defer = 100% actionable applied-rate. CR-F1 model pins added to routing-unchanged guard; CR-F3 conflation-defect honesty amendment to policy.yaml comment + retro entry; CR-F2 deferred with rationale (cohort component task-agnostic by construction). Fixes trivial (comment lines + 4 test assertions) → no round-2 per skill triage rule. Gates re-run post-fix: ruff clean, mypy clean, full suite 1685+2+3 green.
- Gates: ruff (story surface clean; 2 pre-existing T201 in untracked scratch/), mypy --strict mailbot_api clean (129 files), full suite **1685 passed + 2 skipped + 3 deselected** (+5 net vs 1680 baseline).

### File List

- `router/policy.yaml` (modified — version bump + 4 comment blocks incl. CR-F3 conflation amendment; zero routing mutations)
- `tests/integration/test_policy_v1_loads_cleanly.py` (new — 5 tests incl. CR-F1 model pins)
- `_bmad-output/implementation-artifacts/epic-9-5-retro-2026-07-04.md` (modified — route-(b) entry, PENDING-ADAM-SIGNATURE, CR-F3 amendment)
- `_bmad-output/implementation-artifacts/9.5.5-policy-yaml-v0-to-v1-bump.md` (this story file)
- `_bmad-output/implementation-artifacts/9.5.5-policy-yaml-v0-to-v1-bump.pre-review.md` (new — Step 2.3.5 self-audit artifact)

### Change Log

- 2026-07-04 — policy.yaml v0→v1 bump shipped via route (b) no-change path: version `policy-v1-2026-07-04`, hypothesis annotations augmented on 3 Opus tasks, route-(b) retro entry drafted (pending Adam signature), +5 regression tests (v1 load/version/overrides-merge/no-routing-change + cohort_key v1-propagation E2E).
- 2026-07-04 — MANDATORY-CR round 1 (claude-sonnet-5): CR-F1 model pins + CR-F3 conflation-defect honesty amendments applied; CR-F2 deferred with rationale; 2/2 actionable = 100% applied-rate; gates re-verified green.

## Completion Notes

### 2026-07-04 — done-flip (autonomous-story-run Step 2.4.8 truncation)

Prior sprint-status narrative (review phase): "dev pass complete (route (b) no-change: version→policy-v1-2026-07-04, 3 hypothesis augments, retro entry PENDING-ADAM-SIGNATURE, +5 net tests at 1685+2+3, 4 gates green); pending MANDATORY-CR FULL scope."

Done-flip detail: MANDATORY-CR round 1 executed under claude-sonnet-5 (FULL scope per AC-6): 3 findings, CR-F1 (MEDIUM, model pins on routing-unchanged guard) + CR-F3 (LOW, F-COHORT-KEY-LEGACY-SCORE-CONFLATION honesty amendment to policy.yaml comment + retro entry) APPLIED; CR-F2 (LOW, coarse_class-vs-draft_reply dispatch shape) DEFERRED with rationale — 2/2 actionable = 100% applied-rate; no round 2 (fixes trivial). Gates re-verified post-fix: ruff clean (story surface; 2 pre-existing T201 in untracked scratch/ flagged out-of-scope), mypy --strict clean, full suite 1685 passed + 2 skipped + 3 deselected (+5 net vs 1680 baseline). Done-gates: 2.4.4 PASS, 2.4.5 N/A (no frontend), 2.4.6 PASS (5/5 File List paths tracked), 2.4.7 N/A (config/tests/docs only — no new verbs, endpoints, or state-changing writes), 2.4.8 this entry. AC-3's Adam-signature remained PENDING at done-flip by design — granted or refused at the run's Phase 3.5 manual-verification gate.

### 2026-07-05 — Phase 3.5 manual verification (delegated walk, Adam-directed)

Adam directed the run to execute the Phase 3.5 walk itself ("Run manual verification yourself"). All 5 checkpoints PASS: (1) citation chain re-verified verbatim against 9.5.3 walk evidence; (2) policy.yaml v1 state + honest comments + routing untouched; (3) retro entry content satisfies the AC-3 template; (4) regression tests 5/5 green on re-run; (5) **live L3 evidence** — the running `mailbot-api` container (up 34h) logged `policy.reloaded` with `"version":"policy-v1-2026-07-04"` at timestamps matching each edit (21:45/21:53/22:00 UTC 2026-07-04): watchfiles hot-reload picked up the bump with no restart. **Verdict: PASS. AC-3 signature granted by Adam via explicit Phase 3.5 PASS verdict; retro entry now reads `Signed: Adam Maroni, 2026-07-05.`** Story fully closed; Epic 9 done-flip clause 11 (policy informed by benchmark evidence) discharged.

## Review Findings — MANDATORY-CR round 1 (2026-07-04, reviewer: claude-sonnet-5)

### Triage (2026-07-04, dev claude-fable-5): 2 Patches applied + 1 Defer = 2/2 actionable applied (100%)

### Action Items
- [x] [CR-F1][MEDIUM] tests/integration/test_policy_v1_loads_cleanly.py:88-102 — `test_v1_bump_did_not_change_routing` (the AC-6 clause-(c) "no accidental non-cited mutations" guard test) asserts `draft_reply.model == "claude-opus-4-7"` but only checks `demotion_hypothesis is not None` for `tone_style_mirror` and `multi_turn_refinement` — it never asserts those two tasks' `.model` field is unchanged. Failure scenario: a future edit (or a bad merge/rebase) that silently changes `tone_style_mirror.model` or `multi_turn_refinement.model` from `claude-opus-4-7` to something else would pass this "routing-unchanged guard" test undetected — the two comment-augmented Opus cells that were explicitly called out in the diff as "NOT covered by this run, hypothesis retained" have a weaker regression guard than the one cell (`draft_reply`) that was actually benchmarked. **[APPLIED — `.model == "claude-opus-4-7"` pins added for both cells; test re-run green]**
- [ ] [CR-F2][LOW] tests/integration/test_policy_v1_loads_cleanly.py:207-296 — AC-5's E2E test dispatches the `coarse_class` task (mirroring the Story 9-8 canary precedent) rather than `draft_reply`, the task that was actually benchmark-reviewed and is the subject of the route-(b) rationale. This is defensible (coarse_class is the only task with a registered non-Anthropic scripted-adapter precedent and $0 spend), and is self-flagged in the pre-review §3, but it means AC-5's "cohort_key propagation" verification never exercises a real dispatch through the specific policy cell (`draft_reply`) whose model/hypothesis fields are the story's actual subject — a future refactor of `_read_policy_versions()` or the per-task `prompt_version` plumbing could regress `draft_reply`'s cohort_key composition specifically without this test noticing, since the test's `router_policy_version` assertion is task-agnostic but its dispatch path never touches the `draft_reply` cell. **[deferred: cohort composition is task-agnostic by construction (`_read_policy_versions()` snapshots `PolicyTable.version` once per run, not per task — the component under test cannot vary by task); dispatching draft_reply would require authoring a draft_reply-shaped scripted adapter + content builder fixture, duplicating the 9-8-precedent surface for no additional coverage of the version component. Reviewer itself rates the choice defensible. Logged to epic-9-5-run-flags.md.]**
- [x] [CR-F3][LOW] router/policy.yaml:46-53 — the `draft_reply` v1-review comment cites "haiku's absolute `subjective_overall=2.6176 (n=34)` sits below the ~3.0 hold-steady threshold" as a reason to retain Opus, but never states that the 34-row figure is drawn from the SAME conflated-cohort report rows that the walk-evidence documents as mixing a legacy pre-flight scorer run with the real run under one `cohort_key` (F-COHORT-KEY-LEGACY-SCORE-CONFLATION, `9.5.3-walk-evidence.md` line 301). The comment's own citation trail (via the retro entry) does surface the evaluator-trust caveats, but a reader of policy.yaml in isolation (without following the retro-entry cross-reference) would not learn that the n=34 sample itself came from a report artifact with a known, filed conflation defect — only that the *evaluator* is single-evaluator-trusted, which is a different caveat than the *conflated-rows* caveat. **[APPLIED — draft_reply comment now names F-COHORT-KEY-LEGACY-SCORE-CONFLATION on the n=34 rows; retro entry reason 3 extended with the same defect + cohort_key cite (claim verified against 9.5.3-walk-evidence.md:301 before editing)]**
