# Pre-Review Self-Audit — 10-5-3

**Generated:** 2026-07-10 by claude-opus-4-8[1m]
**Story file:** _bmad-output/implementation-artifacts/10-5-3-never-wired-capabilities-draft-pipeline-enrichment-digest-thread.md
**Status at audit time:** in-progress (post dev-pass Tasks 1-5, pre code-review Task 6)

## 1. AC-vs-code drift scan

- **AC-1 (F-10-5-11 draft pipeline chat call site): MATCH.** `draft_reply` registered as MCP tool #26 → `handle_draft_reply`; integration test proves a real `router_calls` row for `task_type='draft_reply'` via a registered fake Opus adapter (no monkeypatched ask_router). Live real-Opus is the Task 7 Adam-hands-on clause (correctly out of dev-pass scope per HYBRID run-mode).
- **AC-2 (F-10-4-3 get_thread reachable): MATCH.** `thread_id` added to `EMAIL_PROJECTION_COLUMNS` + `EmailProjection` + `row_to_projection`; round-trip find_emails→get_thread proven. Implement path chosen (recorded).
- **AC-3 enrichment (F-10-4-4): MATCH.** `_run_enrichment_step` fires enrich_sender+enrich_thread on the ingest path, best-effort. Wire path chosen (recorded).
- **AC-3 digest (F-10-4-6): MATCH (re-doc path).** Confirmed Hermes-side-only intro call site; honest re-doc in README + Hermes follow-up filed. Boundary recorded — the AC explicitly permits "OR its non-wiring is honestly re-documented."
- **AC-4 (MANDATORY-CR): MATCH.** Cadence verdict MANDATORY-CR (§5.12 below); Task 6 dispatches reviewer sonnet-5 ≠ dev opus-4-8.

No DRIFT found; no AC text required amendment.

## 2. File-List-vs-git diff check

`git status --porcelain` + `git diff --cached --numstat` run (see §5.11.a). All 16 File-List paths staged and present. Test-to-prod ratio 624/191 = 3.27 (heavily test-positive; §5.11.b PASS). `scratch/`, `.claude/settings.json`, `epics.md` are pre-existing background work NOT in this story's File List and NOT staged (verified — they remain in `git status` unstaged).

## 3. Adversarial self-review

- [LOW] `mcp_server.py` draft_reply wrapper — the `state="router_error"` branch reads `outcome.router_error.code.value`; if `router_error` were None on a router_error state (contract says it isn't) the guard falls back to "PROVIDER_ERROR". Defensive, correct.
- [MEDIUM] `pipeline.py` `_run_enrichment_step` — enrichment fires on EVERY `process_email`, including re-runs of already-derived emails. Mitigated: enrich_sender/enrich_thread are cache-checked (Rule A) and short-circuit, so re-runs are one cheap cache-read each, not a Router call. No cost regression.
- [MEDIUM] `pipeline.py` — enrichment reads `sender_id` (not `from_address`); an email whose `sender_id` is NULL (older rows before sender upsert) silently skips sender enrichment. Acceptable: best-effort by design; logged. Thread enrichment likewise skips NULL thread_id.
- [LOW] `draft_reply` sensitivity gate — the wrapper does NOT bypass the orchestrator's confidential/sensitive gate; a confidential email returns `confidential_refused` before any Router call (verified in existing orchestrator tests). No privacy regression from exposing the tool.
- [LOW] `EmailProjection.thread_id` default None — pre-existing `EmailProjection(...)` construction sites (compose_digest etc.) stay valid; verified 140 verb+MCP tests green.
- [MEDIUM] Pause interaction — `draft_reply` reaches Opus only via `ask_router`, which reads the authoritative pause row (Story 10.5.1); a paused router refuses the dispatch. The tool is NOT on `_tool_on_pause_allowlist` (which is `dispatch_tool_call`-scoped, a different surface), so no containment gap. Verified by reading the router.py pause gate.
- [LOW] `EMAIL_SENDER_THREAD_SELECT` — new raw SQL, but lives in `db/queries.py` (Rule G writer-monopoly), consumed via the connection helpers. Boundary-clean.

## 4. Self-caught issues remediated this audit

- MEDIUM (enrichment on re-run): **ACCEPT WITH RATIONALE** — cache short-circuit makes re-runs a cheap cache-read; no Router cost. Not a fix.
- MEDIUM (NULL sender_id/thread_id skip): **ACCEPT WITH RATIONALE** — best-effort contract; logged; the AC permits "wired OR honestly documented" and wiring-with-graceful-skip is honest.
- MEDIUM (pause interaction): **ACCEPT WITH RATIONALE** — verified the ask_router authoritative-pause gate covers the draft path; documented in story Task 1 notes + §5.7.
- LOW items: **ACCEPT** (defensive, no behavior change).
- One item flagged to the reviewer: **ESCALATE TO REVIEWER** — confirm the `draft_reply` wrapper's `DraftReplyOutcome`→dict serialization loses no field the Hermes chat surface needs (I mapped all 8 fields, but the reviewer should sanity-check against the Hermes consumer contract, which is outside this repo).

## 5. Posture Audit

### 5.1 Lockfile hygiene
`requirements.txt` not in File List (no dep change).
```
$ git diff --cached --stat -- requirements.txt
(no output)
```
Verdict: N/A — no dependency change.

### 5.2 Cross-doc pair verification
Cross-doc claims: README limitations updated to match code state.
```
Claim: "draft_reply is a registered MCP tool" (README:387 + SKILL.md)
Verification:
  $ Grep "draft_reply" hermes-config/skills/mailbot/SKILL.md  → "### `draft_reply`" heading present + frontmatter "via 26 MCP tools"
  $ Grep '"draft_reply"' mailbot_api/mcp_server.py             → handlers dict + _TOOL_DESCRIPTIONS entry present
Verdict: MATCH — README + SKILL.md + registry agree (tool count 26 in all three; drift test test_frontmatter_mcp_tool_count_matches_registered_count PASSES).
```
§5.2.1 schema-touching: File List contains NO migrations paths (no `mailbot_api/db/migrations/*` — the thread_id column already existed; only `EMAIL_PROJECTION_COLUMNS` string + a new SELECT constant). N/A.

### 5.3 Lifecycle string-uniqueness
N/A — story added no i18n keys (no graphical frontend; Discord/CLI surfaces).

### 5.4 Multi-consumer impact scan
`EMAIL_PROJECTION_COLUMNS` is consumed by `FIND_EMAILS_SELECT_BASE` + `GET_THREAD_PROJECTION_SELECT`; `row_to_projection` by `find_emails` + `get_thread`.
```
Modified shared surface: EMAIL_PROJECTION_COLUMNS / EmailProjection / row_to_projection
Consumers: find_emails (PRIMARY), get_thread (SECONDARY — same column list, verified thread_id appears on its projections too)
Verdict: 2 consumers; thread_id appended LAST so positional row[0..9] mapping unchanged; both verified (round-trip test asserts get_thread projections carry thread_id). 140 verb+MCP tests green.
```

### 5.5 Screenshot-based perception check
N/A — backend-only; no user-visible painted UI (AC-1's live verification is a chat interaction, Adam-hands-on Task 7, not a paint-cycle claim).

### 5.6 Upstream-contract spec coverage
N/A — story is additive (new tool registration, new projection field, new pipeline step). No consumption of an upstream-stripped/role-gated field. The `draft_reply` orchestrator's sensitivity gate is pre-existing (Story 5-9), unchanged.

### 5.7 Module-level mutable container (Python overlay)
```
$ git diff --cached -- 'mailbot_api/**/*.py' | grep -E '^\+\s*(_[a-zA-Z_]+|[A-Z_]+)\s*[:=]\s*(\{|\[|set\(|dict\(|list\()'
(no output)
```
Verdict: ✅ PASS — no module-level mutable container added. New module-level symbols are string constants (`EMAIL_SENDER_THREAD_SELECT`) and appended text in `EMAIL_PROJECTION_COLUMNS` (immutable str). No mutable dict/list/set at module scope; no counters.

### 5.8 Dev-fixture seed-vs-production-shape parity
New test fixtures are DB-real (real migrations + real SQLite via `apply_pending_migrations` + `execute_write` against the actual `emails`/`senders`/`threads` schema) and adapter fakes return the real `AdapterResponse`/prompt-output Pydantic shapes (`DraftReplyOutput`, `daily_digest_intro`, etc.). No hand-crafted consumer-shaped JSON fixture.
Verdict: ✅ PASS — fixtures anchor on real producer shapes (migrations-driven DB rows + real adapter response models).

### 5.9 grep-verify-cited-figures
```
Cite: "26 MCP tools" (SKILL.md frontmatter + _EXPECTED_TOOL_COUNT + tests)
  $ .venv/Scripts/python.exe -m pytest tests/integration/test_mcp_server.py -q  → registers_26 + list_tools count-26 PASS
Cite: "1785 passed, 2 skipped, 3 deselected" (Completion Notes)
  $ .venv/Scripts/python.exe -m pytest -q  → "1785 passed, 2 skipped, 3 deselected" (background run bknus0rnr, 223.56s)
  Verdict: MATCH.
Cite: "+6 net tests" — 3 new test files × 2 tests each = 6. testAdded numstat 624 lines. MATCH (test-count basis, not line basis).
Cite: "mypy 131 files" — $ mypy --strict mailbot_api → "Success: no issues found in 131 source files". MATCH.
```
Verdict: ✅ PASS — all numeric cites command-anchored.

### 5.10 Producer-boundary contract enforcement
§5.10.a typed-column: no unguarded `int()`/`Decimal()`/`fromisoformat()` on third-party JSON added. The `draft_reply` wrapper passes agent-string kwargs into a frozen `DraftReplyRequest` (Pydantic-adjacent dataclass); the orchestrator owns validation. New SELECT returns `(sender_id, thread_id)` — plain str/None, no coercion.
§5.10.b response-shape: `EmailProjection` gained `thread_id` (a Graph conversation id — NOT a credential/PII-sensitive field; it's already exposed on `hydrate_email`). The projection SELECT is an explicit column allow-list (never `SELECT *`); `thread_id` is a deliberate, non-sensitive addition. `draft_reply`'s returned dict explicitly enumerates 8 fields (allow-list), `router_error` dumped via `model_dump()` (already-sanitized RouterError).
Verdict: ✅ PASS — no unguarded coercion; projection widening is an explicit non-sensitive column; response shapes are field-enumerated allow-lists.

### 5.11 Git-evidence consistency check
#### 5.11.a File-List-vs-working-tree
```
$ git diff --cached --numstat  → 16 files (all in File List; see §2)
Unstaged/untracked NOT in File List: .claude/settings.json (M), _bmad-output/planning-artifacts/epics.md (M), scratch/ (??), .autonomous-run-active.json (??)
  → all pre-existing background/run-control, NOT staged, NOT story-scope. UNTRACKED+NOT-IN-LIST → informational, pass.
Verdict: ✅ PASS — every File-List path staged; no silent scope-creep staged.
```
#### 5.11.b Production-only test-to-code ratio
```
testAdded = 221+12+4+281+102+4 = 624
prodAddedExcludingDocs = queries 10 + pipeline 91 + mcp_server 81 + find_emails 1 + schemas 8 = 191
prodOnlyTestRatio = 624 / 191 = 3.27
Threshold: 0.3
Verdict: ✅ PASS — 3.27 ≥ 0.30 (heavily test-positive).
```
#### 5.11.c No-later-commits-under-attribution
Single-session dev pass; story flipped in-progress this session (2026-07-10), no commits made (stage-only skill).
Verdict: ✅ PASS — N/A (single-session, nothing committed).

### 5.12 CR-cadence-mandatory surface classification
```
Criterion 1 (boundary-introducing): NO — no new writer-monopoly/lint boundary (EMAIL_SENDER_THREAD_SELECT lives in the existing queries.py Rule-G monopoly).
Criterion 2 (dep-introducing): NO — no new deps.
Criterion 3 (dev-self-flagged): YES — §4 has one ESCALATE-TO-REVIEWER item (draft_reply dict serialization vs Hermes consumer contract).
Criterion 4 (capstone): NO — 10-5-3 is mid-epic (Cluster C).
Criterion 5 (privacy-invariant): YES — draft_reply exposes the sensitivity-gated draft path to chat (FR-2.3/FR-2.5 + AR-D12 sensitivity-token surface); registering it as a reachable tool touches the privacy-invariant enforcement boundary.
Criterion 6 (load-bearing-orchestrator): YES — the MCP tool registry + the ingest pipeline (process_email) are primary integration surfaces other epics/Hermes call; both modified here.
Cadence verdict: MANDATORY-CR (criteria 3 + 5 + 6 fire) — matches AC-4. Reviewer sonnet-5 ≠ dev opus-4-8.
```

## Posture Audit summary table

| Check | Status |
| --- | --- |
| 5.1 Lockfile hygiene | N/A — no dep change |
| 5.2 Cross-doc pair verification | ✅ PASS |
| 5.3 Lifecycle string-uniqueness | N/A — no i18n keys |
| 5.4 Multi-consumer impact scan | ✅ PASS |
| 5.5 Screenshot-based perception check | N/A — backend-only |
| 5.6 Upstream-contract spec coverage | N/A — additive |
| 5.7 Module-level mutable container | ✅ PASS |
| 5.8 Dev-fixture seed-vs-production-shape parity | ✅ PASS |
| 5.9 grep-verify-cited-figures | ✅ PASS |
| 5.10 Producer-boundary contract enforcement | ✅ PASS |
| 5.11 Git-evidence consistency check | ✅ PASS |
| 5.12 CR-cadence-mandatory surface classification | MANDATORY-CR |
