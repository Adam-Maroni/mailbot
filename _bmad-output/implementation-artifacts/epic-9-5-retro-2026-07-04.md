# Epic 9.5 Retrospective — seed document

> Created 2026-07-04 by the Story 9.5.4 honest-α-re-run session as the Path B filing target per story Task 3 ("File to `_bmad-output/implementation-artifacts/epic-9-5-retro-{date}.md` (create if not exists — this becomes the Epic 9.5 retrospective seed document)"). The full retrospective fires when Epic 9.5 done-flips; entries below are seeds, not the retro itself.

## Path B accept-with-rationale — AC-6 / Epic 9 done-flip clause 9 (Adam-signed)

**AC-6 / Epic 9 done-flip clause 9 — Path B accept-with-rationale (Story 9-11 AC-7 OR-branch), Adam-signed.**

Measured **α = 0.3490** (raw `0.3489827926151641`), verdict=`untrusted`, n_anchors_paired=40, primary=`claude-opus-4-7`, secondary=`claude-haiku-4-5-20251001`; honest re-run 2026-07-04 (~23:17–23:21 Paris) after the F-ANCHOR-ANSWER-KEY-LEAK leave-one-out fix + CR2026-07-04B-W1 SYSTEM count-wording fix, both verified live in the container pre-dispatch; all 80 dispatches fresh-billed (0 response-cache hits — proof the fixed prompts reached the API; the pre-fix prompts would have hit the cache at $0).

**Why accepted, not reconciled:** the honest α is byte-identical to the contaminated run's α — all 40 per-anchor score pairs unchanged (verified by field-level diff of the two `BaselineSnapshot` files) — so the answer-key leak was NOT driving evaluator agreement. Per the per-anchor disagreement table (`9.5.4-walk-evidence.md` § "Honest α re-run"), 10 of 11 non-zero deltas are `claude-haiku-4-5-20251001` compressing `summary_short` to a 2-ceiling where Adam/opus assign 4–5 (F-HAIKU-SCALE-COMPRESSION); the sole other delta is `draft_reply-016` (opus 5 vs haiku 2). Opus tracks Adam's labels 39/40 on-pattern. The cross-evaluator α therefore measures haiku's summary_short scale compression, not pipeline untrustworthiness.

**Honesty caveat (recorded for v2):** the anchor set is synthetic (`scripts/_propose_anchors.py`: 5 corpus items × 4 score-variants each, cyclic 2,3,4,5 labels), so opus's 39/40 agreement may partly reflect pattern-inference from the 19 remaining leave-one-out anchors rather than true scoring calibration; v2 should diversify anchors (real, non-cyclic, non-variant-derived) before re-measuring α.

**v1 release posture:** the eval pipeline is **single-evaluator-trusted for v1 release** (primary=`claude-opus-4-7` only; cross-evaluator corroboration is NOT claimed for v1). **α ≥ 0.6 is a v2 done-flip gate**, to be attempted with a scale-calibrated secondary evaluator or a reconciled/diversified anchor set.

**AC-7 (Story 9.5.3 — anchor-drift baseline real-write / Epic 9 done-flip clause 10) — discharge criteria amended per Adam decision (2026-07-04, honest-re-run session):** Path B alone does not satisfy AC-7's original criterion ("baseline exists at `evals/anchor_baselines/v1.json`") because verdict=`untrusted` writes the FAILED-CALIBRATION sibling only. Amended criterion: **`v1-FAILED-CALIBRATION.json` (BaselineSnapshot-validated, honest post-fix measurement, host-copied) serves as the v1 anchor-drift baseline artifact under the Path B posture; the canonical `v1.json` write becomes a v2 gate alongside α ≥ 0.6.** With this amendment, Story 9.5.3 AC-7 / Epic 9 done-flip clause 10 close at L3 on the honest artifact.

Signed: **Adam Maroni, 2026-07-04.**

## Spend-reconciliation close (post-commit CR D1, deferred to this session)

- Pre-contaminated-walk Console baseline (2026-07-04 morning): **$13.46** (Adam-read exact).
- Pre-honest-re-run Console reading (2026-07-04 ~23:10): **~$18** (Adam-read approximate) → the contaminated walk's real delta ≈ **~$4.5**, consistent with the walk-evidence Section 6 expectation (~$4.4 real at ×0.37).
- Post-honest-re-run Console reading (2026-07-04 ~23:30): **~$26** (Adam-read approximate) → this run's real delta ≈ **~$8**, matching the Adam-authorized ~$8 envelope (placeholder-billed $21.85 × ~0.37 ≈ $8.1). No overshoot.
- Monthly context: ~$26 of the $35 cap; ~$9 buffer remains.
- Run-2 re-authorization (contaminated walk): covered by decision D1 option (c) + this reconciliation; the honest re-run's authorization was explicit (~$8, captured in-session pre-dispatch).
- Exact post-walk figures were Adam-read as approximations; the durable rule (`feedback_anthropic_spend_source_of_truth.md`) is satisfied — Console-manual is the accepted source; no placeholder figure was treated as spend truth.

## Other retro seeds carried from Story 9.5.4

- F-SECONDARY-MODEL-UNREGISTERED (create-story authored a CLI against unregistered `claude-sonnet-4-5`).
- F-COST-ESTIMATE-AGGREGATE-MASK + estimator output-token under-assumption (billed $21.85 vs $9.14 estimate this run — 2.4×, reproducing P5's finding).
- F-HAIKU-SCALE-COMPRESSION — now the documented sole driver of the α failure; candidate v2 fix: per-model scale calibration or secondary-evaluator swap.
- CR2026-07-04-W1..W5 + CR2026-07-04B-W1 defers in `deferred-work.md` (B-W1's SYSTEM wording fix shipped in the honest-re-run session itself; see walk-evidence).
