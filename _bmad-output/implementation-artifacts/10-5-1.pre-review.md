# Pre-Review Self-Audit — 10-5-1

**Generated:** 2026-07-07 by claude-opus-4-8[1m]
**Story file:** _bmad-output/implementation-artifacts/10-5-1-safety-kill-switch-coverage-and-per-process-singleton-class.md
**Status at audit time:** review (post dev-story Tasks 1–4, pre code-review; Task 5 live walk deferred Adam-hands-on)

## 1. AC-vs-code drift scan

- **AC-1 (drainer pause gate, F4): MATCH** — `drainer.py:488` tick-entry + `:517` mid-tick now call `PauseState.is_paused_now(db_path)` (DB read), not the stale `is_paused()`. Proven by `test_drainer.py::test_db_level_pause_short_circuits_run_tick_without_worker_initialize` + `test_worker_drainer_wiring.py::test_cross_process_pause_stops_worker_drainer_dispatch` (real adapter, 0 Graph calls). The *live* walk assertion (259ms scenario against `MailBot-UAT-10-1`) is the deferred Task 5 — AC-1's live clause is NOT discharged here; its offline analog is.
- **AC-2 (per-process-singleton class fix, both instances): MATCH** — `PauseState.is_paused_now` + `BudgetGuard.is_degraded_now` added; both router **dispatch** gates read `is_degraded_now`. Scope refinement (report verbs stay in-memory) documented in §3/§4 and run-flags; the AC's requirement is scoped to reads that "govern mailbox writes or dispatch," which the two dispatch gates satisfy. Cluster E fence honored (no `add_spend`/pricing/re-derive changes).
- **AC-3 (resume-by-chat, F1 + F-10-5-4): MATCH** — `dispatch_tool_call` filters `tools` to `_PAUSE_ALLOWED_TOOLS`; `ask_router` permits `_PAUSE_ALLOWED_TASK_TYPES={"hermes_aux"}`. F4 containment preserved (write tools filtered; propose_action refused). Live resume-from-Discord clause is deferred Task 5; offline analogs proven in `test_dispatch_tool_call_pause_allowlist.py` + `test_chat_completions_tool_calling.py` + `test_router.py`.
- **AC-4 (paused refusals audited, F3): MATCH** — `pause_gate:refused` vocab member + refusal rows at drainer skip, `ask_router` refusal, `dispatch_tool_call` write-tool filter. Asserted in drainer/router/endpoint tests.
- **AC-5 (regression tests that would have caught F4): MATCH** — cross-process staleness tests at pause primitive + drainer unit + worker two-instance integration; degraded twin; mid-tick release; resume-allowlist. No `initialize()` on the checking instance in the F4 tests.
- **AC-6 (gates green): MATCH** — see §5.9 / §5.11.b verification. 1726 passed + 2 skipped + 3 deselected; ruff/mypy-strict/boundaries clean.

No AC text required editing (no drift found).

## 2. File-List-vs-git diff check

`git status --porcelain` (source/test paths):

```
 M mailbot_api/actions/drainer.py
 M mailbot_api/router/audit_vocab.py
 M mailbot_api/router/budget.py
 M mailbot_api/router/pause.py
 M mailbot_api/router/router.py
 M mailbot_api/verbs/budget_admin.py
 M mailbot_api/verbs/cost.py
 M mailbot_api/verbs/router_control.py
 M tests/integration/test_benchmark_runner.py
 M tests/integration/test_chat_completions_tool_calling.py
 M tests/integration/test_worker_drainer_wiring.py
 M tests/unit/actions/test_drainer.py
 M tests/unit/router/test_budget.py
 M tests/unit/router/test_pause.py
 M tests/unit/router/test_router.py
?? tests/integration/test_dispatch_tool_call_pause_allowlist.py
```

Every path above appears in the story `### File List`. `test_dispatch_tool_call_pause_allowlist.py` is UNTRACKED — will be staged at Step 2.6. No File-List path is missing from git output; no source/test path in git is missing from the File List. **Cleaned:** a stray `unused` SQLite file (created by the pre-existing `inspect_policy(db_path="unused")` tests during a run) was removed; it is not recreated by the reverted report-verb code. (`.claude/settings.json`, `epics.md`, `scratch/` were pre-modified/untracked before this run — out of scope.)

Verdict: TRACKED/consistent for all story paths.

## 3. Adversarial self-review

- [MEDIUM] `budget.py` / `pause.py` — **fail-closed on read error returns True (paused/degraded).** Correct for the dispatch gates (a DB hiccup must not re-open writes). But applied blindly to a *reporting* read it would lie. Caught and handled: report verbs (`inspect_policy`/`cost_breakdown`/`reset_degraded_mode.previously`) deliberately kept on `is_degraded()`. → §4 ACCEPT WITH RATIONALE.
- [MEDIUM] `router.py` `ask_router` gate — **`_PAUSE_ALLOWED_TASK_TYPES={"hermes_aux"}` is the only permitted task while paused.** Risk: if a future chat-interpretation task type is added, it would be refused while paused. Acceptable: `hermes_aux` is the sole chat-interpretation lane today; a new one is a conscious future change. → §4 ACCEPT.
- [MEDIUM] `router.py` `dispatch_tool_call` — **tool filtering silently narrows the surface; if ALL offered tools are write/action, the model gets an empty tools list.** Verified this is the intended AC-3 behavior (interpretation still runs, no action reachable). An empty-tools adapter call is well-formed. → no change.
- [LOW] `drainer.py` — **mid-tick gate now does a fresh DB read per row (not the tick-entry snapshot).** This is deliberate (the mid-tick gate must observe a pause arising during the tick) but adds one `fetchone` per claimed row. At batch size 25 and a 2s loop this is negligible; noted for the reviewer. → §4 ACCEPT.
- [LOW] `_emit_pause_skip_audit_row` uses synthetic `task_type="drainer_tick"` + `model="none"` in `router_calls`. These rows are non-dispatch audit rows; they do NOT carry `cost_usd_estimated` (0.0) so they cannot pollute spend aggregations (`ROUTER_CALLS_SPEND_SINCE` sums cost, which is 0). Verified no spend-query contamination. → no change.
- [LOW] `PAUSE_GATE_REFUSED` added to the closed vocab; the audit.py validator derives `LITERAL_REASONS` from the enum, so no validator edit was needed. Confirmed by the full suite (validator tests green). → no change.
- [MEDIUM] Three pre-existing tests changed their assertions. Risk: masking a real regression by "fixing the test." Each change was verified to encode the NEW correct contract (audit-on-refusal / degraded-via-DB / permit-interpretation), not to paper over a break. → §4 documented.

## 4. Self-caught issues remediated this audit

- Fail-closed-lies-on-report (MEDIUM): **ACCEPT WITH RATIONALE** — authoritative fail-closed read applied only to dispatch gates; report verbs kept in-memory. This is the documented scope refinement (run-flags + Debug Log); flagged to the reviewer for confirmation.
- hermes_aux-only allowlist (MEDIUM): **ACCEPT WITH RATIONALE** — matches today's single chat-interpretation lane; future lanes are a conscious change.
- empty-tools-while-paused (MEDIUM): **FIX NOW / no change needed** — verified intended AC-3 behavior; interpretation turn well-formed with empty tools.
- mid-tick per-row DB read (LOW): **ACCEPT WITH RATIONALE** — correctness (observe mid-tick pause) over a negligible per-row read cost.
- synthetic audit-row identity (LOW): **ACCEPT** — 0-cost rows can't contaminate spend sums; verified.
- three pre-existing test-assertion changes (MEDIUM): **ACCEPT WITH RATIONALE** — each re-encodes the new correct contract; the specific behavior deltas are enumerated in the Dev Agent Record Debug Log for reviewer scrutiny.

§3 is not shallow (7 issues surfaced across both new code and the test-contract changes).

## 5. Posture Audit

### 5.1 — Lockfile hygiene

`$ git diff --stat -- requirements.txt` → `(no output)`. Non-dep-change story; no requirements.txt delta. **✅ PASS.**

### 5.2 — Cross-doc pair verification

Cross-doc branch: **N/A** — this story changes runtime code only; it makes no claim that cross-references another canonical doc (no README/architecture claim edits). §5.2.1 schema branch: **N/A** — File List contains no `mailbot_api/db/migrations/*` path (migrations 010/008 already exist; this story reads them, does not add).

### 5.3 — Lifecycle string-uniqueness

**N/A** — story added no i18n keys (no graphical frontend; MailBot surfaces are Discord/CLI text via verbs, and no user-facing lifecycle string set was added).

### 5.4 — Multi-consumer impact scan

Modified shared surfaces + their consumers audited:
- `PauseState` (pause.py) — new methods `is_paused_now`/`reason_now` are ADDITIVE; existing `is_paused()`/`initialize()`/`reason()` unchanged. Consumers of the new methods: `drainer.py` (tick + mid-tick), `router.py` (ask_router gate, dispatch_tool_call gate). All updated in this story.
- `BudgetGuard.is_degraded_now` — additive; consumers wired: `router.py` (2 dispatch gates). Report verbs deliberately NOT switched (documented).
- `ModelChosenReason.PAUSE_GATE_REFUSED` — additive enum member; `LITERAL_REASONS` auto-includes it; the audit validator accepts it with no edit (confirmed by suite).
No existing consumer's behavior changed except the three intended gate/report sites. **✅ PASS.**

### 5.5 — Screenshot-based perception check

**N/A** — no AC asserts human-visible/painted UI; MailBot has no graphical frontend. AC-1/AC-3 live clauses are the deferred Adam-hands-on walk (Discord/CLI), not a paint-cycle assertion.

### 5.6 — Upstream-contract spec coverage

**N/A** — the story does not consume an upstream role-stripped/projection-gated field. It reads the `pause_state`/`degraded_mode_state` singleton rows directly (present-and-absent both covered: `is_paused_now` handles `row is None` → False; `is_degraded_now` handles `row is None`/`None` → False; fail-closed path tested).

### 5.7 — Module-level mutable container check (Python overlay)

Modified `.py` files scanned for module-level mutable containers:
- `pause.py` — `_PAUSE_STATE = PauseState()` (pre-existing module singleton; intentional per-process warm cache, documented). No NEW mutable module container added; the new methods are pure DB reads.
- `budget.py` — `_GUARD = BudgetGuard()` (pre-existing intentional singleton). New method is a pure DB read; no new container.
- `router.py` — added `_PAUSE_ALLOWED_TOOLS: frozenset[...]` and `_PAUSE_ALLOWED_TASK_TYPES: frozenset[...]` — **immutable** `frozenset` constants (acceptable pattern per the check). No mutable module state added.
- `drainer.py` — added `_PAUSE_SKIP_TASK_TYPE`/`_PROMPT_VERSION`/`_MODEL` string constants (immutable). No mutable container.
- verbs (`router_control.py`/`cost.py`/`budget_admin.py`) — reverted to in-memory reads; no new module state.
**✅ PASS** — new module-level additions are all `frozenset`/`str` immutables.

### 5.8 — Dev-fixture seed-vs-production-shape parity

**N/A** — story added no test fixture consumed by code reading ORM output or pipeline payloads. Tests seed real SQLite via `apply_pending_migrations` and drive real `PauseState`/`BudgetGuard`/`run_tick`/`dispatch_tool_call` against real DB rows; the `_RecordingToolAdapter`/`_FakeToolAdapter` return canned adapter responses (shape = the existing `ToolCallAdapterResponse`, the real producer type), not hand-crafted ORM-shaped fixtures.

### 5.9 — grep-verify-cited-figures

Cited figures verified:

```
Cite: "1726 passed + 2 skipped + 3 deselected" (Completion Notes, sprint-status, §1 AC-6)
Command: $ .venv/Scripts/python.exe -m pytest -q  →  "1726 passed, 2 skipped, 3 deselected, 1 warning in 212.15s"
Verdict: MATCH

Cite: "+18 net vs 1708 baseline"
Inputs: baseline 1708 (confirmed pre-change run "1708 passed"); post 1726.  1726 − 1708 = 18
Verdict: MATCH

Cite: "mypy strict clean 129 files"
Command: $ mypy --strict mailbot_api  →  "Success: no issues found in 129 source files"
Verdict: MATCH
```

Line-number cites in the story/Dev Notes (e.g. drainer.py:488) reference PRE-edit anchors from the context-engineering pass; the code moved but the semantic sites are named in the Debug Log by function, not by fragile line number. **✅ PASS.**

### 5.10 — Producer-boundary contract enforcement

§5.10.a typed-column producers: **N/A** — story writes no new typed-column value from third-party JSON (the audit rows use the existing validated `RouterCallRow` Pydantic model via `record_router_call`; no unguarded `int()`/`Decimal()`/`fromisoformat()` on external input added). §5.10.b response-shape co-emission: **N/A** — no new HTTP response shape; the report verbs return existing Pydantic models unchanged. §5.10.c/d: N/A — no normalizer/ingestion path or shared-type re-export touched.

### 5.11 — Git-evidence consistency check

**5.11.a** — File-List-vs-working-tree: every story File-List path present in `git status --porcelain` (see §2); the one untracked path (`test_dispatch_tool_call_pause_allowlist.py`) IS in the File List and will stage at 2.6. No STAGED-but-not-in-File-List creep. **✅ PASS.**

**5.11.b** — test-to-code ratio:

```
git diff --numstat (mailbot_api added lines, excl docs):
  drainer 71 + audit_vocab 10 + budget 32 + pause 41 + router 187 + budget_admin 4 + cost 4 + router_control 8 = 357  (prodAddedExcludingDocs)
testAdded (modified test files): benchmark 16 + chat_completions 64 + worker_wiring 70 + test_drainer 134 + test_budget 51 + test_pause 65 + test_router 51 = 451
      + new file test_dispatch_tool_call_pause_allowlist.py = 257
  testAdded total = 708
prodOnlyTestRatio = 708 / 357 = 1.98
Threshold: 0.3
Verdict: ✅ PASS — 1.98 ≥ 0.30
```

**5.11.c** — no-later-commits-under-attribution: **N/A** — single-session dev pass; story flipped ready-for-dev→in-progress and to review within this session; no intervening commits under the File-List paths. **✅ PASS.**

### 5.12 — CR-cadence-mandatory surface classification

```
Criterion 1 (boundary-introducing): PARTIAL/YES — adds PAUSE_GATE_REFUSED to the closed audit vocab (a shared invariant the boundary-checker + validator enforce) though no new check_boundaries rule.
Criterion 2 (dep-introducing): NO — no new external deps.
Criterion 3 (dev-self-flagged): YES — §4 escalates the scope-refinement (report verbs kept in-memory) to the reviewer for confirmation.
Criterion 4 (capstone): NO — first story of Epic 10.5, not last; not a cross-story-collision integration point.
Criterion 5 (privacy-invariant): PARTIAL — the pause kill-switch is a SAFETY invariant (F4 CRITICAL); not FR-2.5/NFR-PRIV per se, but authorization-adjacent kill-switch enforcement.
Criterion 6 (load-bearing-orchestrator): YES — the drainer + router dispatch gates are the primary write-suppression seam every action-producing surface relies on; the whole point of the story is the load-bearing kill-switch.

Cadence verdict: MANDATORY-CR (criteria 1 + 3 + 6 fire; the story's own CR-cadence note pre-declared MANDATORY-CR per §5.12 criterion 6). Reviewer model ≠ dev model (sonnet-5 ≠ opus-4-8).
```

## Posture Audit summary table

| Check | Status |
| --- | --- |
| 5.1 Lockfile hygiene | ✅ PASS |
| 5.2 Cross-doc pair verification | N/A — no cross-doc claim; no migrations path |
| 5.3 Lifecycle string-uniqueness | N/A — no i18n keys |
| 5.4 Multi-consumer impact scan | ✅ PASS |
| 5.5 Screenshot-based perception check | N/A — no graphical frontend / paint claim |
| 5.6 Upstream-contract spec coverage | N/A — no projection-gated upstream field consumed |
| 5.7 Module-level mutable container | ✅ PASS — new module additions are frozenset/str immutables |
| 5.8 Dev-fixture seed-vs-production-shape parity | N/A — no ORM/pipeline fixture added |
| 5.9 grep-verify-cited-figures | ✅ PASS |
| 5.10 Producer-boundary contract enforcement | N/A — no typed-column/HTTP-shape producer touched |
| 5.11 Git-evidence consistency check | ✅ PASS |
| 5.12 CR-cadence-mandatory surface classification | MANDATORY-CR |
