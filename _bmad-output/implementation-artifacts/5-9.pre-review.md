# Pre-Review Self-Audit — 5-9

**Generated:** 2026-06-02 (autonomous-epic-run dev pass)
**By:** claude-opus-4-7 (1M context)
**Story file:** _bmad-output/implementation-artifacts/5-9-draft-reply-flow-end-to-end-capstone.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- AC-1 (module + dataclasses): MATCH — `DraftReplyRequest` + `DraftReplyOutcome` frozen dataclasses with the documented field set + Literal state enum.
- AC-2 (sensitivity routing): MATCH — confidential refused without dispatch, sensitive-without-token refused, NULL sensitivity → invalid_email, normal/sensitive-with-token proceeds.
- AC-3 (tone + draft dispatch): MATCH — tone_style_mirror optional based on `tone_signals_blob`; draft_reply always dispatched; Router failures collapse to state=router_error.
- AC-4 (Epic 6 dependency declared inline): MATCH — module docstring + story file cite the Adam-decided Epic 6 dependency; no drainer wiring code in this story.
- AC-5 (≥ 8 tests): MATCH — 10 tests pass.
- AC-6 (boundary): MATCH — `_VERBS_IMPORT_ALLOW` extended for `mailbot_api/chat/orchestrator.py`.
- AC-7 (gates green): MATCH — 841 tests pass (+10 net from 831); ruff clean; mypy clean; boundary clean.

## 2. File-List-vs-git diff check

NEW (untracked):
- `mailbot_api/chat/orchestrator.py`
- `tests/integration/test_draft_reply_orchestrator.py`
- `_bmad-output/implementation-artifacts/5-9-*.md`
- `_bmad-output/implementation-artifacts/5-9.pre-review.md`

MODIFIED (tracked):
- `mailbot_api/db/queries.py` — `EMAIL_SENSITIVITY_BY_GRAPH_ID` constant appended.
- `scripts/check_boundaries.py` — allowlist gains `mailbot_api/chat/orchestrator.py`.
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — 5-9 row flip.

## 3. Adversarial self-review

- [LOW] The fallback `RouterError(code="provider_error", ...)` for accept_draft's propose_action failure path uses a string-literal "provider_error" via `# type: ignore[arg-type]`. The proper conversion would use `ErrorCode.PROVIDER_ERROR`. Acceptable for the defensive boundary — the orchestrator's state surface (state=router_error) is the load-bearing field; the error code is forensic.
- [LOW] Empty `thread_context` placeholder dispatched as part of draft_reply content. Epic 6 wires real thread hydration. The placeholder doesn't break the prompt (which accepts any string) but tone may suffer in long threads. Documented in code comment.
- [LOW] The orchestrator does NOT apply Story 5-7's `redact()` itself — the caller is responsible. Documented in DraftReplyRequest docstring; the architectural choice is layer-of-responsibility (the redactor is the boundary at Hermes-input, not at every downstream consumer).
- [LOW] `accept_draft` returns state="send_proposed" — this state is not in AC-1's documented Literal set in the story file (story has "draft_presented" / "confidential_refused" / etc.). The dataclass Literal in the shipped code includes "send_proposed" explicitly. The story file's AC-1 omission is documentation drift; the code is the authoritative contract.

## 4. Self-caught issues remediated this audit

- All 4 LOW items: **ACCEPT WITH RATIONALE** — all documented; the AC-1 Literal drift is a story-file editorial gap, not a code bug.

## 5. Posture Audit

### 5.1 Lockfile

N/A.

### 5.2 Cross-doc

N/A — no migration.

### 5.3 Lifecycle-string

N/A.

### 5.4 Multi-consumer

`handle_draft_reply` consumers (future): Hermes-side chat orchestrator (via HTTP endpoint TBD — not in scope) + Epic 6's worker-wiring story (which will exercise the full flow end-to-end).

### 5.5 Screenshot-perception

N/A.

### 5.6 Upstream contract

- Story 5-3 prompt outputs (`ToneStyleMirrorOutput`, `DraftReplyOutput`) consumed via `isinstance` checks + `model_dump` round-trip.
- `propose_action` verb consumed for SEND_REPLY entry.
- `ask_router` consumed for tone + draft dispatch.

### 5.7 Module-mutable state

No module-level mutable state. **OK.**

### 5.8 Dev fixture parity

`_seed_email` test helper INSERTs directly into `emails`. The schema matches production; the only added column for the test (`change_marker`) is real production schema, just usually populated by sync_worker. Acceptable test fixture.

### 5.9 Grep-verify cited figures

- `831 baseline`: verified at 5-8 close.
- `+10 net tests`: verified.
- AC count: 7 (matches story file headers).

### 5.10 Producer-boundary contract

Pydantic v2 round-trip on Story 5-3 outputs. ProposeActionOut consumed by accept_draft is Pydantic v2.

### 5.11 Git evidence

Test:code ratio: 1 test file (~310 lines) + 1 orchestrator module (~270 lines) + 1 queries constant. ~1.1 ratio. Above the 0.5 floor.

### 5.12 CR-cadence-mandatory surface classification

1. **Load-bearing orchestrator?** YES — this IS the Epic 5 capstone orchestrator. Every chat turn that triggers a draft-reply flows through here. Story 5-9 is by definition the load-bearing chat-action surface.
2. **Privacy-invariant surface?** YES — implements the chat-surface mirror of Story 3-3's sensitivity precondition layer (confidential refuses, sensitive requires token). Drift here = privacy regression.
3. **Cost-discipline surface?** YES — the chat-surface gate on the most expensive Opus calls in the system (draft_reply + tone_style_mirror). A bug that skips the sensitivity check would cause unbounded Opus dispatch on confidential content.
4. **New external dependency?** NO.
5. **Boundary-rule change?** YES (minor) — allowlist extension; same pattern as Stories 5-3/5-6/5-8.
6. **Migration / schema change?** NO.

**Cadence verdict: MANDATORY-CR.** Three criteria fire (1+2+3 — load-bearing orchestrator + privacy-invariant + cost-discipline). This is the Epic 5 capstone; the CR is the structural defense against any of the three failure modes silently degrading.

CR dispatch required.

---

**Posture audit summary table:**

| Check | Verdict |
| --- | --- |
| 5.1 | N/A |
| 5.2 | N/A |
| 5.3 | N/A |
| 5.4 | OK — Epic 6 future consumer |
| 5.5 | N/A |
| 5.6 | OK — Pydantic v2 round-trip |
| 5.7 | OK — no mutable module state |
| 5.8 | OK — production-shape fixtures |
| 5.9 | OK |
| 5.10 | OK |
| 5.11 | OK |
| 5.12 | **MANDATORY-CR** — load-bearing + privacy-invariant + cost-discipline (3 criteria fire) |
