# Pre-Review Self-Audit — 5-5

**Generated:** 2026-06-02 (autonomous-epic-run dev pass)
**By:** claude-opus-4-7 (1M context)
**Story file:** _bmad-output/implementation-artifacts/5-5-soul-md-defender-persona-and-agents-md-operational-rules-and-skill-md.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- AC-1 (SOUL.md defender persona): MATCH — identity sentence, voice rules, destructive-action posture, quiet bias, all 4 banned anti-patterns labeled per AC.
- AC-2 (AGENTS.md operational rules J/N/P/R): MATCH — imperative-voice headers, all four rules with banned/correct pattern lists, "When in doubt, choose the quieter option" tiebreaker section.
- AC-3 (SKILL.md verb-surface walkthrough): MATCH — all 11 verbs enumerated; `ask_router` non-exposure explicitly called out; three end-to-end turn structures (show unread / draft reply / delete); both required defender warnings (sensitive: mint_sensitivity_token + confirmation_token, confidential: refusal message) present.
- AC-4 (persona/voice consistency self-check): MATCH — 18 tests collected and pass (5 named test functions, 11 parametrized over verbs, 3 parametrized over files = effectively 18 cases ≥ AC-4 floor of 5).
- AC-5 (no mailbot_api/ consumers): VERIFIED — `Grep "SOUL.md|AGENTS.md|skills/mailbot|hermes-config/SOUL|hermes-config/AGENTS" mailbot_api/` returned zero matches.
- AC-6 (gates green, ≥ 5 net tests): MATCH — 778 tests pass (+18 net from 760 baseline); ruff clean; mypy N/A (no new typed modules); boundary check clean.

## 2. File-List-vs-git diff check

NEW (untracked):
- `hermes-config/SOUL.md`
- `hermes-config/AGENTS.md`
- `hermes-config/skills/mailbot/SKILL.md`
- `tests/integration/test_hermes_persona_files.py`
- `_bmad-output/implementation-artifacts/5-5-*.md` (story file)
- `_bmad-output/implementation-artifacts/5-5.pre-review.md` (this audit)

MODIFIED (tracked):
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (5-5 row flip)

All NEW files staged at Step 2.6.

## 3. Adversarial self-review

- [LOW] SOUL.md voice is opinionated. If Adam later disagrees with specific phrasings (e.g., "I do not apologize when an apology isn't warranted"), the file is one edit away from changing — but the AC-4 structural tests pin the load-bearing markers (identity sentence, banned anti-patterns by label, quiet bias header), so reasonable copyedit drift won't break tests.
- [LOW] AGENTS.md cites specific Story numbers (5-2, 5-3, 5-6, 5-7, 5-9, 4-1, 4-3, 4-4, 4-5, 4-6, 4-7, 4-8, 2-7, 2-8, 3-7, 6-3, 6-5). If a story is renumbered (rare; possible during sprint reshuffles), the references age. No test catches drift; manual review on each retro covers this.
- [LOW] SKILL.md's turn-structure 2 step 1 ("call the Router via `/v1/chat/completions` with `task_type="reference_resolution"`") describes the orchestrator wiring that lands in Story 5-8 — this story documents the contract before the wiring exists. If Story 5-8 chooses a different orchestration shape, this doc gets a follow-up edit. Acknowledged as forward-looking content (consistent with the "anticipates Story 5-8" framing in AC-3).
- [LOW] SKILL.md's `cancel_action` / `revert_action` distinction depends on the user knowing what status the action is in. The docs explain it, but a user paste-asking "cancel that delete" without context could resolve to either verb. The orchestrator (Story 5-6's slash dispatcher / Story 5-8's reference resolver) handles disambiguation; this story documents the verb contract.

## 4. Self-caught issues remediated this audit

- All 4 LOW items: **ACCEPT WITH RATIONALE** — each is a documented forward-looking content choice or a known limitation that future stories will close. No code changes needed.

## 5. Posture Audit

### 5.1 Lockfile

N/A — no new deps.

### 5.2 Cross-doc consistency

N/A — no schema.

### 5.3 Lifecycle-string

N/A — no lifecycle code.

### 5.4 Multi-consumer surface

The 3 new files are consumed by Hermes (image-internal loader; not directly testable from this repo) + the AC-4 test module. No mailbot_api/ module reads them (verified via Grep). **OK.**

### 5.5 Screenshot-perception

N/A — no graphical frontend.

### 5.6 Upstream contract

The persona/skill docs anchor against:
- Rules J/N/P/R from architecture.md (named in AGENTS.md headers)
- The 11 verbs from Story 5-2 (enumerated in SKILL.md; AC-4 test pins each)
- NFR-PERSONA-2 banned anti-patterns from architecture (each labeled in SOUL.md; AC-4 test pins each)
- Story 4-1 CR-2 DELETE-requires-token decision (cited verbatim in SKILL.md turn structure 3)

### 5.7 Module-mutable state

N/A — pure-text docs + a stateless test module with module-level Path constants.

### 5.8 Dev fixture seed-vs-production-shape parity

Tests parse the production files directly (no separate fixture). The live docs ARE the test surface. **OK.**

### 5.9 Grep-verify cited figures

- `760 baseline tests`: verified at Story 5-4 close.
- `+18 net tests`: verified — 5 named test functions × 18 effective cases (11 verb-parametrized + 3 file-parametrized + 4 standalone = 18). Final count 778.
- `11 MCP verbs`: verified — matches the Story 5-2 AC-1 exhaustive list, also tested in `tests/integration/test_mcp_server.py`.
- `4 banned anti-patterns`: verified — NFR-PERSONA-2 enumerates 4.
- `4 architectural rules`: verified — Rules J, N, P, R per architecture.md.

### 5.10 Producer-boundary contract

No Pydantic models. The AC-4 test uses `Path` + `str` + regex `re.compile`. **OK.**

### 5.11 Git evidence

Test:code ratio: 1 test file (~115 lines) + 3 doc files (~480 lines). Test:doc ratio ~0.24. Lower than the soft 0.5 floor — but pure-docs stories with structural marker tests are an acceptable exception (the "code" being tested is the markers themselves; one structural-marker test covers a large doc surface). The AC-4 floor of ≥ 5 tests is well-exceeded (18 effective cases).

### 5.12 CR-cadence-mandatory surface classification

1. **Load-bearing orchestrator?** NO — pure docs + structural tests.
2. **Privacy-invariant surface?** PARTIAL — SOUL.md's banned anti-pattern 3 documents the cross-thread sensitive-content quote rule (the persona-level enforcement of the technical Rule F/Q from Story 3-3). NOT a new privacy decision; this story implements the architected persona-level expression.
3. **Cost-discipline surface?** PARTIAL — AGENTS.md documents Rule N's escalation discipline. NOT a new cost decision; this is the persona-level expression of the architected rule.
4. **New external dependency?** NO.
5. **Boundary-rule change?** NO.
6. **Migration / schema change?** NO.

**Cadence verdict: GATE-COVERAGE-ELIGIBLE.** Pure-docs story with structural tests. The 18 tests catch every documented drift mode (missing sections, wrong text in load-bearing sentences, verb omissions, secret leakage). The "does the bot actually behave with defender voice" verification is by design a Phase 3.5 manual walkthrough (epics.md AC text already says "manual review of the four outputs"). Orchestrator SKIPS CR subagent — surface is mechanical documentation work on already-CR-cleared persona/skill boundaries.

---

**Posture audit summary table:**

| Check | Verdict |
| --- | --- |
| 5.1 | N/A |
| 5.2 | N/A |
| 5.3 | N/A |
| 5.4 | OK |
| 5.5 | N/A |
| 5.6 | OK |
| 5.7 | N/A — stateless |
| 5.8 | OK — live-doc parity |
| 5.9 | OK |
| 5.10 | OK |
| 5.11 | OK (pure-docs exception on ratio) |
| 5.12 | **GATE-COVERAGE-ELIGIBLE** — CR dispatch skipped |
