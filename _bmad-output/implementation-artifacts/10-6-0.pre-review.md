# Pre-Review Self-Audit — 10-6-0

**Generated:** 2026-07-12 08:30 by claude-opus-4-8
**Story file:** _bmad-output/implementation-artifacts/10-6-0-graph-auth-at-drain-infra-fix.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- **AC-1** (401 → refresh hook + retry once → applied): MATCH — `outlook_adapter.py:_request_with_retry` special-cases `status == 401` before the generic 4xx branch; awaits `self._on_auth_failure()` and `continue`s. `test_401_refresh_hook_retries_once_then_succeeds` asserts `ok=True` + hook awaited once + fresh token on retry.
- **AC-2** (bounded; no-hook unchanged): MATCH — `auth_refresh_used` guard caps at one refresh; `test_401_then_401_with_hook_is_bounded_and_fails_terminal` + `test_401_without_hook_immediate_fail_unchanged` cover both.
- **AC-3** (worker wires the cache-refresh hook): MATCH — `worker.py` passes `on_auth_failure=lambda: _refresh_access_token_cache(db_path, token_cache)`; the sync `access_token_provider` reads the same `token_cache` cell. `_CachedAccessToken` docstring corrected.
- **AC-4** (non-401 4xx + AR-D5-1 unchanged): MATCH — `test_403_with_hook_does_not_refresh_or_retry` proves 403 immediate-fail + hook not awaited; the 429/503/5xx/timeout/transport branches are untouched (only inserted a new 401 branch above the generic 4xx branch).
- **AC-5** (gates + MANDATORY-CR): MATCH on gates (see §5.12 + §5.11.b); CR is the next step (this gate).
- **AC-6** (live drain walk): N/A at code time — deferred to Phase 3.5 (Adam-hands-on).

No DRIFT found; no AC text edits needed.

## 2. File-List-vs-git diff check

`git status --porcelain` (code files, staged):
```
M  mailbot_api/actions/outlook_adapter.py
M  mailbot_api/worker.py
M  tests/unit/actions/test_outlook_adapter.py
```
File List declared 4 paths: the 3 above (TRACKED + STAGED) + the story `.md` (untracked, staged at Step 2.6). Also untracked: `.autonomous-run-active.json` (run-state, not part of story), `10-6-0.pre-review.md` (this artifact), `scratch/` (pre-existing, out-of-scope — 3rd-carry ruff chore = story 10.6.3). No UNTRACKED story-source files. PASS.

## 3. Adversarial self-review

- [MEDIUM] `outlook_adapter.py` 401 branch — the 401-refresh `continue` re-enters the `for range(_MAX_RETRIES+1)` loop, consuming one `attempt` slot. Confirmed bounded: happy path 401→200 = 2 requests; 401→401 = refresh once then `auth_refresh_used` guard falls through to terminal. But a pathological 401→429→429→429 could interleave — the auth refresh (1) + backoff retries (3) share the 4-iteration budget, so a 401 followed by exhausted 429s would terminate at `provider_429_retry_exhausted` after 4 requests. Acceptable — still bounded, no infinite loop. Documented in §4.
- [LOW] The refresh hook is `await`ed with no try/except. If `_refresh_access_token_cache` raises (SQLite locked mid-drain), the exception propagates out of `apply()` — but the drainer's `_dispatch_and_finalize` wraps `adapter.apply()` in `try/except Exception → _mark_failed("adapter_exception:...")` (drainer.py:725-728), so it fails closed to a terminal audited row, not a crash. Acceptable; noted for reviewer.
- [LOW] `on_auth_failure` typed `Callable[[], Awaitable[None]]` — the worker passes a `lambda` returning the coroutine of `_refresh_access_token_cache(...)`. Verified the lambda returns the awaitable (not pre-awaited). mypy-strict passes, confirming the type.
- [INFO] No test exercises the worker wiring end-to-end (the lambda → cache-cell mutation) — covered structurally by mypy + the adapter-level tests using an equivalent mutable-cell hook. The real worker path is exercised by the Phase 3.5 live drain walk (AC-6).
- [INFO] 401-refresh emits an `action.adapter.auth_refresh_retry` info log — no token value logged (only the event). Consistent with the sanitizer posture.

## 4. Self-caught issues remediated this audit

- MEDIUM (shared iteration budget): **ACCEPT WITH RATIONALE** — bounded by construction; the worst case (401 + 3 exhausted backoffs) is 4 requests then a terminal AR-D5-1 error. No infinite loop, no unbounded Graph calls. A dedicated non-`attempt`-consuming refresh loop would be marginally cleaner but adds control-flow complexity for zero correctness gain.
- LOW (no try/except on hook): **ACCEPT WITH RATIONALE** — the drainer's existing `adapter_exception` catch fails closed to a terminal audited row. Adding a try/except in the adapter would duplicate that safety net.
- LOW (lambda awaitable typing): **FIX NOW → already correct** — mypy-strict confirms the lambda returns `Awaitable[None]`; no change needed.
- INFO items: **ESCALATE TO REVIEWER** — flag the "no end-to-end worker-wiring test" gap for the CR's judgment (Phase 3.5 live walk is the compensating control).

## 5. Posture Audit

### 5.1 Lockfile hygiene
`git diff --stat -- requirements.txt` → (no output) — no dependency change. PASS.

### 5.2 Cross-doc pair verification
N/A (cross-doc branch) — the story references `docs/auth-recovery.md` and `worker.py` docstrings but makes no doc *claim that must match a canonical source*; the docstring correction is in-code, verified by reading. §5.2.1 (schema-touching): File List contains no `mailbot_api/db/migrations/*` paths → N/A.

### 5.3 Lifecycle string-uniqueness
N/A — story added no i18n keys.

### 5.4 Multi-consumer impact scan
`OutlookGraphWriteAdapter` construction sites:
```
$ Grep "OutlookGraphWriteAdapter(" mailbot_api tests
mailbot_api/worker.py  (the drainer adapter — updated with on_auth_failure)
tests/unit/actions/test_outlook_adapter.py  (unit constructions)
```
The new `on_auth_failure` param defaults to `None`, so every existing construction (tests without the kwarg, any read-only path) is behavior-unchanged. Only the worker production site opts in. PASS — additive, default-None, one production consumer audited.

### 5.5 Screenshot-based perception check
N/A — backend-only; no user-visible UI surface.

### 5.6 Upstream-contract spec coverage
N/A — the story consumes no upstream-stripped/projection field; the token seam is an internal sync callable, and both present (fresh token) and absent (stale → 401) cases are tested.

### 5.7 Module-level mutable container check
`outlook_adapter.py` + `worker.py` diff: no new module-level `dict`/`list`/`set`/counter introduced. `_CachedAccessToken` is a pre-existing per-instance dataclass cell (not module-level), constructed inside `_worker_main`. `auth_refresh_used` is a local variable inside `_request_with_retry`. PASS — (no output) for new module-level mutable containers.

### 5.8 Dev-fixture seed-vs-production-shape parity
N/A — story added no test fixtures consuming ORM/pipeline payloads; the new tests use in-test `httpx.MockTransport` handlers + a mutable token cell, not data fixtures.

### 5.9 grep-verify-cited-figures
- Cite: "full suite 1886 passed + 3 skipped + 3 deselected (+4 net vs 1885 baseline)":
  - Baseline `--collect-only`: `1885/1888 tests collected (3 deselected)` (captured at dev-pass start).
  - Post-change full run: `1886 passed, 3 skipped, 3 deselected` (bzartopoj.output line 8). +4 collected (1888→1892) = the 4 new adapter tests; net passing 1885→1886 (the other 3 new are 401-no-hook already-passing + the 3 pre-existing opt-in tests now surfacing as runtime skips). MATCH.
- Cite: "adapter file 13→17 tests (+4)": `pytest tests/unit/actions/test_outlook_adapter.py` → `17 passed`; pre-change was 13 (4 new added). MATCH.
- Cite: "numstat test +109 / prod 43": `git diff --cached --numstat` → outlook_adapter 31, worker 12 (=43 prod), test 109. MATCH.

### 5.10 Producer-boundary contract enforcement
N/A — the change does not write to a typed ORM column from third-party JSON, and returns no ORM row to an HTTP client. `GraphApplyResult` is an internal dataclass, not a widened wire shape; no sensitive field (no token) is emitted in the result or log.

### 5.11 Git-evidence consistency check
- **5.11.a** File-List-vs-tree: 3 code paths STAGED + IN FILE LIST ✅; story `.md` + pre-review to be staged at Step 2.6 ✅; `scratch/` UNTRACKED + out-of-scope (10.6.3) — informational, not flagged. PASS.
- **5.11.b** test-ratio: `testAdded=109`, `prodAddedExcludingDocs=43`, ratio `109/43 = 2.53` ≥ 0.30. PASS.
- **5.11.c** no-later-commits: single-session dev pass; story flipped in-progress and reached review in the same session, no intervening commits. N/A.

### 5.12 CR-cadence-mandatory surface classification
- Criterion 1 (boundary-introducing): NO — no new writer monopoly / lint boundary / allowlist.
- Criterion 2 (dep-introducing): NO — no `requirements.txt` change.
- Criterion 3 (dev-self-flagged): YES — §4 escalates the "no end-to-end worker-wiring test" INFO to the reviewer.
- Criterion 4 (capstone): NO — 10.6.0 is first in Epic 10.6.
- Criterion 5 (privacy-invariant): NO — the change touches the auth token seam but does not modify sensitivity/privacy enforcement; no token value is logged or widened (verified §5.10).
- Criterion 6 (load-bearing-orchestrator): YES — `OutlookGraphWriteAdapter._request_with_retry` is the sole Graph write-dispatch seam every Tier-1/2/3 action drains through; the drainer is the primary integration surface for all action-taking epics (10.6.1 AC-6, 10.6.2 send-follow-through depend on this drain path working).

**Cadence verdict: MANDATORY-CR** (criteria 3 + 6 fire). Load-bearing dispatch seam.

## Posture Audit summary table

| Check | Status |
| --- | --- |
| 5.1 Lockfile hygiene | ✅ PASS |
| 5.2 Cross-doc pair verification | N/A — no cross-doc claim; no migration paths |
| 5.3 Lifecycle string-uniqueness | N/A — no i18n keys |
| 5.4 Multi-consumer impact scan | ✅ PASS — additive default-None param, 1 prod consumer |
| 5.5 Screenshot-based perception check | N/A — backend-only |
| 5.6 Upstream-contract spec coverage | N/A — no projection consumption |
| 5.7 Module-level mutable container | ✅ PASS — no new module-level mutable state |
| 5.8 Dev-fixture parity | N/A — no ORM/pipeline fixtures added |
| 5.9 grep-verify-cited-figures | ✅ PASS — test/numstat figures re-verified |
| 5.10 Producer-boundary contract | N/A — no typed-column/HTTP-wire producer |
| 5.11 Git-evidence consistency | ✅ PASS — File List clean, ratio 2.53, single-session |
| 5.12 CR-cadence classification | MANDATORY-CR (criteria 3 + 6) |
