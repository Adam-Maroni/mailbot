# Pre-Review Self-Audit — 10-7-6

**Generated:** 2026-07-18 by claude-opus-4-8[1m]
**Story file:** _bmad-output/implementation-artifacts/10-7-6-drop-memory-toolset-attractor-and-diagnose-turn-binding.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- **AC-1 (memory dropped): MATCH** — `platform_toolsets.discord` = `[mailbot-api, cronjob, clarify]` in config.yaml; `- memory` removed.
- **AC-2 (premise verified live, not inferred): MATCH** — `hermes tools list --platform discord` run pre-trim (`memory ✓ enabled`) and post-trim + restart (`memory ✗ disabled`, `mailbot-api all tools enabled`). Recorded in Dev Notes / Debug Log. Persona rides `.skills_prompt_snapshot.json`, not `memory` (skills precedent), surface resolved cleanly.
- **AC-3 (clarify disposition): MATCH** — KEPT explicitly, one-line rationale in config KEEP-SET comment + Task 4 note (not an attractor in failed walk; legitimate tiebreaker).
- **AC-4 (mailbot-api stays, noise excluded): MATCH** — allow-list keeps `mailbot-api` + `cronjob`; noise set + `messaging` + `memory` all absent. Gated by exclusion tests + keeps-mailbot-verbs.
- **AC-5 (every MCP server on allow-list): MATCH** — `mailbot-api` (only mcp_servers entry) stays; `memory` is a built-in toolset not an MCP server. `test_hermes_config_every_mcp_server_is_on_the_discord_allowlist` passes.
- **AC-6 (turn diagnosis + lever filed): MATCH** — row 15095 classified (`turn` = Hermes control primitive, not a listed/droppable toolset, not mailbot_api source); residual F-10-7-6-R1 filed in story-run-flags.md.
- **AC-7 (offline drift gates only): MATCH** — both new tests parse config text/YAML directly, no Docker/Discord/Anthropic. Live-Discord clause-3 turn explicitly NOT claimed discharged by the diff.
- **AC-8 (clause-3 live re-walk = done-gate): MATCH (defined, not discharged)** — exact pass condition specified (Task 9); it is Adam-hands-on and NOT run by this autonomous pass. Story HELD at `review`.

No DRIFT found — no AC-text fixes required.

## 2. File-List-vs-git diff check

`git status --porcelain`:
```
 M .claude/settings.json                                  (pre-existing, background — NOT in scope)
 M _bmad-output/implementation-artifacts/10-7-0-spike-finding.md  (pre-existing, background — NOT in scope)
 M _bmad-output/implementation-artifacts/sprint-status.yaml       (IN FILE LIST — status flips)
 M _bmad-output/implementation-artifacts/story-run-flags.md       (IN FILE LIST — F-10-7-6-R1)
 M hermes-config/config.yaml                              (IN FILE LIST — the trim)
 M tests/integration/test_hermes_config.py               (IN FILE LIST — drift gates)
?? _bmad-output/implementation-artifacts/.autonomous-run-active.json  (run state file — teardown at end)
?? _bmad-output/implementation-artifacts/10-7-6-drop-memory-toolset-attractor-and-diagnose-turn-binding.md  (IN FILE LIST — story file)
?? _bmad-output/implementation-artifacts/epic-10-7-retro-2026-07-17.md  (pre-existing, background — NOT in scope)
```
Every File List path is present (MODIFIED-NOT-STAGED or UNTRACKED). No File List path is missing from git. Three background changes (`settings.json`, `10-7-0-spike-finding.md`, `epic-10-7-retro-2026-07-17.md`) predate this run and are OUT of scope — will NOT be staged. The `10-7-6.pre-review.md` (this file) + `10-7-6-walk-evidence.md` (created at Adam's walk) are expected artifacts.

## 3. Adversarial self-review

- [LOW] tests/integration/test_hermes_config.py — `_TRIMMED_TOOLSETS` union is introduced but not yet consumed by any test (the two exclusion tests still reference the per-story sets). Not a bug — it's a forward-looking convenience the story spec explicitly permitted ("extend → a union"); harmless but currently unused. Left as an intentional seam for the next trim story.
- [LOW] hermes-config/config.yaml — the `# TRIMMED (Story 10.7.6)` block is long (~35 lines of prose). It duplicates evidence also in story-run-flags.md (F-10-7-6-R1) + the story file. Chosen deliberately: the config file is the runbook artifact operators read at CP-1; the boundary-honesty gate (AC-7) requires the rationale ON the surface. Accept.
- [MEDIUM→verify] The `memory`-drop premise (persona survives via `.skills_prompt_snapshot.json`) is confirmed at RESOLVER level (memory ✗, mailbot-api ✓) but the persona-injection-on-an-actual-turn is only *inferred* from the skills precedent — it is not proven until the AC-8 live walk. This is exactly the risk the story flags (AC-2 alternative branch: "if persona does NOT survive, restore memory + pivot"). It is correctly gated to the live walk, not silently assumed. Escalate to reviewer for a second look at whether resolver-level confirmation is a sufficient pre-walk bar (the story says yes, mirroring 10.7.3).
- [LOW] The `clarify` KEEP decision rests on it "not appearing as an attractor in the failed walk" — a single-walk sample. Defensible (AC-3 default recommendation) but not statistically strong. Recorded honestly as a decision, not a proof.

Non-empty — 4 issues surfaced.

## 4. Self-caught issues remediated this audit

- **LOW `_TRIMMED_TOOLSETS` unused union** → ACCEPT WITH RATIONALE — the story spec explicitly offered the union as an option; it is a low-cost forward seam and does not affect behavior. No fix.
- **LOW config comment length** → ACCEPT WITH RATIONALE — boundary-honesty gate (AC-7) requires the rationale on the config surface; duplication with flags/story is intentional (config = operator runbook).
- **MEDIUM persona-survives is resolver-confirmed but turn-unproven** → ESCALATE TO REVIEWER — the crux AC-2 premise; correctly gated to the AC-8 live walk with a documented pivot branch. Flag for reviewer to confirm the resolver-level bar is the right pre-walk gate (10.7.3 precedent).
- **LOW clarify single-walk sample** → ACCEPT WITH RATIONALE — recorded as an explicit decision (AC-3), not claimed as proof; the live walk will re-expose it if wrong.

## 5. Posture Audit

### 5.1 — Lockfile hygiene
```
$ git diff --stat -- requirements.txt
(no output)
```
N/A — no dependency change; requirements.txt untouched.

### 5.2 — Cross-doc pair verification
No cross-doc claim contradicts a canonical source: the config comment + story + flags all cite the SAME evidence (F-10-7-CLAUSE3-W1 rows 15095–15106) which originates in story-run-flags.md. Schema-touching branch (§5.2.1): **N/A — File List contains no migrations paths.**
Verdict: **N/A (cross-doc branch)** — no external doc cross-reference; the walk evidence is single-sourced in story-run-flags.md.

### 5.3 — Lifecycle string-uniqueness check
N/A — story added zero i18n keys.

### 5.4 — Multi-consumer impact scan
Modified `hermes-config/config.yaml` `platform_toolsets.discord`. Its sole consumer is Hermes's `_get_platform_tools` resolver (out-of-repo) — verified live via `hermes tools list --platform discord` (the resolved surface is the consumer output). In-repo, `tests/integration/test_hermes_config.py` reads the key via `_discord_allowlist`. Both audited.
Verdict: **✅ PASS** — the config key's one in-repo consumer (the drift tests) + the live resolver were both exercised.

### 5.5 — Screenshot-based perception check
N/A — no user-visible UI surface; MailBot has no graphical frontend. The "visible" proof for this story is a Discord router_calls DB row (AC-8 walk), not a painted pixel.

### 5.6 — Upstream-contract spec coverage check
N/A — story does not consume any upstream-stripped/projected field. It is a config allow-list trim; no role-stripped or gated projection contract in scope.

### 5.7 — Module-level mutable container check
```
$ Grep '^(_[A-Z]|[A-Z_]+) *(=|:).*(\{|\[|set\()' tests/integration/test_hermes_config.py (new module-level)
_TRIMMED_TOOLSETS_10_7_6 = frozenset({"memory"})
_TRIMMED_TOOLSETS = _TRIMMED_TOOLSETS_10_7_3 | _TRIMMED_TOOLSETS_10_7_6
```
Both new module-level containers are `frozenset` / frozenset-union — immutable by construction (the acceptable pattern per §5.7 Python overlay). `_REQUIRED_DISCORD_TOOLSETS` (edited) is also a `frozenset`. No `.py` source in `mailbot_api/` modified.
Verdict: **✅ PASS** — new module-level containers are frozensets (immutable); no mutable module state introduced.

### 5.8 — Dev-fixture seed-vs-production-shape parity check
N/A — story added zero test fixtures consuming ORM output or pipeline payloads. The new tests parse the real `config.yaml` file directly (the production artifact itself is the "fixture"), which is maximally shape-faithful by construction.

### 5.9 — grep-verify-cited-figures
```
Cite: "pytest 1974 passed (+2 vs 1972 baseline)" — story Completion Notes + Task 8 + sprint-status row
Verification command:
  $ .venv/Scripts/python.exe -m pytest -q  → "1974 passed, 3 skipped, 3 deselected in 260.11s"
Verdict: MATCH — 1974 confirmed by the actual full-suite run this session. Baseline 1972 is the 10-7-4 done row (sprint-status).

Cite: "14 passed" (hermes-config suite, GREEN) — Debug Log
Verification command:
  $ .venv/Scripts/python.exe -m pytest tests/integration/test_hermes_config.py -q  → "14 passed in 0.17s"
Verdict: MATCH.

Cite: "2 failed / 12 passed" (RED, pre-edit) — Debug Log
  Verified: the RED run this session showed exactly `2 failed, 12 passed`.
Verdict: MATCH.

Cite: "9/11 rows were memory" (F-10-7-CLAUSE3-W1) — config comment + flags + story
  Source: rows 15096-15104 (×9 memory) of 15095-15106 (11 rows). Frozen historical figure from the 2026-07-17 walk, cited from story-run-flags.md § F-10-7-CLAUSE3-W1 (the canonical walk record).
Verdict: MATCH — 9 memory rows of 11 total; arithmetic checks (15096..15104 inclusive = 9 rows).
```
Verdict: **✅ PASS** — all numeric cites verified against command output or the canonical walk record.

### 5.10 — Producer-boundary contract enforcement
N/A — story modifies no normalizer/DTO/service feeding a typed ORM column AND modifies no service returning an ORM row to an HTTP client. It is a YAML config trim + offline tests. No typed-column writes, no HTTP response shapes.

### 5.11 — Git-evidence consistency check

**5.11.a — File-List-vs-working-tree:** (paste in §2 above). Every File List path present; 3 background changes explicitly out-of-scope (will not stage). Verdict: **✅ PASS** — no silent scope-creep (the background files are pre-existing, not this run's work, and won't be staged).

**5.11.b — Production-only test-to-code ratio:**
```
git diff --numstat (test-classifier applied):
  testAdded: 87  (tests/integration/test_hermes_config.py)
  docsAdded: 40  (hermes-config/config.yaml — all comment/YAML config, docs-classified per §5.11.b MailBot overlay) + all *.md (story, flags, pre-review)
  prodAddedExcludingDocs: 0  (no mailbot_api/ source added)
  prodOnlyTestRatio: null (denominator 0)
Threshold: 0.3
Verdict: ✅ PASS — prodAddedExcludingDocs == 0 (config + tests + docs only); the <0.3 flag requires prodAddedExcludingDocs > 0.
```

**5.11.c — No-later-commits-under-attribution:** single-session dev pass; status flipped in-progress → review within this session. N/A.
Verdict: **✅ PASS** (5.11.a) + **✅ PASS** (5.11.b) + **N/A** (5.11.c).

### 5.12 — CR-cadence-mandatory surface classification

```
Criterion 1 (boundary-introducing): NO — no new writer-monopoly / lint boundary / check_boundaries allowlist. (New drift tests, but no new enforced source-code invariant.)
Criterion 2 (dep-introducing): NO — no new deps.
Criterion 3 (dev-self-flagged): YES — §4 escalates the MEDIUM "persona-survives resolver-confirmed but turn-unproven" item TO REVIEWER.
Criterion 4 (capstone): PARTIAL — 10.7.6 is the clause-3 fix story, the last engineering lever before the epic's load-bearing live walk; effectively the epic's integration point.
Criterion 5 (privacy-invariant): NO — allow-list is a tool-surface scope, not an FR-2.3/2.5/NFR-PRIV surface.
Criterion 6 (load-bearing-orchestrator): YES — `platform_toolsets.discord` IS the model-facing per-turn tool surface; it directly shapes what qwen can pick on every Discord turn. The story itself declares MANDATORY-CR (Dev Notes) because "config = model-facing surface contract."

Cadence verdict: MANDATORY-CR (criteria 3 + 4 + 6). Reviewer model MUST ≠ dev model. The CR should specifically probe the AC-2 memory-drop premise (as 10.7.3's CR probed the messaging premise).
```

## Posture Audit summary table

| Check | Status |
| ----- | ------ |
| 5.1 Lockfile hygiene | N/A — no dep change |
| 5.2 Cross-doc pair verification | N/A (cross-doc) — evidence single-sourced; §5.2.1 N/A (no migrations) |
| 5.3 Lifecycle string-uniqueness | N/A — no i18n keys |
| 5.4 Multi-consumer impact scan | ✅ PASS |
| 5.5 Screenshot-based perception check | N/A — no graphical frontend |
| 5.6 Upstream-contract spec coverage | N/A — no projected-field consumption |
| 5.7 Module-level mutable container | ✅ PASS — frozensets only |
| 5.8 Dev-fixture seed-vs-production-shape parity | N/A — no fixtures (tests read real config.yaml) |
| 5.9 grep-verify-cited-figures | ✅ PASS |
| 5.10 Producer-boundary contract enforcement | N/A — no typed-column/HTTP producer |
| 5.11 Git-evidence consistency check | ✅ PASS / ✅ PASS / N/A |
| 5.12 CR-cadence-mandatory surface classification | MANDATORY-CR |
