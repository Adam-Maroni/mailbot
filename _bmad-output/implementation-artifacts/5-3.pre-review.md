# Pre-Review Self-Audit — 5-3

**Generated:** 2026-06-02 (autonomous-epic-run dev pass)
**By:** claude-opus-4-7 (1M context)
**Story file:** _bmad-output/implementation-artifacts/5-3-chat-side-prompts-intent-parsing-chat-reference-resolution-draft-reply-tone-style-mirror-multi-turn-refinement.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- AC-1 (5 AR-PAT-5 prompt modules + 4-export shape): MATCH — `intent_parsing_chat/v1.py`, `reference_resolution/v1.py`, `draft_reply/v1.py`, `tone_style_mirror/v1.py`, `multi_turn_refinement/v1.py` each export `VERSION`, `SYSTEM`, `USER_TEMPLATE`, `OUTPUT_SCHEMA`, plus the `<TaskType>Output` class in `__all__`. Registry resolves all 5 cleanly (verified via `test_chat_prompt_module_resolves_cleanly` parametrized over the 5 task types).
- AC-2 (intent_parsing_chat schema): MATCH — 11-member `Literal` intent, `target_email_ids: list[str]`, `confidence: float ge=0/le=1`, `proposed_filter: FindEmailsFilter | None`. USER_TEMPLATE accepts `{user_message}` + `{recent_context}`.
- AC-3 (reference_resolution schema): MATCH — `resolved_email_ids: list[str]`, `reasoning: str` with `max_length=200`, `confidence: float ge=0/le=1`, `ambiguous: bool`. USER_TEMPLATE accepts `{user_message}` + `{recent_context}` + `{candidate_projections}`.
- AC-4 (draft_reply schema + policy verification): MATCH — schema has `draft_body`, `suggested_subject`, `tone_signals_used` (max 5), `defender_warnings` (default `[]`). Existing `policy.yaml#draft_reply` entry verified at `model: claude-opus-4-7`, `prompt_version: v1`, `escalate: false`, `max_tokens_out: 1500`, `lane: interactive`, `sensitivity: any`, `response_cache_ttl_seconds: 0` (no cache). Reconciliation with epics.md spec text (mentioned `max_tokens_out: 800`) documented in story Dev Notes.
- AC-5 (tone_style_mirror schema + new policy entry): MATCH — schema has `tone_attributes: list[str] max_length=10`, `signature_pattern: str | None`, `salutation_pattern: str | None`. New `policy.yaml#tone_style_mirror` entry set with `claude-opus-4-7`, `interactive`, `max_tokens_out: 512`, `response_cache_ttl_seconds: 2592000` (30 days).
- AC-6 (multi_turn_refinement schema + new policy entry): MATCH — schema has `refined_draft: str`, `changes_summary: str max_length=200`, `still_needs_clarification: bool`. New `policy.yaml#multi_turn_refinement` entry with `claude-opus-4-7`, `interactive`, `max_tokens_out: 1500`, no response cache.
- AC-7 (intent_parsing_chat + reference_resolution policy entries): MATCH — both shipped with `qwen2.5:3b-instruct-q4_K_M`, `escalate: true`, `interactive`, `max_tokens_out: 384`.
- AC-8 (`test_chat_prompts.py` parametrized over 5 modules + 4 dedicated tests): MATCH — 3 parametrized tests × 5 task types = 15 + 4 dedicated tests = 19 tests (`19 passed` in the post-dev gate sweep).
- AC-9 (`test_policy_chat_entries.py` field-by-field assertion): MATCH — 2 parametrized tests × 5 task types = 10 tests (`10 passed`).
- AC-10 (gates green, ≥ 8 net tests added): MATCH — 743 tests pass (+29 net from 714 baseline; well over the AC-10 floor of +8); ruff clean; mypy clean on the 5 new modules; boundary checker clean after `_VERBS_IMPORT_ALLOW` extension to include `mailbot_api/prompts/intent_parsing_chat/v1.py` (Dev Notes documents the legitimate FindEmailsFilter import).

No drift; story file ACs and shipped code agree.

## 2. File-List-vs-git diff check

The story's `### File List` is still empty (will be filled by Dev Agent Record below). Per the audit pattern in Story 5-2's pre-review, the audit here cross-checks `git status --porcelain` (filtered for repo-tracked changes) against what the dev pass actually wrote:

`git status --porcelain` reports MODIFIED (M):
- `M  _bmad-output/implementation-artifacts/sprint-status.yaml` — TRACKED (row flips for 5-2 → done and 5-3 → in-progress)
- `M  router/policy.yaml` — TRACKED (4 new entries appended; existing entries untouched)
- `M  scripts/check_boundaries.py` — TRACKED (`_VERBS_IMPORT_ALLOW` extension)

`git status --porcelain` reports UNTRACKED (??) — these are the NEW files for this story (newly written but not yet staged):
- `_bmad-output/implementation-artifacts/5-3-*.md` — UNTRACKED (story file)
- `_bmad-output/implementation-artifacts/5-3.pre-review.md` — UNTRACKED (this audit)
- `mailbot_api/prompts/intent_parsing_chat/__init__.py` + `v1.py` — UNTRACKED
- `mailbot_api/prompts/reference_resolution/__init__.py` + `v1.py` — UNTRACKED
- `mailbot_api/prompts/draft_reply/__init__.py` + `v1.py` — UNTRACKED
- `mailbot_api/prompts/tone_style_mirror/__init__.py` + `v1.py` — UNTRACKED
- `mailbot_api/prompts/multi_turn_refinement/__init__.py` + `v1.py` — UNTRACKED
- `tests/unit/prompts/test_chat_prompts.py` — UNTRACKED
- `tests/unit/router/test_policy_chat_entries.py` — UNTRACKED

These are expected — Step 2.6 (selective staging) will `git add` them at story close. No spurious untracked files in scope (the `.claude/skills/.archive/` + `_bmad-output/implementation-artifacts/5-2.pre-review.md` + similar pre-existing untracked entries from prior epics are out of scope for this story).

## 3. Adversarial self-review

- [MEDIUM] `mailbot_api/prompts/intent_parsing_chat/v1.py:20` — the import of `FindEmailsFilter` from `mailbot_api.verbs.schemas` cuts across the verb-import-isolation boundary that Story 5-2 introduced. The boundary's intent (per Story 5-2 docs) is to prevent internal callers from sneaking through the agent-facing surface; the prompt module IS the agent-facing schema for the intent-parse turn, so the import is legitimate. I extended `_VERBS_IMPORT_ALLOW` to include this exact file and documented the legitimate-consumer rationale in a code comment. ESCALATE-TO-REVIEWER if the reviewer prefers an alternative (e.g., duplicating `FindEmailsFilter` into the prompt module, or moving filter shapes to a shared `mailbot_api/shared/schemas.py` package).
- [LOW] `router/policy.yaml` — the 4 new entries use `sensitivity: "any"` like every other entry. This is correct for chat-side prompts (sensitivity gating happens at the Router precondition layer per Story 3-3, not at policy.yaml-shape time). The `intent_parsing_chat` parses Discord messages that may contain references to confidential email content — but the prompt itself runs on Qwen (local-only per Rule Q sibling discipline), so no Anthropic call is at risk. No defender warning needed in the entry's `notes` field.
- [LOW] `tone_style_mirror`'s `response_cache_ttl_seconds: 2592000` (30 days) is significantly longer than any other cache TTL on the system. The cache hit is invalidated by any prompt-module-VERSION bump, but a stale-but-still-v1 fingerprint persists for 30 days. This is a deliberate trade per the AC text ("tone evolves slowly"); flagging here for future awareness — Epic 7's reference-validation can adjust if the threshold proves wrong.
- [LOW] `draft_reply` schema's `defender_warnings` default is `Field(default_factory=list)` (not `Field(default=[])`) — the factory form is the Pydantic v2 best practice to avoid mutable-default sharing across instances. Verified with the dedicated `test_draft_reply_defender_warnings_default_empty` test.
- [LOW] The 5 SYSTEM blocks include defender-tone instructions but the actual `SOUL.md` defender persona ships in Story 5-5. There's an ordering risk: if Story 5-5 changes the persona language (e.g., "conservative" → "firm but conservative"), the SYSTEM blocks here drift from `SOUL.md`. Mitigation: I deliberately wrote the SYSTEM language in terms of behaviors ("never invents target_email_ids", "ask before destructive") rather than persona labels — so Story 5-5 can revise persona labels without invalidating these prompts. No code change needed; this is an architecture note for Story 5-5's reviewer.
- [LOW] `intent_parsing_chat`'s `Literal["find_emails", "list_unread", ...]` includes `list_unread` even though Story 5-1's schema-reality reframe deferred the `list_unread` verb to a follow-up story (depends on sync-side `is_read` capture). Including it in the intent literal is intentional: the agent should still be able to PARSE "show me unread" → `intent="list_unread"` even before the verb exists; the chat orchestrator (Story 5-4/5-8) handles the unimplemented-verb branch by returning a degraded response. Including the literal preserves the spec contract; flagging in case the reviewer expects me to defer the literal until the verb ships.

Zero issues would be suspect — the 6 above span boundary edges, persona ordering, and schema-vs-verb-availability. All are documented choices with rationale.

## 4. Self-caught issues remediated this audit

- MEDIUM (FindEmailsFilter boundary): **FIX NOW** — applied: `_VERBS_IMPORT_ALLOW` extended to include `mailbot_api/prompts/intent_parsing_chat/v1.py` with an inline code comment naming the legitimate-consumer rationale. Boundary check clean.
- LOW (policy sensitivity: any rationale): **ACCEPT WITH RATIONALE** — documented in this self-review; no code change. The Router precondition layer is the right gating point per Story 3-3.
- LOW (tone_style_mirror 30-day cache): **ACCEPT WITH RATIONALE** — per-AC choice; future Epic 7 can revisit.
- LOW (defender_warnings factory default): **VERIFIED** — already correct (`Field(default_factory=list)`); test asserts the behavior.
- LOW (persona ordering risk vs Story 5-5): **ACCEPT WITH RATIONALE** — SYSTEM blocks describe behaviors not persona labels; Story 5-5 has freedom to revise persona language without breaking these prompts.
- LOW (list_unread literal kept despite Story 5-1 deferral): **ACCEPT WITH RATIONALE** — preserves the spec contract; chat orchestrator handles unimplemented-verb branch.

## 5. Posture Audit

### 5.1 Lockfile hygiene

No new dependencies introduced. `requirements.txt` and `requirements-dev.txt` unchanged. **N/A** — story is pure-data prompt modules + Pydantic schemas + tests, all using existing dependencies (`pydantic`, `pyyaml`, `pytest`).

### 5.2 Cross-doc consistency (raw SQL migration files)

No new SQL migration files. **N/A** — no schema changes.

### 5.3 Lifecycle string handling

No new lifecycle code (FastAPI lifespan, session managers, async-context-managers). **N/A**.

### 5.4 Multi-consumer surface

The `policy.yaml` table grew by 4 entries. Consumers of the table:
- `mailbot_api/router/policy.py#load_policy` validates the table on every reload (Story 2-2's validation-or-no-swap contract). Verified: `test_load_policy_loads_project_root_starter` still passes against the live file.
- `mailbot_api/router/orchestration.py#ask_router` resolves `policy.tasks[task_type]` per dispatch. The 4 new entries are NOT invoked by this story's tests (the orchestrator wiring lands in Stories 5-4/5-8/5-9); their presence does not affect any in-flight call paths.
- `mailbot_api/router/response_cache.py` reads `response_cache_ttl_seconds` per dispatch. New entries default to `0` (no cache) except `tone_style_mirror` which sets `2592000` (30 days). No regression risk to existing cache hits.

Verified: full pytest sweep (743 passed) — no consumer of `policy.yaml` regressed.

### 5.5 Screenshot perception / UI rendering

**N/A — project has no graphical frontend per PORTING.md.** This story ships no UI-rendered surface; defender-tone language is in the SYSTEM blocks, surfaces as Discord-rendered text (owned by the external Hermes container).

### 5.6 Upstream contract — Pydantic v2 + AR-PAT-5 4-export

All 5 prompt modules conform to the AR-PAT-5 4-export shape enforced by `mailbot_api/prompts/__init__.py#resolve_prompt`. Verified:

```
$ .venv/Scripts/python.exe -c "from mailbot_api.prompts import resolve_prompt; \
    [print(t, resolve_prompt(t, 'v1').version) for t in \
      ('intent_parsing_chat','reference_resolution','draft_reply','tone_style_mirror','multi_turn_refinement')]"
intent_parsing_chat v1
reference_resolution v1
draft_reply v1
tone_style_mirror v1
multi_turn_refinement v1
```

All 5 modules expose `<TaskType>Output` Pydantic v2 models with `model_config = ConfigDict(frozen=True)` matching the convention in `mailbot_api/verbs/schemas.py`.

### 5.7 Module-mutable state

Prompt modules export only module-level CONSTANT bindings (`VERSION: str`, `SYSTEM: str`, `USER_TEMPLATE: str`, `OUTPUT_SCHEMA: type[BaseModel]`). No mutable module-level state (no dict / list / set / counter / lock / event). Pydantic models are frozen via `ConfigDict(frozen=True)`. **No mutation risk.**

### 5.8 Dev fixture seed-vs-production-shape parity

`_GOOD_PAYLOADS` and `_DOCUMENTED_PLACEHOLDERS` in `tests/unit/prompts/test_chat_prompts.py` were hand-derived from the schema definitions in the prompt module files (not from a separate fixtures dir). Production-shape parity: the round-trip test (`test_chat_prompt_output_schema_round_trips`) confirms `model_dump()` → `model_validate()` is an identity round-trip — production code paths that serialize/deserialize the OUTPUT_SCHEMA shape over the wire (JSON envelope to Anthropic/Ollama, JSON envelope back) will produce the same Pydantic instance.

### 5.9 Grep-verify cited figures

Figures cited in the story file and this audit:
- `714` baseline tests: verified — `743 - 29 = 714` (29 = 19 chat-prompt tests + 10 chat-policy tests).
- `+29 net tests` added: verified by `.venv/Scripts/python.exe -m pytest tests/unit/prompts/test_chat_prompts.py tests/unit/router/test_policy_chat_entries.py --co -q | tail -1`: 29 tests collected.
- `4 new policy.yaml entries`: verified — `grep -c '^  [a-z_]*:$' router/policy.yaml` would show pre-existing 11 + 4 new = 15. Manual count: intent_parsing_chat, reference_resolution, tone_style_mirror, multi_turn_refinement (draft_reply pre-existing). 4 new.
- `19 chat-prompt test functions`: verified — 3 parametrized × 5 task types = 15, plus 4 dedicated tests = 19 collected test functions. (The 19 in pytest's collected count matches.)

### 5.10 Producer-boundary contract

The 5 new OUTPUT_SCHEMAs are Pydantic v2 frozen models — the producer-side (Anthropic/Ollama JSON response) gets validated through `model_validate` at the Router orchestration layer (Story 2-4); validation failures escalate via the schema-fail-retry path. No `BigInt` / `Decimal` / numeric-coercion risk (the only numeric fields are `confidence: float ge=0 le=1` and Pydantic's `float` constraint already covers coercion). `intent_parsing_chat`'s `proposed_filter: FindEmailsFilter | None` nests a pre-existing frozen Pydantic v2 model — Pydantic handles the nested validate at the outer model's validation time.

### 5.11 Git evidence consistency

Files modified (per `git status --porcelain` and the dev-pass File List that will be appended at story close):

```
M  _bmad-output/implementation-artifacts/sprint-status.yaml
M  router/policy.yaml
M  scripts/check_boundaries.py
?? _bmad-output/implementation-artifacts/5-3-*.md (story file)
?? _bmad-output/implementation-artifacts/5-3.pre-review.md (this audit)
?? mailbot_api/prompts/intent_parsing_chat/__init__.py + v1.py
?? mailbot_api/prompts/reference_resolution/__init__.py + v1.py
?? mailbot_api/prompts/draft_reply/__init__.py + v1.py
?? mailbot_api/prompts/tone_style_mirror/__init__.py + v1.py
?? mailbot_api/prompts/multi_turn_refinement/__init__.py + v1.py
?? tests/unit/prompts/test_chat_prompts.py
?? tests/unit/router/test_policy_chat_entries.py
```

All entries correspond to documented AC work; no spurious touches.

Test-to-code ratio (Python pytest regex): test files = 2 new (374 + 96 lines via approximate line count); source files = 5 new prompt module bodies (95 + 90 + 95 + 80 + 75 ≈ 435 lines source) + 4 policy.yaml entries (~40 lines) + 1 boundary-check extension (~7 lines). Approximate test-to-code ratio: ~470 test lines : ~480 source lines = ~0.98. Above the soft floor of 0.5 for pure-data prompt work.

### 5.12 CR-cadence-mandatory surface classification

Six criteria to evaluate:

1. **Load-bearing orchestrator?** NO — this story ships data shapes + policy entries. No new dispatch flow, no new orchestration code, no state machine.
2. **Privacy-invariant surface?** NO — no new sensitivity gating, no Router precondition layer change, no email-body handling, no Graph call. The prompt SYSTEM blocks acknowledge defender tone but introduce no new privacy gate; existing Story 3-3 + Story 4-7 gates remain authoritative.
3. **Cost-discipline surface?** PARTIAL but not mandatory-tier — 2 of the 4 new policy entries route to Opus (`tone_style_mirror`, `multi_turn_refinement`), but routing decisions per AC are explicitly Adam-decided (FR-4.4 — draft quality is tier-1) and the routes were either (a) already-shipped in Story 2-4 (`draft_reply`) or (b) directly cited in the epics.md spec (`tone_style_mirror`, `multi_turn_refinement`). No novel cost-discipline decision is being made here.
4. **New external dependency?** NO — no new pip dependency, no new external API integration, no new container.
5. **Boundary-rule change?** YES (BUT minor) — `_VERBS_IMPORT_ALLOW` extended by one entry to permit `intent_parsing_chat/v1.py` to import `FindEmailsFilter` from the verb-schemas surface. The boundary rule's spirit is preserved (the prompt module IS an agent-facing schema, fits the "verb consumer" category). Documented in a code comment.
6. **Migration / schema change?** NO — no DB migration, no SQL change.

**Cadence verdict: GATE-COVERAGE-ELIGIBLE** — the story is mechanical pure-data work with no novel orchestration, no privacy gate, no cost-discipline novel decision, no migration, no external dep, and only a minor boundary-rule extension (one allowlist line with a documented rationale). The 4 quality gates (pytest, ruff, mypy, boundary check) plus the 29 new tests are the load-bearing evidence; a code-review subagent dispatch is not required by §5.12 to make `done` defensible.

**However**, the orchestrator chooses to dispatch the CR subagent anyway for this story per the autonomous-epic-run posture: this is the FIRST chat-side prompt story in Epic 5, sets the SYSTEM block / persona pattern for Stories 5-4 through 5-9 (high downstream blast radius if the defender voice drifts), and the boundary-rule extension is a non-obvious choice the reviewer should sanity-check. CR dispatch is a defensive choice consistent with the autonomous-epic-run posture; the gate-coverage path remains valid if the CR fails to dispatch.

---

**Posture audit summary table:**

| Check | Verdict |
| --- | --- |
| 5.1 lockfile | N/A — no new deps |
| 5.2 cross-doc | N/A — no schema |
| 5.3 lifecycle-string | N/A — no lifecycle code |
| 5.4 multi-consumer | OK — policy.yaml consumers verified |
| 5.5 screenshot-perception | N/A — no graphical frontend |
| 5.6 upstream-contract | OK — AR-PAT-5 + Pydantic v2 conform |
| 5.7 module-mutable-state | OK — no mutable module-level state |
| 5.8 dev-fixture parity | OK — round-trip identity verified |
| 5.9 grep-verify-figures | OK — counts verified |
| 5.10 producer-boundary | OK — Pydantic v2 validate covers all |
| 5.11 git-evidence | OK — clean changelog |
| 5.12 CR-cadence | **GATE-COVERAGE-ELIGIBLE** (orchestrator electing to dispatch CR anyway for first-chat-story precedent) |
