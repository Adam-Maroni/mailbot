# Pre-Review Self-Audit — 10-6-5

**Generated:** 2026-07-14 by claude-opus-4-8
**Story file:** _bmad-output/implementation-artifacts/10-6-5-hermes-tool-surface-fidelity.md
**Status at audit time:** review (post dev-story, pre code-review)

Story is **config-only** (Hermes tool-surface allow-list in `hermes-config/config.yaml`) + a
3-test offline drift gate. Zero `mailbot_api/` source touched. Fix locus verified live via
Hermes's own `_get_platform_tools` resolver inside the running container.

## 1. AC-vs-code drift scan

- **AC-1** (live "find my unread emails" → `tool_calls_count ≥ 1`): `N/A (dev pass)` — live-Discord L3, Adam-hands-on Phase 3.5. Not dev-codeable. The dev pass delivers the config that MAKES it possible (email verbs dominate the surface) + the L2 resolver proof.
- **AC-2** (surface contains MailBot verbs, NOT dominated by unrelated skills): `MATCH` — `platform_toolsets.discord = [mailbot-api, messaging, cronjob, memory, clarify, skills]` added to `config.yaml`; live resolver → `{clarify,cronjob,kanban,mailbot-api,memory,messaging,skills}`, noise-set (tts/image_gen/vision/file/…) leaked = none, `mailbot-api` present. Config diff + tool list both captured (Debug Log).
- **AC-3** (no regression to drain safety gate): `MATCH` — config-only; gate keys on `(action_type, email_id)`, model- and tool-surface-independent. 65 gate-suite tests green.
- **AC-4** (sensitivity gate preserved): `MATCH` — `mailbot-api` MCP server stays fully enabled; NFR-PRIV-2 enforced at router precondition layer, untouched. Same 65-test suite.
- **AC-5** (MANDATORY-CR reviewer ≠ dev; mailbot_api tests stay green): `MATCH (dev half)` — zero `mailbot_api` change; suite 1940 pass. CR pending Step 2.4.
- **AC-6** (Phase 3.5 live re-walk closes clause 3b): `N/A (dev pass)` — Adam-hands-on.

No AC text required editing (all ACs describe the intended behavior faithfully; the dev pass matched them).

## 2. File-List-vs-git diff check

`git status --porcelain` (story-relevant lines):

```
 M _bmad-output/implementation-artifacts/10-6-5-hermes-tool-surface-fidelity.md
 M _bmad-output/implementation-artifacts/sprint-status.yaml
 M hermes-config/config.yaml
 M tests/integration/test_hermes_config.py
?? _bmad-output/implementation-artifacts/.autonomous-run-active.json   (run-state; not staged into story)
```

Story File List:
- `hermes-config/config.yaml` — `MODIFIED-NOT-STAGED` (will stage Step 2.6) ✅
- `tests/integration/test_hermes_config.py` — `MODIFIED-NOT-STAGED` ✅
- `10-6-5-hermes-tool-surface-fidelity.md` — `MODIFIED-NOT-STAGED` ✅

Out-of-scope working-tree entries (NOT this story, correctly excluded from File List):
- ` M .claude/settings.json` — pre-existing at session start (permission allowlist); background, not staged.
- `?? epic-10-6-retro-2026-07-13-PARTIAL.md` — pre-existing partial-retro artifact; background.
- `sprint-status.yaml` — story-tracking mutation (staged as a story artifact per Step 2.6).

No UNTRACKED story-scope source files. Clean.

## 3. Adversarial self-review

- [LOW] `hermes-config/config.yaml:platform_toolsets.discord` — the allow-list drops `web`/`browser`/`terminal`/`file`/`code_execution`. If a future MailBot turn ever legitimately needs one (e.g. a skill that shells out), it'll be silently unavailable on Discord. **Assessed:** intended — MailBot's whole surface is the 26 MCP verbs + notifications; the defender persona (AGENTS.md "quieter option") wants a *narrow* surface. Documented in the config comment. Not a defect; a deliberate scope decision.
- [LOW] `skills` toolset kept — it surfaces `list/view/manage` meta-tools AND indirectly the 88-skill bundle prompt-injection is a *separate* snapshot mechanism (`.skills_prompt_snapshot.json`), not the `skills` toolset. So keeping `skills` does NOT re-admit the noise tool *schemas*; it keeps skill discovery. **Residual risk:** if Hermes ever routes bundled-skill tool schemas through the `skills` toolset, the noise could partially return. Live resolver (current Hermes build) shows it does not. Flagged to reviewer as the one place the config's sufficiency depends on Hermes-internal behavior I verified empirically, not by contract.
- [LOW] `kanban` appears in the resolved set (Hermes-preserved non-configurable entry). It's a collaboration-board tool, not email-noise, and not in WALK-10-6-4-F1's observed pollution set. Harmless; noted so the reviewer isn't surprised the resolved set has 7 entries vs the 6 I listed.
- [INFO] The drift test asserts a *forbidden set* (12 noise toolsets absent) + a *required set* (5 keep + mailbot-api present) rather than an exact-equality match. Chosen deliberately so a future legitimate keep-set addition (e.g. `session_search`) doesn't false-trip the gate, while re-pollution still red-gates. Trade-off: the test would not catch admitting a *new* noise toolset not in the forbidden set. Acceptable — the forbidden set is the concrete WALK-10-6-4-F1 evidence; broadening it is speculative.
- [INFO] Config comment cites the verify command `docker exec mailbot-hermes hermes tools list --platform discord` — a runbook carry-forward for CP-1, mirroring the `hermes fallback add` precedent. Not executable in tests (needs the live container); that's why AC-6 is a live walk.

Zero-issues would be suspect for a surface change; the above 5 are the genuine judgment calls.

## 4. Self-caught issues remediated this audit

- LOW (narrow surface drops shell toolsets): **ACCEPT WITH RATIONALE** — intended narrow surface for a defender email bot; documented in-config.
- LOW (`skills` kept; sufficiency depends on Hermes-internal behavior): **ESCALATE TO REVIEWER** — the one point where the fix's completeness rests on empirically-observed Hermes resolver behavior rather than a documented contract. Reviewer should sanity-check the reasoning that keeping `skills` doesn't re-admit noise tool schemas.
- LOW (`kanban` in resolved set): **ACCEPT WITH RATIONALE** — Hermes-preserved, non-noise, harmless.
- INFO (forbidden-set vs exact-match test design): **ACCEPT WITH RATIONALE** — deliberate to avoid false-trips; forbidden set = concrete walk evidence.
- INFO (verify command not test-executable): **ACCEPT WITH RATIONALE** — inherently a live-container op; captured as AC-6 + runbook note.

## 5. Posture Audit

### 5.1 Lockfile hygiene

Non-dep-change story. `requirements.txt` untouched.

```
$ git diff --stat -- requirements.txt
(no output)
```

Verdict: ✅ PASS — no dependency change.

### 5.2 Cross-doc pair verification

Cross-doc claims made by this story + their canonical sources:

Claim: "the fix locus is `platform_toolsets.discord` in `config.yaml`, resolved by `_get_platform_tools`" (story Dev Notes).
Canonical source: live container `/opt/hermes/hermes_cli/tools_config.py`.
Verification (run this audit):
```
$ docker exec mailbot-hermes python3 -c "... _get_platform_tools(cfg,'discord') ..."
RESOLVED discord toolsets: ['clarify','cronjob','kanban','mailbot-api','memory','messaging','skills']
NOISE LEAKED: none
mailbot-api present: True
```
Verdict: MATCH — the config key resolves to the claimed surface.

Claim: "mailbot-api exposes 26 verbs incl. find_emails" (story, from WALK-10-6-4-F1).
Canonical source: WALK-10-6-4-F1-hermes-tool-surface-pollution.md:17 (introspected live list).
Verdict: MATCH — carried verbatim from the walk evidence.

§5.2.1 (schema-touching): `N/A` — File List contains no migrations paths (zero SQL, config + test only).

### 5.3 Lifecycle string-uniqueness check

`N/A` — story added no i18n keys (no graphical frontend; config + test only).

### 5.4 Multi-consumer impact scan

`N/A` — story modified no shared hook/service/component. `config.yaml` is a leaf config consumed only by the Hermes runtime at startup (not a code module with in-repo consumers); the test file is a leaf test. Grep confirms no `mailbot_api` module imports `config.yaml` as code.

### 5.5 Screenshot-based perception check

`N/A` — no graphical frontend; AC "visible" surfaces (AC-1/AC-6) are Discord-text L3 walks, verified by DB `router_calls` rows (`tool_calls_count`), not pixels. Adam-hands-on Phase 3.5.

### 5.6 Upstream-contract spec coverage check

`N/A` — story consumes no upstream-stripped/projected field. The config allow-list is purely additive (a new `platform_toolsets` key); it reads no role-gated or projection-contract data.

### 5.7 Module-level mutable container check

`N/A` — story modified zero `.py` source files. `test_hermes_config.py` adds two module-level constants, both immutable-by-design: `frozenset(...)` (`_NOISE_TOOLSETS_FORBIDDEN_ON_DISCORD`, `_REQUIRED_DISCORD_TOOLSETS`) — the §5.7 "acceptable pattern" (frozenset for read-only sets). No mutable dict/list/counter/global.

Grep of the diff for module-scope mutable patterns:
```
$ git diff -- tests/integration/test_hermes_config.py | grep -E "^\+(_[A-Z_]+ = )(\[|\{[^}]*\}$|dict|set\()"
(no mutable-container match — both new module constants use frozenset())
```
Verdict: ✅ PASS — both new module-level containers are `frozenset` (immutable).

### 5.8 Dev-fixture seed-vs-production-shape parity check

`N/A` — story introduced no test fixture consumed by ORM-output/pipeline-payload readers. The new tests parse the real `hermes-config/config.yaml` directly (the file IS the artifact under test, not a synthesized fixture) and assert its shape. No producer-shape drift surface.

### 5.9 grep-verify-cited-figures

Cited numeric figures + verification:

Cite: "suite 1940 passed / 3 skipped / 3 deselected (+3 net vs 1937)" (story Completion Notes + sprint-status).
Verification:
```
$ .venv/Scripts/python.exe -m pytest -q
1940 passed, 3 skipped, 3 deselected, 1 warning in 257.99s
```
Verdict: MATCH. +3 net = the 3 new drift tests (10-6-4 baseline was 1937). Arithmetic: 1937 + 3 = 1940 ✓.

Cite: "65 gate-suite tests green".
Verification:
```
$ pytest test_mint_requires_user_confirmation.py test_sensitive_escalation_handshake.py test_authorization.py -q
65 passed in 4.00s
```
Verdict: MATCH.

Cite: "mypy-strict clean (134 files)".
Verification:
```
$ mypy mailbot_api
Success: no issues found in 134 source files
```
Verdict: MATCH.

Cite: "12 noise toolsets, 5-key keep-set + mailbot-api".
Verification: `_NOISE_TOOLSETS_FORBIDDEN_ON_DISCORD` has 12 members (tts, image_gen, vision, video, file, browser, terminal, code_execution, web, todo, delegation, computer_use = 12 ✓); `_REQUIRED_DISCORD_TOOLSETS` has 5 (mailbot-api, messaging, cronjob, memory, clarify) — note `mailbot-api` is the MCP server, the 4 others are toolsets; `skills` is a 6th allow-list entry not in the required-set assertion (kept but not asserted-required). Verdict: MATCH.

### 5.10 Producer-boundary contract enforcement

`N/A` — story modified no normalizer/DTO/service feeding a typed ORM column, and no service returning an ORM row to an HTTP client. Config + test only; no runtime value production, no typed-column write, no response shape.

### 5.11 Git-evidence consistency check

**5.11.a — File-List-vs-working-tree:**
```
$ git status --porcelain
 M .claude/settings.json                    (background — NOT in File List, correctly excluded)
 M _bmad-output/.../10-6-5-...md             STAGED-PENDING + IN FILE LIST ✅
 M _bmad-output/.../sprint-status.yaml       story-tracking artifact ✅
 M hermes-config/config.yaml                 MODIFIED + IN FILE LIST ✅
 M tests/integration/test_hermes_config.py   MODIFIED + IN FILE LIST ✅
?? .autonomous-run-active.json               run-state (not a story artifact) ✅
?? epic-10-6-retro-...PARTIAL.md             background ✅
```
Verdict: ✅ PASS — every File List path is in git output; no silent scope-creep (`.claude/settings.json` + retro PARTIAL are pre-existing background, not staged into this story).

**5.11.b — Production-only test-to-code ratio:**
```
$ git diff --numstat -- hermes-config/config.yaml tests/integration/test_hermes_config.py
46  0  hermes-config/config.yaml
89  0  tests/integration/test_hermes_config.py
```
- testAdded = 89 (test_hermes_config.py)
- config.yaml (46 added) is a `.yaml` config → counts as docs per the §5.11.b classifier (non-test, non-prod-logic). prodAddedExcludingDocs = 0.
- prodOnlyTestRatio = 89 / 0 = null (denominator 0) → ratio gate N/A.
Verdict: ✅ PASS (N/A denominator) — zero production-logic lines added; all additions are test (89) + config-as-docs (46). A config-only story with a drift test is the ideal ratio shape.

**5.11.c — No-later-commits-under-attribution:**
Story status-flip to in-progress: 2026-07-14 (same session).
```
$ git log --oneline --since="2026-07-14 12:00" -- hermes-config/config.yaml tests/integration/test_hermes_config.py
(empty)
```
Verdict: ✅ PASS — same-session dev pass, no commits under attribution since flip.

### 5.12 CR-cadence-mandatory surface classification

Criterion 1 (boundary-introducing): NO — no new writer-monopoly / lint boundary / check_boundaries allowlist. A config allow-list is not a code invariant surface.
Criterion 2 (dep-introducing): NO — requirements.txt untouched; no new package/adapter/MCP server (mailbot-api already registered).
Criterion 3 (dev-self-flagged): **YES** — §4 escalated one LOW to the reviewer (the `skills`-kept sufficiency reasoning rests on empirically-observed Hermes resolver behavior).
Criterion 4 (capstone): NO — 10-6-5 is a sibling story; 10-6-2 (clause 4) is the other open done-flip dependency. Not last-in-epic (the retro + done-flip come after).
Criterion 5 (privacy-invariant): NO directly — but AC-3/AC-4 assert the sensitivity + drain-safety gates are unaffected. The story does not *modify* enforcement (config-only), but it touches the surface those gates live behind. Bias toward review.
Criterion 6 (load-bearing-orchestrator): **YES** — the Discord per-turn tool surface is the primary integration surface for EVERY real user turn (it decides which verbs the model can call). The story ships the config that governs that surface. "Other turns/epics lean on this" is exactly the criterion.

Cadence verdict: **MANDATORY-CR** (criteria 3 + 6 fire; criterion 5-adjacent reinforces). Reviewer ≠ dev (sonnet-5 ≠ opus-4-8), persona/tool-registration seam.

## Posture Audit summary table

| Check                                            | Status                                    |
| ------------------------------------------------ | ----------------------------------------- |
| 5.1 Lockfile hygiene                             | ✅ PASS — no dep change                    |
| 5.2 Cross-doc pair verification                  | ✅ PASS — config→resolver + walk verified  |
| 5.3 Lifecycle string-uniqueness                  | N/A — no i18n keys                         |
| 5.4 Multi-consumer impact scan                   | N/A — no shared code module modified       |
| 5.5 Screenshot-based perception check            | N/A — no graphical frontend               |
| 5.6 Upstream-contract spec coverage              | N/A — no upstream-projected field consumed |
| 5.7 Module-level mutable container               | ✅ PASS — new constants are frozenset       |
| 5.8 Dev-fixture seed-vs-production-shape parity  | N/A — no fixture; test parses real config  |
| 5.9 grep-verify-cited-figures                    | ✅ PASS — all cites command-verified        |
| 5.10 Producer-boundary contract enforcement      | N/A — no typed-column/HTTP producer touched|
| 5.11 Git-evidence consistency check              | ✅ PASS — scope clean, ratio N/A, no commits|
| 5.12 CR-cadence classification                   | MANDATORY-CR (criteria 3 + 6)              |

One item ESCALATED to reviewer (§4): verify the `skills`-kept reasoning does not re-admit noise tool schemas.
