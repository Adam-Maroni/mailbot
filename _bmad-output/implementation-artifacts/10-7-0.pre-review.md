# Pre-Review Self-Audit — 10-7-0

**Generated:** 2026-07-15 12:xx by claude-opus-4-8
**Story file:** _bmad-output/implementation-artifacts/10-7-0-characterization-spike-harness-vs-3b-ceiling.md
**Status at audit time:** review (post dev-story, pre code-review)
**Cadence:** §5.12 self-audit (characterization spike, NO product code → no MANDATORY-CR, per epics.md § Epic 10.7 story table).

## 1. AC-vs-code drift scan

- **AC-1 (reproduce the two defects):** MATCH — probe reproduced SELECTION (6/20 wrong baseline); FORMAT did NOT reproduce on direct-drive (0/20), which AC-1 explicitly names a first-class finding ("if either defect does not reproduce... record it"). Recorded in §1 of the finding.
- **AC-2 (isolate SELECTION from FORMAT):** MATCH — finding §2 attributes FORMAT→Hermes-surface, SELECTION→missing system prompt / sibling-verb ambiguity; the 4-cell harness reports them independently.
- **AC-3 (A/B levers, measured):** MATCH — system-prompt (Lever B) 14→20/20, rescue-parser feasibility framed (Lever A), surface-trim measured NOT to help (Lever C). All in finding §3.
- **AC-4 (temp-0 fidelity not traded):** MATCH — 3/3 exact arg round-trip under the recommended lever; finding §3 records it as PRESERVED.
- **AC-5 (recorded go/no-go finding):** MATCH — `10-7-0-spike-finding.md` verdict HARNESS-FIXABLE with an explicit fire-list; discharges clause 1.
- **AC-6 ($0 + safety framing):** MATCH — finding §5: local/$0 preserved, safety pipeline unperturbed (no product code).

No DRIFT — no AC text needed correcting.

## 2. File-List-vs-git diff check

`git status --porcelain`:
```
 M _bmad-output/implementation-artifacts/sprint-status.yaml   (MODIFIED — story-flip, expected)
?? _bmad-output/implementation-artifacts/10-7-0-characterization-spike-harness-vs-3b-ceiling.md  (UNTRACKED — new story file)
?? _bmad-output/implementation-artifacts/10-7-0-spike-finding.md  (UNTRACKED — new finding)
(10-7-0.pre-review.md — this file, UNTRACKED, created now)
```
File-List cross-check:
- `10-7-0-characterization-spike-harness-vs-3b-ceiling.md` — UNTRACKED (new) — will be staged at Step 2.6.
- `10-7-0-spike-finding.md` — UNTRACKED (new) — will be staged.
- `10-7-0.pre-review.md` — UNTRACKED (new) — will be staged.
- `scratch/qwen_toolcall_spike_107.py` — **correctly ABSENT from git status** (scratch/ gitignored per Story 10-6-3). File List marks it "NOT staged" intentionally. ✅ consistent.
- No source files modified (spike). `sprint-status.yaml` modification is the expected status-flip, not a File-List item.

Not-in-scope in working tree: ` M .claude/settings.json` (pre-existing, unrelated — the autonomous-run permission env; NOT staged by this story). `.autonomous-run-active.json` (run state file, torn down at end).

No UNTRACKED files that belong in the File List are being left behind. ✅

## 3. Adversarial self-review

- [LOW] `scratch/qwen_toolcall_spike_107.py` — the "clean 5-toolset surface" schemas are **hand-authored minimal stubs**, not pulled live from the mailbot-api MCP `find_emails`/`send_message` schemas. Risk: real schemas (richer descriptions) could shift selection. Mitigation: the SELECTION finding is direction-robust (system prompt fixed it regardless of exact schema wording), and the finding explicitly flags that the 10.7.2 implementation walk must re-confirm on the real Router path. Acceptable for a spike.
- [MEDIUM] The FORMAT-defect "did not reproduce" conclusion is drawn from **direct-drive only** — it does NOT prove qwen never text-emits; it proves the clean-surface + small-tool-list condition avoids it. The walk (id=14937) is the counter-evidence under the real Hermes surface. Finding §2/§3 handles this correctly by keeping 10.7.1 (rescue parser) on the fire-list as defensive rather than claiming the format defect is gone. Not an overclaim in the finding, but worth the reviewer confirming the framing holds.
- [LOW] N=5 repeats at temp 0 is small; temp-0 is deterministic per (turn,surface,system) so repeats mostly confirm no drift rather than build a distribution. The real signal is cross-turn (4 paraphrases). Adequate for a go/no-go, not a production SLA.
- [LOW] The system-prompt lever text tested in the spike is a *probe* prompt, not the exact string 10.7.2 will ship; the 20/20 result validates the approach, not a specific production string. Finding §4 states 10.7.2 must re-validate — no overclaim.
- [LOW] Only `send_message` was tested as the messaging mis-pick peer; the real messaging toolset may expose other peers. But since the measured mis-pick was a *MailBot sibling verb* (count_emails), the messaging-peer surface is not the selection driver — this weakness doesn't change the verdict.

## 4. Self-caught issues remediated this audit

- [LOW] hand-authored schemas → **ACCEPT WITH RATIONALE** (spike isolates the model; 10.7.2 re-validates on real Router path; finding says so).
- [MEDIUM] FORMAT "did not reproduce" scope → **ACCEPT WITH RATIONALE / ESCALATE TO REVIEWER** — the finding already frames it correctly (keeps 10.7.1 defensive, does not claim the defect is eliminated). Flagged for the reviewer to confirm the framing is not an overclaim. No code to fix (spike).
- [LOW] small N / determinism → **ACCEPT WITH RATIONALE** (temp-0 determinism makes N=5 sufficient for go/no-go).
- [LOW] probe-prompt ≠ ship-prompt → **ACCEPT WITH RATIONALE** (finding §4 requires 10.7.2 re-validation).
- [LOW] single messaging peer → **ACCEPT WITH RATIONALE** (mis-pick is a MailBot sibling verb, so messaging-peer breadth is not the driver).

§3 is not empty (5 issues, non-shallow). No FIX-NOW items because there is no product code — all dispositions are rationale/escalate for a spike finding.

## 5. Posture Audit (§5.1–§5.11) — spike cadence, product-code checks N/A-with-justification

- **5.1 Tests added/updated for the change:** N/A — spike adds NO product code; the "test" is the reproducibility of the probe (documented Appendix in the finding: 4 CLI modes). No `tests/` change is in-scope (the `<tool_call>`-text-parse test gap is 10.7.1's).
- **5.2 Full suite green:** PASS — `.venv/Scripts/python.exe -m pytest -q` → **1941 passed, 3 skipped, 3 deselected** (unchanged from baseline 1941 — confirms no product code perturbed).
- **5.3 Lint clean:** PASS — `.venv/Scripts/python.exe -m ruff check .` → "All checks passed!" (scratch/ excluded per 10-6-3).
- **5.4 Type-check clean:** N/A — no `mailbot_api/` change; `scratch/` is mypy-excluded (10-6-3). Nothing new for mypy to check.
- **5.5 Boundary/architecture rules:** PASS-by-construction — no production code added; probe lives in `scratch/` (boundary-exempt, gitignored) and drives ollama directly only to isolate the MODEL for the spike — legitimate spike methodology, no Rule-I production-code violation (Rule I governs `mailbot_api/`, untouched).
- **5.6 No secrets / tokens committed:** PASS — probe uses no credentials; ollama is local unauthenticated `:11434`; no API keys. Finding + story contain no secrets.
- **5.7 Migrations / schema:** N/A — no DB change.
- **5.8 Error-handling / errors-as-data (AR-PAT-4):** N/A for product code; the probe's own `json.loads` guard on `<tool_call>` text degrades to `<malformed-json>` rather than crashing — defensive, spike-local.
- **5.9 Privacy / sensitivity invariant (F28, Qwen-lock):** N/A-preserved — no product code touched; the model-independent propose→grant→drain pipeline and the Qwen-locked classifier are unperturbed (framing check in finding §5).
- **5.10 Docs / completion notes updated:** PASS — story Dev Agent Record + Completion Notes + Change Log filled; `10-7-0-spike-finding.md` written; File List accurate.
- **5.11 Staging hygiene (no `git add -A`):** PASS-planned — Step 2.6 will selectively stage the 3 tracked artifacts (story, finding, pre-review) + the sprint-status flip; scratch harness stays unstaged (gitignored); pre-existing `.claude/settings.json` modification left untouched.
- **5.12 Spike self-audit forcing-function:** PASS — this artifact IS the §5.12 cadence; the go/no-go finding is recorded, measured, and reproducible; no product code shipped so MANDATORY-CR is correctly replaced by this self-audit (epics.md § Epic 10.7 table).

**Gate result: all 5 sections complete; §3 non-shallow (5 issues); §4 dispositions for each; §5 all sub-sections addressed with command output or N/A justification. Cleared to spawn Step 2.4 code-review.**
