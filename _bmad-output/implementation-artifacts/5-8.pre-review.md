# Pre-Review Self-Audit — 5-8

**Generated:** 2026-06-02 (autonomous-epic-run dev pass)
**By:** claude-opus-4-7 (1M context)
**Story file:** _bmad-output/implementation-artifacts/5-8-conversational-reference-resolution-built-and-instrumented-fr-4-3-validated-in-epic-7.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- AC-1 (module + dataclasses): MATCH — `DiscordTurn`, `ReferenceContext`, `ReferenceResolutionResult` all shipped as frozen dataclasses with the documented field shapes.
- AC-2 (builder): MATCH — `build_reference_resolution_content` produces exactly 3 keys (`user_message`, `recent_context`, `candidate_projections`) matching the Story 5-3 USER_TEMPLATE placeholders; memory + sender_summaries threaded with `---` separators per the Story 5-3 placeholder injection contract.
- AC-3 (dispatch): MATCH — validation refuses empty/wrong-role contexts WITHOUT dispatching; happy path parses the output; failure paths collapse to ambiguous=True.
- AC-4 (read-only memory): MATCH — no memory writes; `relevant_senders_memory=None` is a documented happy path (cold-start).
- AC-5 (router_calls implicit): MATCH — `ask_router` writes the audit row internally per Story 2-1; the test verifies dispatch happens with `task_type="reference_resolution"`.
- AC-6 (integration tests ≥ 9): MATCH — 12 tests pass.
- AC-7 (boundary): MATCH — `_VERBS_IMPORT_ALLOW` extended for `mailbot_api/chat/reference.py`; check passes.
- AC-8 (gates green): MATCH — 829 tests pass (+12 net from 817 baseline; meets the AC floor of ≥ 826); ruff clean; mypy clean; boundary clean.

## 2. File-List-vs-git diff check

NEW (untracked):
- `mailbot_api/chat/reference.py`
- `tests/integration/test_reference_resolution.py`
- `_bmad-output/implementation-artifacts/5-8-*.md` (story file)
- `_bmad-output/implementation-artifacts/5-8.pre-review.md`

MODIFIED (tracked):
- `scripts/check_boundaries.py` — `_VERBS_IMPORT_ALLOW` gains `mailbot_api/chat/reference.py`.
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — 5-8 row flip.

## 3. Adversarial self-review

- [LOW] `router_call_id: int | None` is currently ALWAYS set to `None` because the existing `RouterResult` contract does NOT surface the SQL `router_calls.id`. The AC-1 field exists for forward-compat — future Router work can add `router_call_id` to `RouterResult` and this orchestrator already plumbs it through. Documented in code comment. Acceptable for now since Epic 7's sampler queries `router_calls` by `task_type` + timestamp, not by id linkback.
- [LOW] `sanitize_error(Exception(err.message))` wraps the string in an `Exception` just to feed sanitize_error's signature contract. Cleaner would be a `sanitize_str` helper. The wrap-then-unwrap pattern works and matches `RouterError` reasoning practice; not a correctness issue.
- [LOW] `test_router_ok_but_no_output_collapses_to_ambiguous` uses `RouterResult.model_construct` to bypass the `_check_ok_error_consistency` validator (which would otherwise reject `ok=True, output=None, error=None`). The defensive test exercises an "impossible" Router output that protects future-self if a Router bug regresses the invariant. Acceptable defensive test.

## 4. Self-caught issues remediated this audit

- All 3 LOW items: **ACCEPT WITH RATIONALE** — documented in code + test comments. No fixes needed.

## 5. Posture Audit

### 5.1 Lockfile

N/A — no new deps.

### 5.2 Cross-doc

N/A — no schema change.

### 5.3 Lifecycle-string

N/A — no new lifespan code.

### 5.4 Multi-consumer

`resolve_reference` consumers:
- Story 5-9 chat orchestrator (capstone) will call this on every Discord message that resolves to a referential intent.
- No other consumers ship in this story.

### 5.5 Screenshot-perception

N/A.

### 5.6 Upstream contract

- The orchestrator's `content` dict matches the Story 5-3 reference_resolution prompt USER_TEMPLATE placeholders exactly (test pinned).
- Router contract honored (ask_router returns RouterResult; ok=True / ok=False mutually exclusive).
- `RouterError` is passed through to the result's `error` field unchanged.

### 5.7 Module-mutable state

`reference.py` has no module-level mutable state. All state passes through the `ReferenceContext` dataclass. **OK.**

### 5.8 Dev fixture parity

Tests construct `ReferenceContext` via the same `DiscordTurn` + `EmailProjection` dataclasses production code uses. No separate fixture shape. **OK.**

### 5.9 Grep-verify cited figures

- `817 baseline tests`: verified at Story 5-7 close.
- `+12 net tests`: verified (5 builder + 2 refuse-without-dispatch + 4 result-handling + 1 dispatch-args).
- `9 ACs`: matches the story file's AC headers.

### 5.10 Producer-boundary contract

`ReferenceResolutionOutput` (Pydantic v2 frozen, from Story 5-3) is the producer-side contract. The orchestrator parses dict-shaped Router output via `model_validate(.model_dump())`. Round-trip safe.

### 5.11 Git evidence

Tests vs code: 1 test file (~250 lines) + 1 module (~200 lines). Test:code ratio ~1.25. Above the 0.5 floor.

### 5.12 CR-cadence-mandatory surface classification

1. **Load-bearing orchestrator?** YES — `resolve_reference` is THE entry point Story 5-9 will call for every chat turn that needs reference resolution. Drift here corrupts every multi-turn conversation.
2. **Privacy-invariant surface?** PARTIAL — dispatches via `ask_router` with `email_id=None` (cross-email-task bypass per Story 3-3). The bypass is documented + tested; the Router's precondition layer still applies to the prompt's content (no body bytes leak through `recent_context` because the chat orchestrator threads only projections + summaries, never bodies).
3. **Cost-discipline surface?** PARTIAL — `caller_origin="verb-ask-router"` is the documented default; Story 5-9 may override. Cost is policy-driven (Qwen first, Haiku escalation per Story 5-3 AC-7 policy entry).
4. **New external dependency?** NO.
5. **Boundary-rule change?** YES (minor) — `_VERBS_IMPORT_ALLOW` extended by one entry, same pattern as Stories 5-3 / 5-6.
6. **Migration / schema change?** NO.

**Cadence verdict: MANDATORY-CR.** Criterion 1 (load-bearing orchestrator — the reference-resolution surface for the entire chat experience) fires; the FR-4.3 ≥ 90% threshold validated in Epic 7 depends on this orchestrator producing the right `router_calls` rows. A drift here is invisible until Epic 7's sampler runs.

CR dispatch required.

---

**Posture audit summary table:**

| Check | Verdict |
| --- | --- |
| 5.1 | N/A |
| 5.2 | N/A |
| 5.3 | N/A |
| 5.4 | OK — Story 5-9 future consumer |
| 5.5 | N/A |
| 5.6 | OK — Story 5-3 USER_TEMPLATE pinned |
| 5.7 | OK — no module-level mutable state |
| 5.8 | OK — production-shape dataclasses |
| 5.9 | OK |
| 5.10 | OK — Pydantic v2 round-trip |
| 5.11 | OK |
| 5.12 | **MANDATORY-CR** — load-bearing orchestrator for chat reference-resolution |
