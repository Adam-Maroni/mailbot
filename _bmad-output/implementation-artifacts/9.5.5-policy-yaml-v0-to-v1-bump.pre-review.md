# Pre-Review Self-Audit — 9.5.5-policy-yaml-v0-to-v1-bump

**Generated:** 2026-07-04 by claude-fable-5 (autonomous-story-run inline dev walk)
**Story file:** _bmad-output/implementation-artifacts/9.5.5-policy-yaml-v0-to-v1-bump.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- AC-1: MATCH — verdict review executed against the pre-rendered report artifact `9.5.3-report-db48480f.md` + walk-evidence interpretation (Adam-signed 2026-07-03); `INSUFFICIENT_DATA` → bullet-3 no-change path taken. Note: the AC's "Adam reads the rendered report" clause was discharged UPSTREAM at the 9.5.3 walk (Adam read + signed the interpretation); this run re-verified the artifacts exist and re-applied the decision, it did not re-render.
- AC-2: N/A — change path; verdict implies no routing change (per AC-1 bullet 3). Hot-reload audit-log verification is change-path-only.
- AC-3: MATCH — version bumped `policy-v0-2026-06-01` → `policy-v1-2026-07-04`; hypothesis annotations on all 3 Opus tasks augmented with `# v1-review (2026-07-04)` lines, none removed; retro entry drafted in `epic-9-5-retro-2026-07-04.md` with run_id + report-path citation. DRIFT-ADJACENT (declared, not silent): the entry is **PENDING-ADAM-SIGNATURE** — the AC requires "Adam-signed"; an autonomous run cannot self-sign. Signature is requested at Phase 3.5 (PASS = granted, entry edited to signed). Escalated in §4.
- AC-4: MATCH — `tests/integration/test_policy_v1_loads_cleanly.py` covers (1) real-file `load_policy()` success, (2) `version == "policy-v1-2026-07-04"`, (3) Story 9-4 shallow-leaf overrides merge on v1 (+ a 4th route-(b) guard test: routing unchanged, hypotheses non-null).
- AC-5: MATCH — E2E test runs the 5-item canary through the real `benchmark.runner` under the real repo v1 policy: rows freeze `router_policy_version=policy-v1-2026-07-04`, stored `cohort_key` recomputed via `compute_cohort_key` from row-frozen components; historical-v0 rows (simulated via `model_copy(update={"version": v0})` run, all other components identical) remain queryable post-v1 with disjoint cohort_key. The AC's live-container reading of this check is not autonomously reachable at $0; the E2E test uses the REAL runner/Router path with only the adapter faked (9-8 precedent). Escalated in §4 as ACCEPT WITH RATIONALE.
- AC-6: MATCH — MANDATORY-CR FULL scope verdict recorded at §5.12; Step 2.4 dispatch follows this artifact.

## 2. File-List-vs-git diff check

`git status --porcelain` (rtk-compact, pasted in §5.11.a): policy.yaml M / retro M / sprint-status M / settings.json M (pre-existing) / story file ?? / test file ?? / state file ?? / scratch/ ?? (pre-existing).

- `router/policy.yaml` — MODIFIED-NOT-STAGED (staged at Step 2.6)
- `tests/integration/test_policy_v1_loads_cleanly.py` — UNTRACKED (staged at Step 2.6)
- `_bmad-output/implementation-artifacts/epic-9-5-retro-2026-07-04.md` — MODIFIED-NOT-STAGED (staged at Step 2.6)
- `_bmad-output/implementation-artifacts/9.5.5-policy-yaml-v0-to-v1-bump.md` — UNTRACKED (staged at Step 2.6)

All four File List paths present in git output; staging is deferred to Step 2.6 by the run's own contract, so UNTRACKED here is expected, not a gap.

## 3. Adversarial self-review

- [MEDIUM] router/policy.yaml:~50 — draft_reply v1-review comment cited haiku's subjective scores as anti-demotion evidence without noting those scores came from the pre-leak-fix (contaminated-calibration) evaluator run; honesty gap between the comment and the retro entry's caveat #3.
- [MEDIUM] epic-9-5-retro-2026-07-04.md § 9.5.5 entry — AC-3 requires "Adam-signed"; the drafted entry is unsigned by construction (autonomous run). Risk: story marked done with an AC clause formally undischargeable until Phase 3.5.
- [MEDIUM] tests/integration/test_policy_v1_loads_cleanly.py:~245 — AC-5's literal wording implies a live post-v1 runner invocation; the test satisfies it at the E2E-with-faked-adapter level ($0 spend). If the reviewer reads the AC hyper-literally, this is a scope reduction.
- [LOW] test file `_V1_VERSION` hardcodes the dated version string in two assertions — any future v2 bump must touch this test. Intentional: the test IS the pin; a silent version change should fail loudly.
- [LOW] `.autonomous-run-active.json` `started_at` is a placeholder midnight UTC timestamp (orchestrator has no reliable clock read); memo-only field, never consumed by assertions.
- [LOW] `_ScriptedAdapter.model_id` attribute is set but unused by the registry path (mirrors 9-8 canary adapter shape for consistency).

## 4. Self-caught issues remediated this audit

- [MEDIUM] contaminated-scores honesty gap → **FIX NOW** — FIXED: comment extended with "Scores are from the pre-leak-fix evaluator run (evaluator-trust caveats in epic-9-5-retro-2026-07-04.md § 'policy.yaml v0→v1')"; tests re-run 5/5 green; diff now 18 insertions / 1 deletion, still comments-only + version line.
- [MEDIUM] PENDING-ADAM-SIGNATURE vs "Adam-signed" AC clause → **ESCALATE TO REVIEWER** + Phase 3.5 — reviewer should verify the entry's content satisfies the AC's required statement verbatim-in-substance; the signature itself is granted (or refused) by Adam at Phase 3.5. Precedent: 9.5.4 Path B entry was Adam-signed in-session; the autonomous analog is the Phase 3.5 verdict.
- [MEDIUM] AC-5 E2E-vs-live reading → **ACCEPT WITH RATIONALE** — the E2E test drives the REAL `runner_main` → Router precondition/lane/cost → `record_benchmark_run` path with only the model adapter faked at the registry boundary (Story 9-8 CR-cleared precedent); a live-container re-run would add real spend for zero additional cohort_key coverage (the component is frozen at `_read_policy_versions()` which the E2E exercises verbatim). Live confirmation arrives free with the next real benchmark run (v2 gate work).
- [LOW] ×3 (version-string pin / state-file timestamp / adapter attribute) → **ACCEPT WITH RATIONALE** — each documented in §3; none has a runtime surface.

## 5. Posture Audit

### 5.1 — Lockfile hygiene

```
$ rtk git diff --stat -- requirements.txt
(no output)
```

✅ PASS — no dependency changes; requirements.txt untouched.

### 5.2 — Cross-doc pair verification

Claim 1: "verdict token INSUFFICIENT_DATA, route (b), run_id db48480f-9692-4791-b3e2-4b3a2ab1fed8" (story Dev Notes + policy.yaml comments + retro entry)

```
$ Grep "db48480f|INSUFFICIENT_DATA" 9.5.3-walk-evidence.md
105: Verdict token for Story 9.5.5 pickup: **`INSUFFICIENT_DATA` ...** ... Story 9.5.5 → **route (b) accept-with-rationale**.
121: **run_id:** `db48480f-9692-4791-b3e2-4b3a2ab1fed8`  ← **Story 9.5.5 citation source for route (b) rationale**
```

Verdict: MATCH.

Claim 2: "report artifact exists at 9.5.3-report-db48480f.md"

```
$ Glob _bmad-output/implementation-artifacts/9.5.3-report-db48480f.md
_bmad-output\implementation-artifacts\9.5.3-report-db48480f.md
```

Verdict: MATCH.

Claim 3: "haiku subjective_overall=2.6176 (n=34)" (policy.yaml comment + retro entry) — canonical source 9.5.3-walk-evidence.md:153 `subjective_overall: 2.6176 (5-scale, based on 34 anchor-calibrated evaluations)`. Verdict: MATCH.

§5.2.1 schema-touching branch: N/A — File List contains no migrations paths.

### 5.3 — Lifecycle string-uniqueness

N/A — story added no i18n keys (no i18n surface exists in this project).

### 5.4 — Multi-consumer impact scan

Modified shared config: `router/policy.yaml` (consumed via `load_policy`/`get_policy`).

```
$ grep -rl "load_policy" mailbot_api benchmark scripts   (pyc noise elided)
mailbot_api/ingest/pipeline.py
mailbot_api/main.py
mailbot_api/router/policy.py
mailbot_api/sensitivity/__init__.py
scripts/mailbot.py
(+ benchmark/runner.py via get_policy at _read_policy_versions)
```

Semantic delta for ALL consumers: the `version` string value only (comments invisible to the YAML parser). No task entry, model, lane, token cap, or prompt_version changed — verified by `git diff` (18 insertions: 17 comment lines + 1 version line; 1 deletion: old version line). Version-string consumers: Story 9-1 reload logging (string compare — tolerant), Story 9-6 cohort composition (covered by the new AC-5 E2E test). Full suite 1685 passed confirms no consumer regression. ✅ PASS.

### 5.5 — Screenshot-based perception check

N/A — project has no graphical frontend; no AC asserts human-visible UI state.

### 5.6 — Upstream-contract spec coverage

N/A — story consumes no upstream-stripped/projection contract; the one upstream artifact (9.5.3 verdict) is a static walk-evidence document verified at §5.2.

### 5.7 — Module-level mutable container check

Story modified zero production `.py` files. New test file's module-level names: `_REPO_POLICY_PATH` (Path, treated read-only), `_V0_VERSION`/`_V1_VERSION`/`_QWEN`/`_COST_MOCK_ENV` (str — immutable). No module-level dict/list/set; all mutable state is function-local or fixture-reset. ✅ PASS.

### 5.8 — Dev-fixture seed-vs-production-shape parity

Fixture 1: `evals/fixtures/canary_5.jsonl` — Pattern 1 (recorded canonical Story 9-5 fixture, unmodified, copied to tmp per 9-8 precedent). PASS.
Fixture 2: `_ScriptedAdapter` JSON response `{"class_coarse": "newsletter", "confidence": 0.9}` — Pattern 3 shape-faithful synthesis, byte-matching the 9-8 canary's `_good_coarse_class_response()` (CR-cleared in Story 9-8; the coarse_class output schema is the producer contract). Infeasibility of patterns 1/2: recording a real qwen response requires a live Ollama round-trip the hermetic suite is not set up for (same rationale as 9-8). Drift sentinel: prompt-module schema validation inside the real Router path rejects shape drift at dispatch time (the test would fail with schema_failed outcome). PASS.

### 5.9 — grep-verify-cited-figures

Cite 1: "full suite 1685 passed + 2 skipped + 3 deselected (+5 net vs 1680 baseline)" — story Completion Notes + sprint-status row.

```
$ .venv/Scripts/python.exe -m pytest -q
1685 passed, 2 skipped, 3 deselected, 1 warning in 215.24s
```

Baseline 1680+2+3 per sprint-status 9.5.4 close row (frozen historical figure, verified at commit 65d9bd7). Arithmetic: 1685 − 1680 = +5 = number of tests in the new file. Verdict: MATCH.

Cite 2: "5 tests in test_policy_v1_loads_cleanly.py"

```
$ pytest tests/integration/test_policy_v1_loads_cleanly.py -q
5 passed in 2.26s
```

Verdict: MATCH.

Cite 3: "policy.yaml diff = 18 insertions / 1 deletion, comments + version line only"

```
$ rtk git diff --stat -- router/policy.yaml
router/policy.yaml | 19 ++++++++++++++++++-
 1 file changed, 18 insertions(+), 1 deletion(-)
```

(Post-§4-fix figure; the pre-fix 16/1 cite in Task 3's checkbox note was superseded — noted here rather than rewritten, per no-rewrite discipline the +2 delta is the §4 FIX NOW comment extension.) Verdict: MATCH with counting rationale.

Cite 4: "1531+2+3 / 1680 baselines, run_id, α values in Dev Notes" — frozen historical figures cited with source documents (sprint-status rows, walk evidence), not re-derived. Exempt as frozen-historical with named source.

### 5.10 — Producer-boundary contract enforcement

N/A — story modified no normalizer/DTO/service feeding typed columns AND no service returning rows to HTTP clients (config version-string + comments + tests + docs only).

### 5.11 — Git-evidence consistency

#### 5.11.a File-List-vs-working-tree

```
$ rtk git status
~ Modified: .claude/settings.json | epic-9-5-retro-2026-07-04.md | sprint-status.yaml | router/policy.yaml
? Untracked: .autonomous-run-active.json | 9.5.5-policy-yaml-v0-to-v1-bump.md | scratch/ | tests/integration/test_policy_v1_loads_cleanly.py
```

- policy.yaml / retro / story file / test file — IN FILE LIST + in git output ✅
- `sprint-status.yaml` M — NOT IN FILE LIST — run-artifact convention (every story run mutates it; staged at 2.6 per prior-story precedent) — classified story-adjacent, will stage.
- `.claude/settings.json` M — NOT IN FILE LIST — PRE-EXISTING at run start (git-status snapshot in session header shows it modified before this run) — NOT story scope, will NOT stage.
- `scratch/` ?? — PRE-EXISTING 9.5.3/9.5.4 walk leftover — NOT story scope, will NOT stage (carries the 2 ruff T201s flagged in run-flags).
- `.autonomous-run-active.json` ?? — run state file, torn down at Phase 3.5 — never staged.

Verdict: ✅ PASS — no silent scope-creep; two out-of-scope modifications positively identified and excluded.

#### 5.11.b Production-only test-to-code ratio

Nothing staged yet; applying the classifier to the would-be-staged set: testAdded ≈ 290 (new test file), docsAdded ≈ story file + retro delta + sprint-status (md/yaml planning artifacts), prodAddedExcludingDocs = 18 (policy.yaml — all-but-one comment lines). Ratio 290/18 ≈ 16.1 ≥ 0.3. ✅ PASS.

#### 5.11.c No-later-commits-under-attribution

Single-session dev pass (status flipped in-progress and developed in this same session; baseline_commit 65d9bd7 is HEAD). N/A.

### 5.12 — CR-cadence-mandatory surface classification

Criterion 1 (boundary-introducing): NO — no new writer monopoly / lint boundary / allowlist.
Criterion 2 (dep-introducing): NO — requirements.txt untouched (§5.1).
Criterion 3 (dev-self-flagged): YES — §4 carries one ESCALATE TO REVIEWER (PENDING-ADAM-SIGNATURE vs "Adam-signed" AC clause).
Criterion 4 (capstone): YES — 9.5.5 is the last story in Epic 9.5 (load-bearing closure story for Epic 9.5 + Epic 9 done-flip clause 11).
Criterion 5 (privacy-invariant): NO — no FR-2.x / NFR-PRIV-* surface touched.
Criterion 6 (load-bearing-orchestrator): YES — `router/policy.yaml` is THE production routing policy every dispatch reads; story spec itself pre-declares criterion 6 + MANDATORY-CR FULL scope.

**Cadence verdict: MANDATORY-CR (criteria 3 + 4 + 6 fire) — FULL scope per story AC-6. BINDING.**

### Posture Audit summary table

| Check | Status |
| --- | --- |
| 5.1 Lockfile hygiene | ✅ PASS |
| 5.2 Cross-doc pair verification | ✅ PASS (schema branch N/A — no migrations) |
| 5.3 Lifecycle string-uniqueness | N/A — no i18n keys |
| 5.4 Multi-consumer impact scan | ✅ PASS |
| 5.5 Screenshot-based perception | N/A — no graphical frontend |
| 5.6 Upstream-contract spec coverage | N/A — no projection contract consumed |
| 5.7 Module-level mutable container | ✅ PASS |
| 5.8 Dev-fixture parity | ✅ PASS |
| 5.9 grep-verify-cited-figures | ✅ PASS |
| 5.10 Producer-boundary contract | N/A — no producer surface |
| 5.11 Git-evidence consistency | ✅ PASS (5.11.c N/A — single-session) |
| 5.12 CR-cadence classification | **MANDATORY-CR (FULL scope; criteria 3+4+6)** |
