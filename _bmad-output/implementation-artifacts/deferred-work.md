# Deferred Work

## Deferred from: code review of 10-5-1-safety-kill-switch-coverage-and-per-process-singleton-class (2026-07-07)

- `ask_router`'s permitted-interpretation branch (router.py:349-375) falls through to full dispatch with no second pause re-check immediately before the real adapter call; safety currently rests entirely on `hermes_aux` being documented as text-only/non-action-producing, not on a structural gate. Defense-in-depth architectural concern, not a demonstrated reachable bug in this diff.
- `_PAUSE_ALLOWED_TASK_TYPES` (router.py:281) and `_PAUSE_ALLOWED_TOOLS` (router.py:418-435) are two independently maintained allowlists with no shared invariant or test tying them together. Maintainability/DRY concern, no demonstrated bug today.
- `dispatch_tool_call`'s tool-surface filter (router.py:480-508) is computed once at gate-check time with no re-check immediately before the adapter dispatch later in the same function. Same class of TOCTOU concern as the `ask_router` branch above; narrow window, no `await` yielding control between filter and use beyond further gate logic.
- `is_paused_now` / `is_degraded_now` (pause.py:240, budget.py:204) use a broad `except Exception` that would also swallow programming/schema bugs under the same fail-closed path as genuine DB-connectivity failures. This is the documented deliberate fail-closed safety design for a CRITICAL kill-switch; narrowing the catch is a legitimate hardening idea but changes the safety contract's conservatism and needs a human call, not an unambiguous patch.
- `_emit_pause_skip_audit_row` (drainer.py:40-66) and `_emit_pause_refusal_audit_row` (router.py:284-318) are near-duplicate helpers (same shape, same reason code, same zero-cost fields) built independently in two modules. Cosmetic DRY/simplification opportunity, no correctness impact.
- `_tool_name()`'s dict-shape fallback (router.py:438-455) validates only `function.name`, not the rest of the tool schema — a malformed-but-named dict could still pass the allowlist check. The fail-safe direction is already correct (unknown/malformed → filtered OUT); reaching this requires a synthetic malformed-dict-with-valid-control-verb-name shape not evidenced as reachable in this diff.
- Drainer mid-tick loop (drainer.py:582) calls `is_paused_now` once per claimed row with no circuit breaker; a transient DB outage during a large batch would produce one `fetchone` + one audit-row write per remaining row. Pre-existing-class DB-under-load concern at current batch sizes (25), already acknowledged as negligible in the pre-review self-audit (§3, LOW).

## Deferred from: code review of 9-11-anchor-stability-audit-one-shot-cross-evaluator-calibration (2026-06-28)

- CR-F5 (LOW): α=-1.0 sentinel value (set when `krippendorff_alpha_ordinal` raises `ValueError` due to too-few-pairable-observations) is indistinguishable from a legitimate α=-1.0 (perfect systematic disagreement) in the persisted baseline file. No `outcome` or `audit_error` field in `evals/schemas/anchor_baseline.schema.json` to discriminate the two cases. A forensic reader of a `v1-FAILED-CALIBRATION.json` file cannot determine whether α=-1.0 means "evaluators always disagreed completely" or "computation failed for insufficient data". Carry-forward to Epic 10+ schema v2: add an optional `audit_error: string | null` field that the CLI populates when the α falls back to the -1.0 sentinel path. [`benchmark/anchor_stability_audit.py:326-332`, `evals/schemas/anchor_baseline.schema.json`]

## Deferred from: code review of 9-9-report-renderer-pareto-frontier-and-demote-promote-with-confidence-intervals-and-sample-size-gate (2026-06-28)

- CR-F6 (LOW): Wilson CI applied to `f1_macro` which is not a binomial proportion. `f1_macro` is in `_WILSON_METRICS` alongside genuinely proportion-shaped metrics (accuracy, precision_macro, recall_macro). The CI formula synthesizes a fictitious trial count via `round(f1_macro * sample_count)`, producing bounds that look plausible but have no valid frequentist interpretation. Pre-review §3 [S3] accepted with rationale (future-story carry-forward). Add an inline comment at `_WILSON_METRICS` in `benchmark/report.py` distinguishing proportion-shaped members from derived-metric members, and document that CI on derived metrics is approximate. [`benchmark/report.py:73-84`]

## Deferred from: code review of 9-8-e2e-join-5-item-canary-corpus-runner-scorer-report (2026-06-28)

- CR-F5 (LOW): Test 1 asserts `len(scores) > 0` but not the expected score-row count — a scorer that emits a single aggregate row would pass; add assertion for expected count (2 models × N metrics per objective task). Pre-existing weak-assertion pattern from `test_scorer.py`. [`tests/integration/test_benchmark_e2e_canary.py:266`]
- CR-F6 (LOW): Latent collision in DISTINCT concatenation query in Test 2 — `corpus_item_id || ':' || task_type || ':' || model || ':' || prompt_version` assumes no field contains `:`; safe for canary IDs today but fragile for future corpora. Carry-forward to corpus validation tooling or use a parameterized GROUP BY instead. [`tests/integration/test_benchmark_e2e_canary.py:381-389`]
- CR-F7 (LOW): `asyncio.run(read_run_scores(...))` in `render_report` will raise `RuntimeError` if called from a running event loop (e.g., FastAPI route or async CLI in Story 9.9); add an `async def arender_report(...)` variant alongside the sync form per the two-name-pair Python pattern. [`benchmark/report.py:145`]

## Deferred from: code review of 9-7-scorer-objective-and-subjective-with-anchor-calibrated-auto-eval-and-cross-evaluator-agreement (2026-06-28)

- CR-F4 (MEDIUM): Partial anchor calibration (< 20 anchors scored due to sustained dispatch failures) silently computes MAE over a reduced denominator. Add `_logger.warning("anchor_calibration_partial n_scored=%d n_expected=%d", len(auto_scores), len(anchors))` inside `_run_anchor_calibration` before the MAE return when `len(auto_scores) < len(anchors)`. [`benchmark/scoring/subjective.py:247-260`]
- CR-F7 (LOW): `ScoreOutcomeLiteral` in `benchmark/schemas.py` includes `"scorer_error"` but no production code path emits this outcome (no per-pair exception handler writes a scorer_error row). Matches Story 9-6 CR-F2 dormant-ErrorCode pattern. Add a try/except around the per-pair scoring loop in `_run_async` that writes a `scorer_error` row with the exception message in `extra_json`; or document + lint-check alongside Story 9-6 carry-forward. [`benchmark/schemas.py:44`]
- CR-F8 (LOW): AC-12 cache reuse contract ("second run produces FEWER adapter `.call()` invocations than the first") is not directly asserted by `test_scenario_5_unique_constraint_enforcement` (which only validates INSERT OR REPLACE row-count stability). Add a call-count assertion using `_ScriptedSubjectiveAdapter.call_log` comparing run-1 vs run-2 dispatch counts. Story 9-11 real-spend run will exercise cache hit rate organically. [`tests/integration/test_scorer.py:386-421`]

## Deferred from: code review of 9-6-benchmark-runner-and-benchmark-runs-table-and-cost-confirmation-gate-and-cohort-key (2026-06-28)

- CR-F7 (LOW): AC-9 Test 6 (SIGINT simulation) not implemented — pre-review attributes omission to Windows SIGINT-in-asyncio brittleness; the SIGINT handler path in `_run_dispatch_loop` is manually code-reviewable; add a test targeting Linux CI or use `signal.raise_signal(signal.SIGINT)` in a subprocess asserting on row state post-exit. [`tests/integration/test_benchmark_runner.py`]
- Pre-review §3 MEDIUM ACCEPT WITH RATIONALE (Story 9-6): `read_completed_cells` set-membership returns cells with ANY status (`completed` / `aborted_cost_cap` / `interrupted`). An operator who wants to RE-RUN aborted cells after the monthly cap rolls over has no flag — must run `DELETE FROM benchmark_runs WHERE run_id = ? AND status != 'completed'` then `--resume`. Add a `--retry-aborted` flag (or `--include-statuses completed,interrupted` allow-list) to `benchmark.runner` in a future story. [`benchmark/db.py:read_completed_cells` + `benchmark/runner.py:_run_async`]
- CR-F2-related (Story 9-6): `MONTHLY_BUDGET_EXCEEDED` + `BUDGET_EXCEEDED` are defined in `mailbot_api/router/errors.py` but never emitted anywhere in production code (only `DEGRADED_MODE_BLOCKED` fires when the monthly cap is hit). Future tooling story: lint check that every `ErrorCode` enum member is emitted at least once in production code, OR remove the dormant codes. Documented in `benchmark/runner.py:_map_outcome` + `_is_cap_blocking` docstrings as forward-compat retention. [`mailbot_api/router/errors.py:51,54`]

## Deferred from: code review of 9-1-5-f35-watchfiles-thrash-on-runtime-delete-detect-and-stop (2026-06-26)

- CR-F5 (LOW): `test_baseline_edit_after_delete_resumes_policy_reloaded` asserts exactly `len(reloaded_events) == 1` — some CI filesystem backends deliver two watchfiles fires per write, which would cause spurious failure; existing Story 9-1 tests use `>= 1` for this reason. Pre-existing risk profile of real-FS integration tests. [`tests/integration/test_policy_overrides_delete_at_runtime.py:233`]
- CR-F6 (LOW): `test_recreating_override_at_runtime_does_not_auto_pickup` asserts `len(swap_events) == 0` inside a 2-second hold window but does not recheck after `stop_event.set()` — late-arriving events between assertion and teardown are invisible. Not a production defect; test-coverage blind-spot. Pre-existing risk profile of real-FS integration tests. [`tests/integration/test_policy_overrides_delete_at_runtime.py:294-303`]

## Deferred from: code review of 6-12-anthropic-temperature-deprecated-on-opus-4-7-f19-closure (2026-06-05)

- Model gate string literal vs prefix match [mailbot_api/router/models.py:559,660] — `self.model_id != "claude-opus-4-7"` is exact-literal; a future Anthropic date-suffixed variant (e.g., `claude-opus-4-7-20261001`) with the same no-temperature contract would miss the gate. Kept as exact literal for precision (over-broad prefix risks gating future variants that may re-accept temperature). Revisit if Anthropic ships a date-suffixed Opus 4.7 variant.
- `call_with_tools` F19 regression test does not assert `tools` inclusion [tests/unit/router/test_anthropic_adapter.py:275-296] — test verifies temperature absence but not that the `tools` key was correctly populated in the request body. A refactor stripping tools would not be caught by this test. Tools-translation coverage is Story 6-9's responsibility; tracking here for completeness.
- `call` method lacks F14 empty-system guard [mailbot_api/router/models.py:539-545] — `call_with_tools` has `if system and system.strip():` (F14 fix from Story 6-9 CP-2 walk) but `call` unconditionally wraps system in `TextBlockParam` with `cache_control: ephemeral`. Empty-system Opus 4.7 call via `call()` would likely return 400. Pre-existing asymmetry; apply F14's guard to the `call` path in a future story.

## Deferred from: code review of story-1-9 (2026-06-01)

- Consent-flow `?error=...` callback maps to exit code 2 [scripts/mint_refresh_token.py:373-385] — the spec's exit-code table doesn't enumerate this path; exit 2 is a defensible bucket. Could be tightened to a dedicated exit code in a future polish pass.

## Deferred from: code review of 6-2-mailbot-logs-mailbot-pause-mailbot-resume-cli (2026-06-03)

- Per-call `import json as _json` inside `_filter_log_line` body [scripts/mailbot.py:726] — re-imports the module on every line processed; Python caches module imports so this is functionally a dict lookup, not a real module load. Unconventional in a hot-path per-line function but not a defect. Move import to module level in a future cleanup pass.

## Deferred from: code review of 6-11-ingest-pipeline-provider-error-investigation (2026-06-04)

- Regression test calls `init_pipeline_runtime` directly rather than `_worker_main` [tests/integration/test_worker_pipeline_runtime_init.py:135,175] — TRIPLE-source consensus (Blind+Edge+Auditor): a future regression removing the `init_pipeline_runtime(db_path)` line from `mailbot_api/worker.py:283` would NOT be caught because the test calls the helper directly. AC-3 wording technically satisfied ("process_email end-to-end against a real ModelAdapter boundary"). Stronger interpretation: add an `inspect.getsource(_worker_main)` substring assertion or a `mock.patch("…init_pipeline_runtime")` + `_worker_main(db_path)` invocation. Documented as test-strength gap, not a code defect.
- `init_default_adapters()` nested inside `if not skip_policy:` [mailbot_api/ingest/pipeline.py:735] — `MAILBOT_SKIP_POLICY=1, MAILBOT_SKIP_PATTERNS=0` combination silently skips adapter registration. Verified this matches `main.py:162` exactly (faithful mirror of the api lifespan) — pre-existing pattern carried over for symmetry, not introduced by 6-11. Per spec "surgical fix, not a refactor", fixing the lifespan's adapter-skip coupling is out of scope. Either hoist `init_default_adapters()` out of the policy branch in BOTH places, or add a third `MAILBOT_SKIP_ADAPTERS` flag — a separate, project-wide concern.
- Brittle boolean parsing of `MAILBOT_SKIP_*` flags [mailbot_api/ingest/pipeline.py:714-715, mailbot_api/config.py:get_secret_optional] — values `"true"`, `"True"`, `"1 "` (trailing space), `"yes"`, `"on"` all silently fail to skip because the comparison is `== "1"`. Same pattern across all skip flags in the codebase (`MAILBOT_SKIP_DB`, `MAILBOT_SKIP_MCP`, `UVICORN_RELOAD`, etc.) — project-wide pattern, not Story 6-11. Centralize a `_truthy(value)` helper in `config.py` and sweep callers in a follow-up cleanup pass.
- AC-4 backlog drain verified over 2 ticks instead of spec-mandated 6; `backpressure_active → false` flip explicitly deferred [_bmad-output/implementation-artifacts/6-11-…md AC-4 + Task 4] — dev acknowledged in Task 4 sub-bullet ("1614 is still well above the backpressure floor — drain will continue naturally over many hours"). Strictly-decreasing trend IS met across the 2 verified ticks; the floor flip is a time-dependent observation that won't manifest until ~9 hours of drain. Adam-acceptable per dev's own note.


## Deferred from: code review of story-9.5.3 (2026-07-03)

- CR-F8 Fenced raw text stored in audit rows / cache raw [`mailbot_api/router/router.py`] — cache-write path re-serializes via `parsed.model_dump_json()` so cache rows are always clean; audit rows record raw response text by design. No reachable failure mode; consistency-only concern.
- CR-F9 Benchmark carve-out skips escalation gate [`mailbot_api/router/limits.py:80`] — escalation gate belongs to Story 9-6 cost gate which is the intended enforcement point for benchmark spend; rate-limit lane check is orthogonal.
- CR-F13 AC-4 scope re-scoped from `--tasks all --models policy-default` without epics.md amendment [`_bmad-output/implementation-artifacts/9.5.3-walk-evidence.md:50-56`] — walk-evidence's own § "AC-4 scope adjustment" absorbs this for Epic 9.5 retro F-track per walk-evidence note.

## Deferred from: code review of story-9.5.4 (2026-07-04)

- CR2026-07-04-W1 Degenerate calibration context unguarded [`benchmark/scoring/subjective.py:237` + `:111-141`] — `load_anchors` never validates anchor count; a truncated anchors file (n=1) yields an empty leave-one-out rubric block and the blind score silently enters MAE/α. Production guard = new scope; Epic 9.5 retro F-track.
- CR2026-07-04-W2 No provenance marker separating pre-fix (contaminated) from post-fix metrics [`benchmark/anchor_baselines.py:46-71`] — `BaselineSnapshot` carries no calibration-prompt/fix version; `benchmark_scores` rows regime-indistinguishable; drift/MAE comparisons silently mix incomparable regimes (9.5.3's calibration_mae=0.0 case). File alongside F-COHORT-KEY-LEGACY-SCORE-CONFLATION (Story 9-9 follow-up).
- CR2026-07-04-W3 Per-row scoring path still leaks the answer key on corpus-item overlap [`benchmark/scoring/subjective.py:288-337` + `evals/corpus_schema.py:157`] — when a benchmark row shares `corpus_item_id` with an anchor, the evaluator context contains that item WITH Adam's scores; the leak closed for calibration re-opens verbatim per-row. Production fix candidate; Epic 9.5 retro F-track.
- CR2026-07-04-W4 Anchor-set integrity unvalidated where leave-one-out depends on it [`benchmark/scoring/subjective.py:237`, `evals/corpus_schema.py:155`] — no id-uniqueness check (duplicate ids = over-broad exclusion + silent n_anchors_paired shrink); content-duplicate anchors bypass id-based exclusion. Fold into W1 anchor-validation follow-up.
- CR2026-07-04-W5 No test pins the `_audit_all_tasks` × 3-arg `_run_anchor_calibration` seam [`benchmark/anchor_stability_audit.py:297-298`] — the exact call-site the leak fix changed has no dedicated regression test (only subjective.py does). Roll into W1/W2 follow-up.

## Deferred from: code review of story-9.5.4 pass 2, CR-of-CR over 089f601..4177bdd (2026-07-04)

- ~~DISCHARGED 2026-07-04 (same day, honest-re-run session)~~ CR2026-07-04B-W1 Calibration SYSTEM prompt hard-codes "the 20 anchored examples" but leave-one-out ships 19 [`mailbot_api/prompts/anchor_calibrated_eval/v1.py:29-32` + `:10-12`, `benchmark/scoring/subjective.py:145`] — post-leak-fix, every calibration dispatch instructs the evaluator to use "the 20 anchored examples" while the anchors block renders only n−1=19 (`### Anchor 1`–`### Anchor 19`, renumbered); a count-sensitive evaluator can flag the mismatch or treat the item-under-test as the missing 20th. Fix requires editing the byte-stable SYSTEM prompt (cache-invalidating, re-bills). Route to the honest-re-run session — the leak fix already invalidated the response cache, so bundling the SYSTEM wording change there costs nothing extra; also update `build_anchors_block`'s "Render the 20 anchors" docstring in the same pass.

## Deferred from: code review of 10-5-4-operator-recovery-tooling-rederive-replay-move-family-resurrection (2026-07-10)

- Reverter's claim-first placeholder `pre_state='{}'` for legacy no-history rows is asserted future-safe by comment only, not verified codebase-wide [`mailbot_api/actions/reverter.py:270-275`, `ACTION_HISTORY_INSERT_IF_ABSENT`]. The narrow claim (move-family actions never reach this insert because they refuse `PRE_STATE_MISSING` earlier in `revert_action`) was independently verified true by tracing the check order. The broader claim ("no other code path in the codebase ever reads this placeholder expecting a real snapshot") is a standing assumption, not an enforced invariant — a future consumer of `action_history.pre_state` could be surprised by a permanent placeholder row. No fix needed today; no current reader outside the reverter's own move-family branch exists.
- CR-10-2-D1 concurrency regression test only exercises 2 concurrent revert callers; the claim-first serialization is correct by construction for N>2 (SQLite PK uniqueness doesn't degrade past 2 racers) but untested at N=3+ [`tests/unit/actions/test_reverter.py:72`]. Coverage gap, not a bug.

## Deferred from: code review of story 10-2 (2026-07-05)

- ~~CR-10-2-D1 Residual double-revert TOCTOU for legacy rows with NO action_history row~~ **CLOSED 2026-07-10 (Story 10.5.4 Task 4, addressed not re-deferred).** [`mailbot_api/actions/reverter.py`] — the CR-10-2-1 claim-first fix atomically claims `reverted_at` via `ACTION_HISTORY_MARK_REVERTED` rowcount, but rows with an absent history row (Story 4-4 tolerance: history written only during drain, legacy rows may lack one) had nothing to claim on, so two concurrent reverts of such a row could both insert inverse rows. **Fix:** `revert_action` now `INSERT OR IGNORE`s a placeholder `action_history` row (`action_history.action_id` is the PRIMARY KEY → the insert serializes) when `history_row is None`, then runs the same `ACTION_HISTORY_MARK_REVERTED` claim — so the double-revert guard is now uniform for legacy rows too. Only static-map (mark/category) inverses reach this path (move-family with absent history already refuses `PRE_STATE_MISSING`), and the placeholder's `pre_state='{}'` is unused by static-map inverses. Regression test: `test_reverter.py::test_revert_legacy_no_history_concurrent_reverts_queue_exactly_one_inverse` (two concurrent reverts → exactly one inverse queued + one `ALREADY_REVERTED`).
