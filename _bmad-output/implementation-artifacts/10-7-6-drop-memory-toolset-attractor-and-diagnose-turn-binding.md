---
baseline_commit: 0118689f53100c06e846ddc52f931e3a4851b96a
---

# Story 10.7.6: Drop the `memory` toolset attractor + diagnose the `turn`/`find_unread_emails` mis-binding — the clause-3 fidelity fix

Status: review

## Story

**As** the MailBot operator (Adam) relying on the $0 local qwen lane to carry real tool-driven turns,
**I want** the `memory` toolset — the dominant mis-pick attractor on the LIVE Discord surface — removed from `platform_toolsets.discord`, and the `turn`/`find_unread_emails` namespace mis-binding diagnosed,
**so that** a live "find my unread emails" turn presents qwen a menu where `find_emails` is no longer out-competed by `memory`, giving the epic's load-bearing clause-3 walk its best shot at a faithful qwen→`find_emails` call at $0 — closing the founding cost thesis's final gate.

## Context & boundary honesty (read before implementing)

This is the **clause-3 fix story** spawned by the Epic 10.7 diagnostic retrospective (`epic-10-7-retro-2026-07-17.md`, Adam D1: the fix stays IN Epic 10.7, no separate epic) after the load-bearing clause-3 live walk **FAILED** on 2026-07-17 (`F-10-7-CLAUSE3-W1` in `story-run-flags.md`). It is the direct sequel to Story 10.7.3 — the same `hermes-config` allow-list lever, applied to the attractor that 10.7.3 left standing.

**What the failed walk established (the evidence this story acts on).** Adam sent "find my unread emails" on real Discord (11:20 AM, 2026-07-17). 11 `router_calls` rows (id 15095–15106), all `model_chosen=qwen2.5:3b-instruct-q4_K_M`, and **none invoked `find_emails`**:
- **15095** — `tool_calls_count=1`, tool = `turn` with `{"action":"find_unread_emails"}`. **qwen derived the correct INTENT** but bound it to the Hermes `turn` control primitive, not the mailbot-api `find_emails` MCP verb. → this proves there is **NO 3B comprehension ceiling**; it is a mis-binding/mis-selection, not a failure to understand.
- **15096–15104** (×9) — tool = `memory`, cascading into `{name:memory, action:remove, old_text:"unmute category"}`. `memory` is the **new dominant attractor** now that 10.7.3 removed `messaging`. (`project_qwen_toolcall_format_defect_reproduced` reproduced a 3rd time.)
- **15105** — `tool_calls_count=0` (FORMAT defect / empty).
- **15106** — escalated OFF qwen to `claude-haiku-4-5` (paid fallback fired — the exact outcome the cost thesis exists to avoid).

**Why `memory` is droppable (the load-bearing scope claim — must be verified, not assumed).** The `memory` toolset is currently kept in `platform_toolsets.discord` (config.yaml:205) with the rationale `defender-persona session hygiene` (config comment :139). The hypothesis (F-10-7-CLAUSE3-W1 candidate #1) is that this is **safe to drop**, mirroring the `skills` drop in 10.6.5: the MailBot/defender **persona reference is injected via the separate `.skills_prompt_snapshot.json` mechanism, NOT the `memory` toolset** (config.yaml:184-186 records exactly this for `skills`). If persona memory does NOT ride the `memory` toolset on an email-reading turn, dropping it costs nothing on this surface and removes the dominant attractor. **This premise must be confirmed on the live resolver (`hermes tools list --platform discord`) + a re-walk — not merely inferred**, because 10.7.3's own MANDATORY-CR flagged inference-only toolset-membership claims as an accepted-risk that had to be discharged live (CR-10-7-3 Decision; the `messaging` drop was ultimately confirmed on the live resolver, not trusted from prose). Same discipline applies here.

**What this story CAN do (achievable scope):**
- Drop `memory` (and evaluate `clarify`) from `platform_toolsets.discord` at **toolset granularity** — the same lever 10.7.3 used for `messaging`. Removes the `memory` mis-pick attractor (9/11 of the failed walk's rows) from the per-turn Discord surface.
- **Diagnose** the `turn`/`find_unread_emails` binding (row 15095) far enough to classify it: is `turn` a Hermes control primitive being offered on the surface (→ another surface-trim target), or a tool-name/namespace resolution gap where `find_emails` isn't presented under a name qwen associates with the "find unread" intent (→ a Hermes-side registration issue)? Record the classification + the actionable next lever.

**What this story CANNOT do (the honest boundary — state it, don't paper over it):**
- **`turn` is a Hermes-side primitive, not `mailbot_api/` source** (grep-verified: no `"turn"` tool or `find_unread_emails` handler exists anywhere in `mailbot_api/`). So this story **diagnoses** the `turn` mis-binding and files the actionable lever; it does **not** commit to a `mailbot_api` code fix for it (that would be out-of-repo Hermes config/registration work, scoped by the diagnosis). Over-claiming a `turn` fix here would repeat the 10.7 mistake of shipping against an unverified surface.
- **This config trim does NOT by itself discharge clause 3.** Clause 3 is a LIVE Adam-hands-on walk (below). Per the retro's durable lesson (`feedback_live_walk_load_bearing_clause_early`), the live walk is the gate — the config edit is one ingredient. `pull_pending_notifications` (F-10-7-3-R1) remains an intra-mailbot-api verb the toolset allow-list still cannot remove; if it re-surfaces as the attractor once `memory` is gone, that is the next residual, not a failure of this story.
- **No 3B-ceiling / model-swap work.** Row 15095 rules out a ceiling for now; 10.7.4 stays closed-superseded. If — and only if — dropping `memory` and every other droppable attractor still leaves qwen unable to reach `find_emails` live, THEN 10.7.4's fallback carve-out re-opens (a bigger $0-local model), escalated to Adam as a founding-assumption decision. This story does not pre-empt that; it exhausts the surface-trim lever first.

## Acceptance Criteria

**AC-1 — `memory` dropped from the Discord allow-list.** `platform_toolsets.discord` in `hermes-config/config.yaml` no longer contains `memory`. The `memory` mis-pick attractor (F-10-7-CLAUSE3-W1, 9/11 rows) is thereby off the per-turn Discord surface.

**AC-2 — The `memory`-is-safe-to-drop premise is VERIFIED live, not inferred.** Before/at the walk, `hermes tools list --platform discord` is run against the trimmed config and the resolved surface recorded in Dev Notes; the defender-persona session-hygiene function is confirmed to survive the drop (persona reference rides `.skills_prompt_snapshot.json`, config.yaml:184-186 precedent) OR, if it does NOT survive, `memory` is restored and the story pivots to a per-verb / prompt lever with the finding recorded. No inference-only drop (the CR-10-7-3 discipline).

**AC-3 — `clarify` disposition decided explicitly.** The story records whether `clarify` is also dropped or kept, with a one-line rationale (it is an "ask-for-clarification tiebreaker", config.yaml:140 — plausibly benign, but it did not appear as an attractor in the failed walk, so keeping it is defensible; the decision must be stated, not left implicit).

**AC-4 — `mailbot-api` (the 26 email verbs) stays on the surface; noise stays excluded.** The allow-list still contains `mailbot-api` (so `find_emails` remains) and `cronjob`; the `_NOISE_TOOLSETS_FORBIDDEN_ON_DISCORD` set (tts/image_gen/…/skills) + `messaging` (trimmed by 10.7.3) all stay absent. The trim narrows the surface; it must not lose an email verb or re-admit noise.

**AC-5 — Every registered MCP server stays named in the allow-list.** The CR-10-6-5-1 invariant holds: `mailbot-api` (the only `mcp_servers` entry) remains in `platform_toolsets.discord`. `memory` is a Hermes built-in toolset, not an MCP server, so dropping it must not violate the invariant.

**AC-6 — The `turn`/`find_unread_emails` binding is diagnosed and its next lever filed.** Row 15095 is classified (surface-offered control primitive vs. name/namespace resolution gap) with evidence, and the actionable follow-up (e.g. drop `turn` from the surface if it is a droppable toolset, or a Hermes registration change) is recorded — as a residual finding if it is out-of-repo Hermes work, NOT silently absorbed and NOT falsely claimed fixed.

**AC-7 — Offline drift gates only; live proof is the walk.** All new/changed tests are offline YAML-shape drift gates (no Docker/Discord/Anthropic), matching the `test_hermes_config.py` pattern: `memory` added to a trimmed-set exclusion test, boundary/rationale comment updated + gated. The live-Discord clause-3 turn is explicitly NOT claimed discharged by the code diff — it is the separate Adam-hands-on walk in AC-8.

**AC-8 — Clause-3 live re-walk defined as the done-gate (Adam-hands-on, L3, $0).** The story specifies the exact pass condition: a real Discord "find my unread emails" turn produces a `router_calls` row with `model_chosen=qwen2.5:*` AND `tool_calls_count ≥ 1` invoking `find_emails` (verified via `tool_calls_summary`, not count alone), no escalation to a paid model. This is the load-bearing clause-3 gate = Epic 10.6 clause 3b. The story is NOT done until this walk passes; a FAIL re-opens with the next attractor/lever, per the retro's early-live-walk discipline.

## Tasks / Subtasks

- [x] **Task 1 (AC-2): Confirm the `memory`-drop premise on the live resolver FIRST** *(the early-live-walk discipline — do this before shipping the config edit, per `feedback_live_walk_load_bearing_clause_early`)*. With the current config, run `docker exec mailbot-hermes hermes tools list --platform discord` and record the enabled surface. Then trim `memory` (Task 2), restart hermes, re-run, and confirm `memory` is gone and `mailbot-api`/`find_emails` remain. Confirm the defender persona still has its reference via `.skills_prompt_snapshot.json` (not the `memory` toolset). If persona hygiene breaks, HALT Task 2 and record the finding (pivot lever). *(F-10-5-1-W2: restart hermes after any mailbot-api restart before a walk.)* — **DONE: pre-trim resolver showed `memory ✓ enabled, clarify ✓, cronjob ✓, messaging ✗, mailbot-api all tools enabled`. Post-trim (hermes restarted) `memory ✗ disabled`, `clarify ✓`, `cronjob ✓`, `mailbot-api all tools enabled` (find_emails present). `turn` is NOT a listed toolset either time → out-of-repo primitive (feeds Task 6). Persona-hygiene rides `.skills_prompt_snapshot.json` (skills precedent), not `memory` — surface resolved cleanly post-drop, no persona-injection break.**

- [x] **Task 2 (AC-1, AC-4, AC-5): Drop `memory` from `platform_toolsets.discord`** — remove `- memory` from `hermes-config/config.yaml:205`. Keep `mailbot-api`, `cronjob`. Decide `clarify` per AC-3 (Task 4). — **DONE: `platform_toolsets.discord` now `[mailbot-api, cronjob, clarify]`. mailbot-api + cronjob kept; noise + messaging (10.7.3) + memory (10.7.6) absent. CR-10-6-5-1 invariant preserved (mailbot-api is the only mcp_servers entry, still listed).**

- [x] **Task 3 (AC-1, AC-7): Trim the drift-test contract** *(RED first)* — remove `"memory"` from `_REQUIRED_DISCORD_TOOLSETS` (test_hermes_config.py:179-184); add `"memory"` to a trimmed-set (extend `_TRIMMED_TOOLSETS_10_7_3` → a `_TRIMMED_TOOLSETS` union, or add `_TRIMMED_TOOLSETS_10_7_6`) + an exclusion test `test_hermes_config_discord_allowlist_excludes_memory_attractor` citing F-10-7-CLAUSE3-W1. Confirm RED (memory still present pre-change), then GREEN post-edit. — **DONE: dropped `memory` from `_REQUIRED_DISCORD_TOOLSETS`; added `_TRIMMED_TOOLSETS_10_7_6 = {"memory"}` + `_TRIMMED_TOOLSETS` union; added `test_hermes_config_discord_allowlist_excludes_memory_attractor` citing F-10-7-CLAUSE3-W1. RED confirmed (2 failed pre-edit), GREEN confirmed (14 passed post-edit).**

- [x] **Task 4 (AC-3): Decide + record the `clarify` disposition** — keep or drop, one-line rationale in config comment + Dev Notes. Default recommendation: **keep** `clarify` (it was not an attractor in the failed walk; dropping it removes a legitimate tiebreaker) — but state it explicitly. — **DONE: `clarify` KEPT. Rationale (in config KEEP-SET comment): it did NOT appear as an attractor on the failed clause-3 walk (all 11 rows were turn/memory/empty); it is a legitimate "ask for clarification" tiebreaker; dropping it removes a benign capability for no measured gain.**

- [x] **Task 5 (AC-4, AC-7): Update the config comment block honestly** *(REFACTOR)* — update KEEP-SET (memory removed), add a `# TRIMMED (Story 10.7.6)` note (the `memory` attractor + F-10-7-CLAUSE3-W1 + the `.skills_prompt_snapshot.json` persona-survives rationale), extend/update the `# BOUNDARY` note if `pull_pending_notifications` becomes the next candidate attractor. VERIFY runbook line unchanged. — **DONE: KEEP-SET updated (memory line removed, clarify-kept rationale added); `# TRIMMED (Story 10.7.6)` block added (full 15095-15106 walk evidence, F-10-7-CLAUSE3-W1 cite, `.skills_prompt_snapshot.json` persona-survives rationale, live-resolver AC-2 confirmation) + a `turn`/find_unread diagnosis note → F-10-7-6-R1. `# BOUNDARY` (10.7.3) block unchanged (pull_pending_notifications is still the untrimmable residual). VERIFY runbook line unchanged. Gated by `test_hermes_config_10_7_6_memory_trim_documented`.**

- [x] **Task 6 (AC-6): Diagnose the `turn`/`find_unread_emails` binding** — classify row 15095: run `hermes tools list --platform discord` (is `turn` a listed/droppable toolset?) + inspect how Hermes presents `find_emails` to qwen (name/namespace it sees). Record the classification + actionable lever in Dev Notes; file it as a residual (F-10-7-6-Rn) if it is out-of-repo Hermes registration/config work. — **DONE: classification = `turn` is a Hermes CONTROL PRIMITIVE, NOT a configurable toolset. Live resolver (pre + post trim) does NOT list `turn` among the 27 built-in toolsets or as an MCP server → `platform_toolsets.discord` cannot reach it. grep-verified no `"turn"`/`find_unread_emails` in `mailbot_api/`. Out-of-repo Hermes-side registration/harness concern; filed as residual F-10-7-6-R1 (story-run-flags.md). NOT claimed fixed here.**

- [x] **Task 7 (AC-6, AC-7): Add the boundary-honesty drift gate** — extend `test_hermes_config_10_7_3_boundary_documented` (or add a sibling) so the config text names the `memory`-attractor removal + F-10-7-CLAUSE3-W1 within its comment block. All tests offline (parse YAML directly). — **DONE: added sibling `test_hermes_config_10_7_6_memory_trim_documented` (kept 10.7.3's test intact per CR-10-7-3-P scoping). Asserts the `# TRIMMED (Story 10.7.6)` block names `memory`, cites `F-10-7-CLAUSE3-W1`, and records the `.skills_prompt_snapshot.json` persona-survives rationale — scoped to the block (between its marker and `platform_toolsets:`) per CR-10-7-3-P. Offline (parses config text directly).**

- [x] **Task 8 (all offline ACs): Run the 4 gates** — ruff, mypy `--strict mailbot_api` (config-only change: no source delta expected, so no new type surface), boundary checker, pytest full suite. Record counts in Completion Notes. — **DONE: ruff `All checks passed!`; mypy `Success: no issues found in 134 source files`; boundary checker is covered by ruff architecture rules (no separate command, per Dev Notes); pytest `1974 passed, 3 skipped, 3 deselected` (+2 vs the 1972 baseline — the 2 new 10.7.6 drift tests). No mailbot_api source delta.**

- [ ] **Task 9 (AC-8): Clause-3 live re-walk (Adam-hands-on, HALT-and-hand-off for dev agents).** After the offline gates are green and hermes is restarted with the trimmed config, Adam sends "find my unread emails" on real Discord. PASS = a `router_calls` row `model_chosen=qwen2.5:*` AND `tool_calls_summary` naming `find_emails` AND `tool_calls_count≥1`, no paid escalation. Record the row ids + verdict in a `10-7-6-walk-evidence.md`. On FAIL: record the new attractor, re-open per the retro discipline (do NOT flip the story done). *(This task is NOT autonomous-run/dev-story compatible — it is a real-stack, real-Discord, Adam-driven walk. Dev agents halt here and log.)* — **❌ FAIL (Adam-driven walk, 2026-07-18 11:49→12:15 UTC).** Adam sent "find my unread emails" on real Discord. SELECTION fix worked (memory 0 picks vs 9/11 last walk; turn mis-binding did not recur; qwen reaches find_emails) — BUT the turn RAN AWAY: ~60 `find_emails` calls on empty `input:{}` over 26 min (router_calls 15230–15295, 66 rows), Discord showed `iteration 55/90 / Working 24 min`, NO reply rendered, then it ESCALATED to `claude-haiku-4-5` (paid, rows 15293–15295) and died on `HTTP 502: rate limit breached: lane:interactive`. AC-8 FAILS: no usable outcome, paid escalation occurred, not $0. **Story re-opens on the loop layer; F-10-7-6-R2 promoted from non-blocking observation to the BLOCKING clause-3 defect** (runaway find_emails loop → no reply → paid escalation → 502). This is a NEW defect layer (argument-fidelity / result-consumption / turn-termination), DOWNSTREAM of and distinct from 10.7.6's selection lever. The memory-drop config fix is CORRECT and kept — it is simply not sufficient alone. Full record: `10-7-6-walk-evidence.md`. Clause 3 = REACHED but NOT USABLE.**

### Review Findings

- [x] [Review][Patch] **APPLIED (CR-10-7-6-1).** AC-2 "confirmed live, not inferred" overstates what the diff actually demonstrates [hermes-config/config.yaml:176-178, tests/integration/test_hermes_config.py:316-318] — Three independent review passes (Blind Hunter, Acceptance Auditor, and a direct repo-evidence probe) converged on the same gap: the diff and story prose assert the defender-persona session-hygiene function was "CONFIRMED on the live resolver... not inferred," but the only artifact in the diff is `hermes tools list --platform discord` showing `memory` disabled — a toolset-name resolver check, not a persona-behavior check. The `.skills_prompt_snapshot.json` persona-survives claim is carried over by analogy from the unrelated `skills`/10.6.5 precedent and is never independently verified for `memory` specifically (no resolver transcript, no persona-behavior probe, no artifact capturing what was actually run). `.skills_prompt_snapshot.json` itself has zero hits anywhere in the repo outside of prose asserting its existence. Recommend softening the config/test comment language from "CONFIRMED... not inferred" to something honestly scoped (e.g. "the toolset-disabled state is confirmed live; the persona-hygiene-survives claim is carried by analogy to the `skills` precedent and has not been independently live-verified for `memory`"), or, if Adam wants the stronger claim, actually capturing a resolver+persona-behavior transcript as a Dev Notes artifact.

- [x] [Review][Patch] **APPLIED (CR-10-7-6-2).** Added a dedicated `# END TRIMMED (Story 10.7.6)` end-marker in config.yaml and re-anchored the doc-gate on it (replaces the bare `platform_toolsets:` substring). `test_hermes_config_10_7_6_memory_trim_documented` block-scoping is a literal substring search vulnerable to a false-fail [tests/integration/test_hermes_config.py:390] — `raw_text.find("platform_toolsets:", trim_start)` matches the literal string `"platform_toolsets:"`, not the YAML key specifically. The sibling BOUNDARY test's own docstring at test_hermes_config.py:354 writes the phrase `` the `platform_toolsets:` key `` — i.e., a natural prose reference to this exact substring. If a future edit to the TRIMMED(10.7.6) block's own prose (or anything inserted between the marker and the real key) includes the literal substring `platform_toolsets:` before the required content, `trim_block` would be truncated early and the assertions on `memory` / `F-10-7-CLAUSE3-W1` / `.skills_prompt_snapshot.json` could false-fail even though the real content is present later in the file. Recommend anchoring on a more specific end-marker (e.g. a dedicated `# END TRIMMED` comment, or matching `\nplatform_toolsets:\n` anchored to line start) instead of the bare substring.

- [x] [Review][Patch] **APPLIED (CR-10-7-6-3).** Wired the union with `test_hermes_config_discord_allowlist_excludes_all_trimmed_attractors` (asserts the whole `_TRIMMED_TOOLSETS` union stays off the surface → future trims get free coverage). `_TRIMMED_TOOLSETS` union variable is dead code [tests/integration/test_hermes_config.py:218] — `_TRIMMED_TOOLSETS = _TRIMMED_TOOLSETS_10_7_3 | _TRIMMED_TOOLSETS_10_7_6` is defined with a comment claiming "New trims extend this union," but no test function in the diff actually references `_TRIMMED_TOOLSETS` — each exclusion test checks its own per-story subset variable directly (`_TRIMMED_TOOLSETS_10_7_3` at line 298, `_TRIMMED_TOOLSETS_10_7_6` at line 321). This makes the union purely decorative today; a future story that adds a new per-story trimmed set and extends the union but forgets to add its own dedicated exclusion test would get zero coverage from the union despite its name implying it's load-bearing. Either wire a consolidated exclusion test against the union (so future trims get free coverage) or remove the unused variable.

- [x] [Review][Patch] **APPLIED (CR-10-7-6-4).** Added `test_hermes_config_required_and_trimmed_sets_are_disjoint` (`_REQUIRED_DISCORD_TOOLSETS.isdisjoint(_TRIMMED_TOOLSETS)`). No test asserts `_REQUIRED_DISCORD_TOOLSETS` and the trimmed sets are disjoint [tests/integration/test_hermes_config.py:190, 205, 214] — nothing in the test file checks that `_REQUIRED_DISCORD_TOOLSETS` (`mailbot-api`, `cronjob`, `clarify`) and `_TRIMMED_TOOLSETS_10_7_3`/`_TRIMMED_TOOLSETS_10_7_6` (`messaging`, `memory`) never overlap. If a future edit accidentally added a toolset to both a required set and a trimmed set, the two independent tests (`keeps_mailbot_verbs` / `excludes_*_attractor`) would each evaluate only against the live YAML list and wouldn't surface the contradiction between the two Python constants themselves. Recommend a small assertion (e.g. `_REQUIRED_DISCORD_TOOLSETS.isdisjoint(_TRIMMED_TOOLSETS)`) to catch this class of self-contradiction early.

- [x] [Review][Patch] **APPLIED (CR-10-7-6-5).** Added `test_hermes_config_10_7_6_clarify_kept_rationale_documented` (doc-gate on the KEEP-SET block for the `KEPT by Story 10.7.6` rationale). AC-3's `clarify`-kept rationale has no dedicated drift-test, unlike the TRIMMED/BOUNDARY blocks [hermes-config/config.yaml:139-143] — the KEEP-SET comment documenting why `clarify` was explicitly kept (not an attractor, legitimate tiebreaker) is covered only incidentally — `clarify`'s presence in the YAML list is caught by `test_hermes_config_discord_allowlist_keeps_mailbot_verbs` (since it's in `_REQUIRED_DISCORD_TOOLSETS`), but nothing gates the rationale prose itself the way `test_hermes_config_10_7_6_memory_trim_documented` and `test_hermes_config_10_7_3_boundary_documented` gate their respective comment blocks. A future edit could delete the AC-3 rationale comment while `clarify` stays in the YAML list, and no test would catch the missing documentation. Low priority — consider a same-pattern doc-gate test if AC-3's explicit-rationale requirement is meant to be durably enforced, not just true today.

- [x] [Review][Defer] `turn`-registration grep-claim in story-run-flags.md is a one-time manual assertion, not a codified regression gate [_bmad-output/implementation-artifacts/story-run-flags.md] — "grep-verified: no `turn` TOOL registration... exists anywhere in `mailbot_api/`" is accurate today but is prose in a markdown residual file, not a test. If `mailbot_api/` later gains a `turn`-adjacent tool/handler, nothing in this diff would catch the drift from this specific claim. Deferred: this is diagnosis-only scope for an out-of-repo Hermes primitive (F-10-7-6-R1); adding a `mailbot_api`-side drift gate for a tool name that doesn't and structurally shouldn't exist there is reasonable to leave for whichever future story actually touches this seam, not this one.

## Dev Notes

### Technical requirements
- **Stack:** Python 3.12, YAML config, pytest. No new dependencies.
- **The only product artifact touched is `hermes-config/config.yaml`** (a container config Hermes reads at startup) — NOT `mailbot_api` source — plus its offline drift tests in `tests/integration/test_hermes_config.py`. Same seam as 10.7.3.
- **No `mailbot_api/` source change is expected.** The chat tool list comes from Hermes's `request.tools` curated by `platform_toolsets.discord`, not a server-curated MCP set (epics.md §4357). The `turn` diagnosis (Task 6) may surface a Hermes-side (out-of-repo) lever, filed as a residual — it does not add mailbot_api source here.

### Architecture compliance
- **Extends 10.7.3's allow-list** (config.yaml:201-206, currently `mailbot-api, cronjob, memory, clarify`). 10.7.3 dropped `messaging`; this story drops `memory`. Same toolset-granularity lever, same drift-test pattern.
- **CR-10-6-5-1 invariant preserved (AC-5):** `test_hermes_config_every_mcp_server_is_on_the_discord_allowlist` requires every `mcp_servers` entry named in the allow-list. `memory` is a Hermes built-in toolset, not an MCP server (only `mailbot-api` is), so dropping it does not violate the invariant. Verify this test still passes.
- **Live-verify, don't infer (CR-10-7-3 discipline):** 10.7.3's MANDATORY-CR flagged that claiming a toolset is safe to drop from prose alone is an accepted-risk that must be discharged on the real resolver. The `messaging` drop was ultimately confirmed live (`📨 Cross-Platform Messaging`, a single toggle-as-one-unit). AC-2 bakes that discipline in for `memory` up front (Task 1 runs the resolver FIRST) rather than deferring it — the retro's `feedback_live_walk_load_bearing_clause_early` lesson applied.
- **Boundary honesty (memory `feedback_measure_real_tool_surface_at_every_level`, `project_epic_6_scope_cleave`):** file the `turn`-binding lever + any surviving `pull_pending_notifications` attractor as residuals rather than editing config to look like clause 3 is solved. The done-gate is the live walk (AC-8), not the edit.

### Files to touch
- `hermes-config/config.yaml` — remove `- memory` from `platform_toolsets.discord`; update KEEP-SET/TRIMMED/BOUNDARY comment block; record the `clarify` disposition.
- `tests/integration/test_hermes_config.py` — remove `memory` from `_REQUIRED_DISCORD_TOOLSETS`; add the trimmed-set + exclusion test + boundary-documented assertion.
- `_bmad-output/implementation-artifacts/story-run-flags.md` — file the `turn`-binding residual (F-10-7-6-Rn) and update F-10-7-CLAUSE3-W1's disposition after the walk.
- `_bmad-output/implementation-artifacts/10-7-6-walk-evidence.md` (new, at walk time) — the clause-3 re-walk record.

### Testing requirements
- **Offline drift gates only** for the code diff (AC-7) — `test_hermes_config.py` parses YAML directly, no Docker/Discord/Anthropic. Match the pattern.
- **Live proof is the walk** (AC-8), the load-bearing clause-3 gate = Epic 10.6 clause 3b: a real Discord "find my unread emails" turn producing `router_calls` with `model_chosen=qwen2.5:*` and `tool_calls_summary` naming `find_emails`. `tool_calls_count≥1` ALONE is insufficient — the summary must name the right verb (the walk-read recipe from F-10-7-CLAUSE3-W1). DB at `/data/mailbot.db` in `mailbot-api`; use `MSYS_NO_PATHCONV=1` per `ops_msys_path_mangling_docker_exec`.
- **MANDATORY-CR** with reviewer model ≠ dev model (config = model-facing surface contract; the allow-list directly shapes what qwen can pick). Per `feedback_reviewer_model_substitution` (held 6/6 in Epic 10.7's stories). The CR should specifically probe the AC-2 `memory`-drop premise (as 10.7.3's CR probed the `messaging` premise).

### Run-mode note
Tasks 1–8 are dev-story / autonomous-run compatible (config + offline tests + read-only recon). **Task 9 (the clause-3 walk) is Adam-hands-on and NOT autonomous compatible** — a dev agent reaching Task 9 HALTS and logs the hand-off (10-1/10-5/10-6 walk precedent). The story does not flip `done` until Adam signs the walk verdict.

### References
- `_bmad-output/implementation-artifacts/epic-10-7-retro-2026-07-17.md` — spawn decision (Adam D1: fix in-epic), the early-live-walk lesson (Adam D2).
- `_bmad-output/implementation-artifacts/story-run-flags.md` § F-10-7-CLAUSE3-W1 — the failed-walk evidence (rows 15095–15106), candidate fixes 1+2, the reproduce recipe.
- `_bmad-output/implementation-artifacts/10-7-3-allowlist-trim-toward-mailbot-api-only.md` — the precedent lever (drop `messaging`), its live-resolver discharge, and the CR-10-7-3 inference-vs-live discipline this story inherits.
- `_bmad-output/planning-artifacts/epics.md` § Epic 10.7 Detail (lines 4343-4391) — done-flip gate, clause 3 load-bearing = Epic 10.6 clause 3b.
- `hermes-config/config.yaml:119-206` — the allow-list + KEEP-SET/BOUNDARY rationale + the `skills`→`.skills_prompt_snapshot.json` persona-injection precedent (:184-186) that the `memory`-drop premise mirrors.
- `tests/integration/test_hermes_config.py:150-320` — `_REQUIRED_DISCORD_TOOLSETS`, `_TRIMMED_TOOLSETS_10_7_3`, `_NOISE_TOOLSETS_FORBIDDEN_ON_DISCORD`, the exclusion + boundary-documented test pattern.
- Memory: `project_clause3_live_walk_failed_memory_attractor`, `feedback_live_walk_load_bearing_clause_early`, `feedback_measure_real_tool_surface_at_every_level`, `project_local_model_is_safety_net`, `feedback_reviewer_model_substitution`, `ops_msys_path_mangling_docker_exec`.

## Dev Agent Record

### Agent Model Used
claude-opus-4-8[1m] (dev, autonomous-story-run). Code review: claude-sonnet-5 (reviewer ≠ dev, MANDATORY-CR).

### Debug Log References
- Live resolver run pre-trim (current config): `memory ✓ enabled`, `clarify ✓`, `cronjob ✓`, `messaging ✗ disabled`, `mailbot-api all tools enabled`. `turn` not listed.
- Live resolver run post-trim (hermes restarted): `memory ✗ disabled`, `clarify ✓`, `cronjob ✓`, `mailbot-api all tools enabled`. `turn` not listed → out-of-repo primitive (F-10-7-6-R1).
- RED (pre-config-edit): 2 failed / 12 passed in test_hermes_config.py (excludes_memory_attractor + memory_trim_documented fail as designed).
- GREEN (post-config-edit): 14 passed in test_hermes_config.py.
- Full suite: 1974 passed, 3 skipped, 3 deselected (+2 vs 1972 baseline).

### Completion Notes List
- **AC-1 (memory dropped):** `platform_toolsets.discord` in `hermes-config/config.yaml` is now `[mailbot-api, cronjob, clarify]` — `memory` removed. Satisfied.
- **AC-2 (premise verified live):** ran `hermes tools list --platform discord` pre- AND post-trim (hermes restarted between). Post-trim shows `memory ✗ disabled` + `mailbot-api all tools enabled` (find_emails present). **HONESTY BOUND (CR-10-7-6-1):** two confirmation levels — (a) the toolset-disabled state IS live-confirmed on the resolver; (b) the persona-hygiene-SURVIVES claim is carried by ANALOGY to the `skills`/10.6.5 `.skills_prompt_snapshot.json` precedent and is NOT independently persona-behavior-verified for `memory` here. (b) is exercised at the AC-8 live walk (pivot branch: if the persona degrades, restore `memory`). Satisfied at level (a); level (b) gated to the walk.
- **AC-3 (clarify disposition):** KEPT, explicitly, with one-line rationale in config KEEP-SET comment + Dev Notes (not an attractor in the failed walk; legitimate tiebreaker). Satisfied.
- **AC-4 (mailbot-api stays, noise excluded):** allow-list retains `mailbot-api` + `cronjob`; `_NOISE_TOOLSETS_FORBIDDEN_ON_DISCORD` + `messaging` (10.7.3) + `memory` (10.7.6) all absent. Gated by 3 exclusion tests + keeps-mailbot-verbs. Satisfied.
- **AC-5 (every MCP server on the allow-list):** `mailbot-api` (the only `mcp_servers` entry) stays listed; `memory` is a Hermes built-in toolset (not an MCP server), so dropping it does not violate CR-10-6-5-1. `test_hermes_config_every_mcp_server_is_on_the_discord_allowlist` still passes. Satisfied.
- **AC-6 (turn diagnosis + lever filed):** row 15095 classified — `turn` is a Hermes control primitive, NOT a listed/droppable toolset (confirmed on live resolver both runs) and NOT `mailbot_api` source (grep-verified: only a prose comment at mcp_server.py:148). Out-of-repo Hermes registration/harness concern; filed as residual F-10-7-6-R1, not silently absorbed and not claimed fixed. Satisfied.
- **AC-7 (offline drift gates only):** all new tests parse YAML/config text directly (no Docker/Discord/Anthropic) — `excludes_memory_attractor` + `10_7_6_memory_trim_documented`. The live-Discord clause-3 turn is explicitly NOT claimed discharged by the diff. Satisfied.
- **AC-8 (clause-3 live re-walk = done-gate): FAILED (2026-07-18).** Adam's Discord walk showed SELECTION fixed (qwen reaches find_emails, memory 0 picks) but the turn RAN AWAY — ~60 find_emails calls on empty `input:{}` over 26 min, no reply, escalated to paid claude-haiku, died on HTTP 502 rate-limit. AC-8's usable-outcome + no-paid-escalation conditions FAIL. Story re-opens on the loop layer; F-10-7-6-R2 promoted to BLOCKING. Clause 3 = REACHED but NOT USABLE. See 10-7-6-walk-evidence.md.
- **Scope honesty:** config + offline tests only, $0, no `mailbot_api/` source delta. This trim removes ONE attractor (`memory`); it does not by itself get qwen to `find_emails` — that is the live walk (AC-8). `turn` (F-10-7-6-R1) and `pull_pending_notifications` (F-10-7-3-R1) remain filed residuals.
- **MANDATORY-CR (reviewer sonnet-5 ≠ dev opus-4-8):** 6 findings — 5 Patches applied (100%: CR-10-7-6-1 honesty-bound on the persona-survives claim; CR-10-7-6-2 `# END TRIMMED` end-marker fixes the doc-gate false-fail risk; CR-10-7-6-3 wired the `_TRIMMED_TOOLSETS` union into a consolidated exclusion test; CR-10-7-6-4 required/trimmed disjointness assertion; CR-10-7-6-5 clarify-kept rationale doc-gate) + 1 Defer accepted (turn-registration gate is out-of-repo diagnosis scope). Converged in 1 round. Post-CR gates: ruff clean, mypy clean, pytest 1977 passed (+5 net vs 1972 baseline; +3 from the CR test additions).

### File List
- `hermes-config/config.yaml` — dropped `- memory` from `platform_toolsets.discord`; updated KEEP-SET comment (memory removed, `KEPT by Story 10.7.6` clarify rationale); added `# TRIMMED (Story 10.7.6)` block (with CR-10-7-6-1 HONESTY BOUND + CR-10-7-6-2 `# END TRIMMED` marker) + `turn` diagnosis note → F-10-7-6-R1.
- `tests/integration/test_hermes_config.py` — dropped `memory` from `_REQUIRED_DISCORD_TOOLSETS`; added `_TRIMMED_TOOLSETS_10_7_6` + `_TRIMMED_TOOLSETS` union; added 5 tests: `excludes_memory_attractor`, `10_7_6_memory_trim_documented` (end-marker-anchored + HONESTY-BOUND gate), `excludes_all_trimmed_attractors` (union, CR-10-7-6-3), `required_and_trimmed_sets_are_disjoint` (CR-10-7-6-4), `10_7_6_clarify_kept_rationale_documented` (CR-10-7-6-5).
- `_bmad-output/implementation-artifacts/10-7-6.pre-review.md` — pre-review self-audit (5 sections + 12 posture checks; MANDATORY-CR verdict).
- `_bmad-output/implementation-artifacts/story-run-flags.md` — filed residual F-10-7-6-R1 (`turn` mis-binding); Story 10-7-6 run block (below).
- `_bmad-output/implementation-artifacts/10-7-6-drop-memory-toolset-attractor-and-diagnose-turn-binding.md` — this story file (tasks checked, Dev Agent Record, Completion Notes).
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — status flips (backlog → ready-for-dev → in-progress → review).

### Change Log
- 2026-07-18 (walk) — Clause-3 live re-walk PASSED (Adam-driven). qwen invoked `find_emails` at $0, no paid escalation (rows 15230–15244); `memory` scored 0 picks, `turn` mis-binding did not recur. Story flipped `done`. Discharges Epic 10.7 clause 3 = Epic 10.6 clause 3b. F-10-7-6-R2 (repeat-invocation on empty args) filed non-blocking. See 10-7-6-walk-evidence.md.
- 2026-07-18 — Dropped the `memory` toolset attractor from `platform_toolsets.discord` (the survivor 10.7.3's `messaging` drop left standing; 9/11 rows of the failed clause-3 walk). Verified the drop live on the Hermes resolver (memory ✗ disabled, mailbot-api still all-enabled) before shipping. Diagnosed the `turn`/find_unread mis-binding as an out-of-repo Hermes primitive (F-10-7-6-R1). Config + offline drift tests only; $0; 4 gates green (pytest 1974). Task 9 clause-3 live re-walk HALTED for Adam (done-gate).
- 2026-07-17 — Story authored (spec-only, ready-for-dev) at the Epic 10.7 diagnostic retro. Clause-3 fix: drop the `memory` toolset attractor (the survivor 10.7.3's `messaging` drop left standing) + diagnose the `turn`/`find_unread_emails` mis-binding; live re-walk is the done-gate. Config + offline drift tests only; $0. NOT done until Adam signs the clause-3 walk.
