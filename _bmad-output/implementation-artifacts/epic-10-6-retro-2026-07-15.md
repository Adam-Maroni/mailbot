# Epic 10.6 Retrospective — Capability Reachability (FULL, done-flip)

**Date:** 2026-07-15
**Facilitator:** Amelia (Developer)
**Project Lead:** Adam
**Participants:** Amelia (Developer), Alice (Product Owner), Charlie (Senior Dev), Dana (QA)
**Format:** Tight, decision-focused retro (Adam-chosen). Supersedes the partial retro of 2026-07-13 (2/4 stories).

> ✅ **FULL RETRO — Epic 10.6 DONE-FLIPPED by this session.** All 6 stories done (roster grew 4→6 via two walk-spawned stories). This retro discharges the done-flip gate and closes the epic. The partial retro (`epic-10-6-retro-2026-07-13-PARTIAL.md`) reviewed stories 10-6-0 and 10-6-1 only and set the finish path; this document reviews the full epic and reflects Adam's two closing decisions (D1: flip now; D2: qwen-management epic before Epic 7).

---

## 1. Scope

- **Epic theme:** *"wired + capable + tested ≠ reached on the real user path"* — every story closes with a DB-ground-truth or live-walk proof, not an isolated unit test.
- **Stories (all done):**
  - **10-6-0** — Graph-auth-at-drain infra fix (restore stale-token 401 recovery)
  - **10-6-1** — AI-1 Phase 2: route chat tool-calls to local qwen (policy + persona dispatch)
  - **10-6-2** — AI-2: Opus draft-pipeline persona-reach (F-10-5-11)
  - **10-6-3** — AI-4: `scratch/` ruff cleanup (4th-carry chore, retired)
  - **10-6-4** — Cheap-lane latency: usable tool-call turn (walk-spawned, closes F-10-6-1-W1)
  - **10-6-5** — Hermes per-turn tool-surface fidelity (walk-spawned)

---

## 2. Delivery metrics

- **Stories done:** 6 / 6 (100%). Planned roster was 4; two stories (10-6-4, 10-6-5) walk-spawned via the N.5 policy.
- **Test suite:** **1885 → 1941** (+56 net across the epic). Per-story chain: 10-6-0→1889, 10-6-1→1905, 10-6-2→1911, 10-6-3→1913, 10-6-4→1937, 10-6-5→1941. 4 gates green at every close.
- **MANDATORY-CR:** ran on every substantive story; **reviewer `claude-sonnet-5` ≠ dev `claude-opus-4-8` on all 6** (substitution rule from the Epic 10.5 retro / durable memory — held again).
- **Real Anthropic spend:** ~**$0.012** total (a single 2-Opus-call `draft_reply` walk in 10-6-2; local estimator). Everything else is Graph-only, local-qwen, or config chores.
- **Commit trail:** `bc180e6` (10-6-0) → `18ea6d4` (10-6-1) → `c9f8f14` (10-6-2) → `1d56e2a` (10-6-3) → `71b154e` (10-6-4) → `2366904` (10-6-5).

**Founding arc:** top of Epic 10.5 = **18/18 tool-call failures** (local lane couldn't tool-call at all) → end of Epic 10.6 = a real Discord turn **routed to and served by local qwen at $0**, completing within budget.

---

## 3. Done-flip gate — clause-by-clause (VERDICT: FLIP, per Adam D1)

The original gate had 4 clauses; clause 3 was split into 3a (latency) / 3b (tool-fidelity) by Adam on 2026-07-14.

| # | Clause | Status | Evidence |
|---|--------|--------|----------|
| 1 | Stories 10.6.0–10.6.5 = done | ✅ | 6/6 done in sprint-status; all committed |
| 2 | Graph-auth drain works (10.6.0) | ✅ | Live: induced 401 → on-demand refresh → Graph 200, `mark_read` applied, real mailbox changed & restored |
| 3a | Cheap lane **usable** — latency (10.6.4) | ✅ | `2026-07-13 outcome=failed` (>30s) → `2026-07-14 outcome=ok` on qwen; same turn **25.3s → 3.4s** via `keep_alive:-1` |
| 3b | Cheap lane **reaches tools** — surface (10.6.5) | ✅ *scope* / ⬜ *fidelity → qwen epic* | Two pollution channels closed (built-ins + `skills`); `find_emails` proven on qwen's function list (ListTools). Actual invocation (`tool_calls_count ≥ 1`) blocked on **model fidelity**, reassigned per D1 |
| 4 | Draft pipeline REACHED (10.6.2) | ✅ | Real turn: 2× Opus 200 OK, `router_calls` id 14861 `model_chosen=claude-opus-4-7 task=draft_reply`; sensitivity gate preserved (confidential → refused, 0 Opus calls) |

**Adam D1:** Clause 3b's *fidelity* residual (qwen actually invoking a tool) is a **model problem, not an epic-scope defect** — 10-6-5 delivered its scope (email verbs reach the turn, ListTools-proven). The residual moves to the qwen-management epic. Epic 10.6 done-flips.

---

## 4. What went well

1. **The epic proved its own thesis against itself, repeatedly.** 10-6-1 hit "wired ≠ reached" three layers deep in one story: adapter `tools_unsupported` (a stale Story-6-9 write-off), router capability gate hard-coded `^claude-*`, and policy sourcing the default model from the `hermes_aux`=haiku entry (no `chat_completions_tool_call` task existed). Each layer looked done+tested; none reached qwen until the one above was fixed.
2. **Root causes were measured, not guessed.** 10-6-4: the ">30s timeout" was **cold prompt ingest from model eviction** (no `keep_alive`), NOT `num_ctx` (measured red herring). Live proof *before* code: `keep_alive:-1` → same turn 25.3s → 3.4s (ingest 18598ms → 84ms).
3. **Adversarial CR did real work, reviewer ≠ dev 6/6.** 10-6-0: caught a final-iteration silent-wrong-error (forced `for`→`while`+counter restructure) and a 401-refresh stealing a backoff slot. 10-6-4: F1 nomic **dead config** ("symmetry" claim was false — `embed()` never consumed the wired `keep_alive`) + F2 timeout-parse crashes boot on malformed env. Load-bearing correctness, not nits.
4. **Boundary-honesty held under autonomous pressure.** 10-6-0 scope-fenced off `oauth.py`/`graph_client.py` and FILED the true-token-refresh residual instead of faking a full fix (CR: ACCEPT-WITH-RATIONALE). 10-6-2 and 10-6-5 touched **zero `mailbot_api` source** — the fixes were genuinely persona/config-side, located there instead of editing product code to look busy.
5. **The 4th-carry chore finally died.** 10-6-3 retired the `scratch/` ruff debt (A6 → Epic 9.5 → Epic 10 → here) **permanently** via `extend-exclude` (+ mypy-exclude symmetry from CR), not site-by-site. 6 T201 sites; won't carry a 5th time.

---

## 5. What didn't (challenges / growth areas)

1. **The fidelity tail keeps regenerating one level down.** 10-6-1 fixed routing → exposed latency (10-6-4). 10-6-4 fixed latency → exposed tool-surface pollution (10-6-5). 10-6-5 fixed the surface → exposed **model tool-fidelity** (qwen mis-selects `send_message` over `find_emails`; emits `<tool_call>{…}</tool_call>` as *text* the harness can't dispatch). Real progress, but a chain that only stopped when Adam called the qwen epic.
2. **Two walk-spawned stories = the plan didn't see the tail.** Scoped at 4, delivered 6. N.5 policy working as designed — but "wired ≠ reached" had more layers than even the epic *named after that lesson* anticipated.
3. **L3 sign-off still partially owed to Adam.** 10-6-1 confidential-on-local privacy disposition and 10-6-2 AC-5 Opus-draft-*quality* judgment are both Adam-only Phase 3.5. Real Opus money proved the *pipeline* (~$0.012); the *quality* call hasn't been made.

---

## 6. Previous-retro (partial 2026-07-13) follow-through

| Commitment | Status in this retro |
|---|---|
| **D2:** finish 10-6-2 → 10-6-3 → close clauses → done-flip | ✅ Done (plus 10-6-4 + 10-6-5 also landed) |
| **D1:** clause 3 "REACHED ≠ usable" — F-10-6-1-W1 as a blocker | ✅ Discharged — 10-6-4 closed F-10-6-1-W1; latency usable |
| **10-6-2** twice-deferred (also missed in 10.5 retro) | ✅ Finally delivered — pattern broken |
| **10-6-3** 4th-carry risk | ✅ Retired permanently |
| Reviewer≠dev substitution (durable memory) | ✅ Held 6/6 again |

Every partial-retro commitment closed. Clean follow-through.

---

## 7. Adam decisions this retro

| ID | Decision | Consequence |
|----|----------|-------------|
| **D1** | **Done-flip Epic 10.6 now.** Clause 3b's fidelity residual (qwen invoking a real tool) is a model problem → owned by the qwen-management epic, not an Epic 10.6 defect. | `epic-10-6` → `done`; `epic-10-6-retrospective` → `done`. Done-flip gate discharged; 3b-fidelity reassigned. |
| **D2** | **Spawn the qwen-management epic and sequence it BEFORE Epic 7.** | Next epic is qwen fidelity (tool-selection + call-format, F-10-6-5-W1), not Production Calibration. Calibrating routing (Epic 7) while the cheap lane can't reliably invoke tools would calibrate an unusable product. |

---

## 8. Next epic — qwen-management (before Epic 7)

**Charter (from the two open fidelity defects, F-10-6-5-W1):**
- **Tool SELECTION:** qwen picks plausible-but-wrong tools (`send_message` over `find_emails`) even when the right tool is provably on its function list (ListTools-confirmed).
- **Tool-call FORMAT:** qwen emits `<tool_call>{…}</tool_call>` as literal text instead of a structured function call → `tool_calls_count=0`, harness can't dispatch.
- **Sibling class** to 10-6-1 (argument fidelity) and 10-6-4 (latency): all model/harness fidelity on the same local lane.
- **Charlie's flag:** may bump into Qwen 2.5 3B's raw capability ceiling. Scope a **characterization spike first** (prompt-template/harness fix vs. model-too-small) before committing to a fix approach — mirroring 10-6-4's measure-before-fix discipline.
- Untested candidate levers: trim allow-list toward `mailbot-api`-only; note the format bug is model/harness, independent of surface.

**Epic 7 (Production Calibration)** stays sequenced *after* the qwen epic. CP-1 (VPS deploy) remains the final ship gate outside all epics; local viability is top priority.

---

## 9. Residuals carried out of Epic 10.6 (all filed, non-blocking)

- **F-10-6-5-W1** — qwen tool-selection/format fidelity → **qwen-management epic (the charter)**.
- **10-6-0 true-token-endpoint refresh** — DB-re-read no-op in the deep stale-cache race, bounded + audited; needs a follow-up touching scope-fenced `oauth.py`/`graph_client.py`.
- **WALK-10-6-2-F1** — empty `draft_body` on a no-reply marketing email (pre-existing Opus pipeline, Story 5-9/10.5.3, INFO). Candidate draft-pipeline-UX story.
- **Deferred Hermes-runtime levers** (10-6-4): B1 tool-trim, B3 retry-tame (~3× re-pay) — Hermes-agent-runtime-owned, not `config.yaml` knobs.
- **Adam Phase-3.5 sign-offs owed:** 10-6-1 confidential-on-local privacy disposition; 10-6-2 AC-5 draft quality.

---

## 10. Readiness assessment — Epic 10.6 DONE

| Dimension | Status |
|-----------|--------|
| Stories | ✅ 6/6 done, all committed |
| Tests / quality | ✅ 1941 + 3 skip + 3 desel; 4 gates green; MANDATORY-CR reviewer≠dev 6/6 |
| Live validation (L3) | ✅ 10-6-0 (real Graph), 10-6-2 (real Opus draft), 10-6-4 (real qwen latency), 10-6-5 (real Discord walks, DB-verified) |
| Done-flip gate | ✅ discharged; clause 3b fidelity reassigned to qwen epic (D1) |
| Deployment | N/A — local stack only; CP-1 outside all epics |
| Stakeholder (Adam) acceptance | ⚠️ 2 Phase-3.5 sign-offs owed (privacy disposition, draft quality) — non-blocking for done-flip |
| Unresolved carry-forward | F-10-6-5-W1 (→ qwen epic), 10-6-0 refresh residual, WALK-10-6-2-F1, 2 deferred Hermes levers |

**Verdict:** Epic 10.6 is **complete and done-flipped.** The cost thesis went from 18/18 tool-call failures to a live qwen turn at $0. The three highest-value capabilities — Graph-auth drain, cheap-lane routing+latency, Opus draft pipeline — are all reached and L3-verified. The remaining open question (does qwen reliably *invoke* the right tool?) is a model-fidelity problem owned by the next epic, not an Epic 10.6 defect.

---

## 11. Key takeaways

1. **"Reached → usable → faithful" is a three-rung ladder, and we climbed all three in one epic.** Partial retro added "reached ≠ usable" (latency). This retro adds the third rung: **usable ≠ faithful** (the model must pick the right tool AND format the call). Each rung was a separate story.
2. **The cost thesis is demonstrably alive on the real path** — 18/18 failures → a live qwen turn at $0 within budget. The last open question (reliable tool invocation) is now a *named epic*, not a vague worry.
3. **Measure the root cause before fixing it.** 10-6-4's `num_ctx` red herring vs. the real `keep_alive`/eviction cause is the model for the qwen epic: characterize before committing to an approach.
4. **Reviewer≠dev + boundary-honesty are durable** — 6/6 this epic, catching load-bearing bugs (nomic dead-config, boot-crash timeout parse, silent-wrong-error) the dev over-claimed as done.
