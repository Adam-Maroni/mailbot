# Pre-Review Self-Audit — 10-6-1 (AI-1 Phase 2, cheap-lane reachability)

**Generated:** 2026-07-13 by claude-opus-4-8[1m]
**Story file:** ai-1-local-tool-caller-and-chat-path-reachability.md
**Status at audit time:** review (post dev-story, pre code-review)

## 1. AC-vs-code drift scan

- **AC-1..AC-4 (Phase 1):** N/A — DONE + committed in Phase 1 (3c204/5880d1e); not touched this pass.
- **AC-5** (default chat tool-call ROUTED to qwen, proven by a `router_calls` row `task_type=chat_completions_tool_call, model_chosen=qwen2.5:*`): **MATCH (dev-layer)** — `test_dispatch_tool_call_default_routes_to_local.py` + `test_main_tool_call_alias_resolves_local.py` assert exactly that row via the real dispatch path. The "from a real Discord turn" clause is discharged at the Phase 3.5 walk (it is AC-6's precondition), not at dev-layer — this is a live-validation (L3) item, correctly deferred, not drifted.
- **AC-6** (live Discord L3 walk: reversible served-by-qwen executes w/o prompt; irreversible prompts): **N/A at dev-layer — Adam-hands-on Phase 3.5.** The story text marks it OPEN/live; the manual-verification prompt at run-end carries it. No code claims to satisfy AC-6.

No AC text required editing (no drift found in AC statements vs. shipped code).

## 2. File-List-vs-git diff check

`git status --porcelain` (relevant paths):

```
 M mailbot_api/main.py                                   TRACKED (modified)
 M mailbot_api/router/models.py                          TRACKED (modified)
 M mailbot_api/router/router.py                          TRACKED (modified)
 M router/policy.yaml                                    TRACKED (modified)
 M scripts/register_discord_commands.py                  TRACKED (modified)
 M tests/integration/test_chat_completions_tool_calling.py            TRACKED (modified)
 M tests/integration/test_dispatch_tool_call_override_bridges.py      TRACKED (modified)
 M tests/integration/test_sensitivity_refusal_envelope_boundary.py    TRACKED (modified)
 M tests/unit/router/test_ollama_adapter.py             TRACKED (modified)
 M _bmad-output/.../ai-1-...reachability.md             TRACKED (story file, modified)
?? tests/integration/test_dispatch_tool_call_default_routes_to_local.py   UNTRACKED — new (staged in 2.6)
?? tests/integration/test_main_tool_call_alias_resolves_local.py          UNTRACKED — new (staged in 2.6)
```

Every File-List path is present in git status. The two `??` files are intentional NEW test files (listed in the File List) — they are staged in Step 2.6, not a File-List omission. No File-List path is missing from git. **No untracked File-List-claimed-tracked file → no §2.4.6 HALT.**

Out-of-scope untracked (NOT staged): `scratch/` (story 10-6-3), `.autonomous-run-active.json` (run artifact), `.claude/settings.json` (pre-existing session `M`, not mine).

## 3. Adversarial self-review

- [MEDIUM] `router/policy.yaml` / `dispatch_tool_call` — the dispatcher still hard-codes `hermes_aux` as the LANE proxy (`policy.tasks.get("hermes_aux")` at router.py:1868). If a deployment removes the `hermes_aux` entry (now that its model role is superseded) the dispatcher returns PROVIDER_ERROR. Mitigation: `hermes_aux` is retained + its note documents the dual role; not removed. Acceptable — flagged for reviewer awareness.
- [MEDIUM] Privacy: confidential tool-calls are no longer API-blocked on the default (local) path. Correct per NFR-PRIV-2 (blocks EXTERNAL APIs, not local inference) but a real behavior change. FIX: surfaced explicitly in the story + covered both directions by tests. Disposition escalated to reviewer/Adam (§4).
- [MEDIUM] Ollama arg translation: a malformed (non-JSON) `arguments` string is left as-is rather than raising. This is deliberate (translator must not raise mid-history) but means a model-emitted malformed arg surfaces later at the ollama layer. Covered by `test_...malformed_args_preserved`. Accept-with-rationale.
- [LOW] `chat_completions_tool_call` excluded from `/model persist` Discord choices (silent-no-op guard, same as `embedding`). A user wanting to pin the tool-call model uses `task=hermes_aux` (the key the persistent peek honors). This is a slightly non-obvious UX; documented in the exclusion comment.
- [LOW] The dispatcher's model is resolved UPSTREAM by main.py; `dispatch_tool_call` in the no-override branch does not re-resolve the model from the new policy entry (it only sets the reason). Direct callers of `dispatch_tool_call` (tests, future internal callers) must pass the already-resolved model. Documented in the no-override test's docstring.
- [LOW] Degraded-mode demotion + the new default both target qwen — a demotion is now a no-op on the default path (qwen is the floor). Harmless; noted in the policy entry.

## 4. Self-caught issues remediated this audit

- MEDIUM (hermes_aux lane-proxy fragility): **ACCEPT WITH RATIONALE** — entry retained + documented; removal is a separate deployment concern.
- MEDIUM (confidential local disposition): **ESCALATE TO REVIEWER + Adam** — behavior change is correct-by-privacy-model but load-bearing; both directions tested; explicit sign-off wanted.
- MEDIUM (malformed-arg pass-through): **ACCEPT WITH RATIONALE** — deliberate non-raising contract, tested.
- LOW (persist-picker exclusion): **FIX NOW (done)** — added to `EXCLUDED_FROM_PERSIST_CHOICES` with rationale; keeps frozen Discord fixture valid.
- LOW (no-override no-re-resolve): **ACCEPT** — documented in test docstring; matches pre-existing dispatcher contract.
- LOW (degraded no-op): **ACCEPT** — harmless, documented.

## 5. Posture Audit

### 5.1 — Lockfile hygiene
Python pip+venv, hand-pinned requirements.txt (AR-BOOT-1). No dep change this story.
```
$ git diff --stat -- requirements.txt
(no output)
```
**PASS.**

### 5.2 — Cross-doc pair verification
Cross-doc branch: the story references `hermes-config/config.yaml` + `hermes-config/AGENTS.md` (Task 5 persona-delegation claim).
```
Claim: "persona sends model: hermes_aux + delegates model choice to the Router" (story Task 5)
Verification:
  $ grep -n 'default:\|model: "hermes_aux"' hermes-config/config.yaml   → line 24 default: "hermes_aux"; lines 51/56 model: "hermes_aux"
  $ grep AGENTS.md Rule N   → line 49-51 "for most tasks you do not name a model at all ... Router dispatches per policy.yaml"
Verdict: MATCH — persona delegates; gap was 100% server-side.
```
Schema-touching branch (§5.2.1): **N/A** — no migrations directory path in File List (no SQL migration this story).
**PASS.**

### 5.3 — Lifecycle string-uniqueness check
New audit reason string `policy:chat_completions_tool_call:default` is produced by `policy_default(_TOOL_CALL_TASK_TYPE)` (single producer) and matches the producer-boundary regex:
```
$ python -c "from mailbot_api.router.audit_vocab import POLICY_DEFAULT_RE, policy_default; s=policy_default('chat_completions_tool_call'); print(s, POLICY_DEFAULT_RE.match(s) is not None)"
policy:chat_completions_tool_call:default True
```
No new hand-written lifecycle string literals introduced. **PASS.**

### 5.4 — Multi-consumer impact scan
`policy_default(...)` consumers: `router.py:534` (ask_router — unchanged, uses `task_type`) + the new `dispatch_tool_call` site. `hermes_aux` model access: ONLY `dispatch_tool_call` (lane proxy) + `main.py` (alias resolution, now → chat_completions_tool_call). Both consumers audited; no third consumer of the tool-call default model exists.
```
$ grep -rn "tasks.get(.hermes_aux\|tasks\[.hermes_aux" mailbot_api/   → dispatch_tool_call (lane) + main.py (alias) only
```
**PASS.**

### 5.5 — Screenshot-based perception check
**N/A** — no graphical frontend (PORTING.md `<frontend-src>` N/A); no AC asserts a human-visible pixel-level surface. AC-6's "prompts for confirmation" is Discord text, verified at the Phase 3.5 walk.

### 5.6 — Upstream-contract spec coverage check
The OpenAI↔Ollama tool_calls translation is the upstream contract. New coverage: assistant `tool_calls[].function.arguments` string→dict (happy + malformed). The fix is validated against the REAL `ollama.Message.model_validate` (not just the fake client) in `test_...string_to_dict` — pins the actual upstream schema. **PASS.**

### 5.7 — Module-level mutable container check
No new module-level mutable containers introduced. `_PAUSE_ALLOWED_TOOLS` / `EXCLUDED_FROM_PERSIST_CHOICES` are frozensets (immutable); the latter gained one member (a literal edit, not runtime mutation). **PASS.**

### 5.8 — Dev-fixture seed-vs-production-shape parity check
Test fixtures add a `chat_completions_tool_call` policy task mirroring the real `router/policy.yaml` shape (same keys: model/prompt_version/escalate/max_tokens_out/lane/sensitivity). Production policy.yaml is the source; fixtures match it. **PASS.**

### 5.9 — grep-verify-cited-figures
Story cites "+9 net tests" and "1898 passed". Verified: full suite `1898 passed, 3 skipped, 3 deselected` (run 2026-07-13); 10-6-0 baseline was 1889 passing → +9. Figures grep-verified against the actual run output. **PASS.**

### 5.10 — Producer-boundary contract enforcement
The new default reason is emitted only via `policy_default()` (the single producer helper), never hand-written. Confirmed it passes `POLICY_DEFAULT_RE` (§5.3). The `_API_BOUND_MODEL_RE` gate is unchanged (qwen correctly non-matching → local privacy exemption). **PASS.**

### 5.11 — Git-evidence consistency check
```
$ git diff --stat -- mailbot_api/ router/policy.yaml scripts/register_discord_commands.py
 mailbot_api/main.py                  | 22 +++++---
 mailbot_api/router/models.py         | 72 ++++++++++++++++++++++--
 mailbot_api/router/router.py         | 10 ++++-
 router/policy.yaml                   | 11 +++-
 scripts/register_discord_commands.py | 15 +++++-
 5 files changed, 118 insertions(+), 12 deletions(-)
```
Diff size proportional to the described change (small routing seam + one translator helper + one exclusion). No mass reformat. Story File List matches the changed set (+ 2 new test files + updated tests). **PASS.**

### 5.12 — CR-cadence-mandatory surface classification
**MANDATORY-CR REQUIRED (load-bearing).** This story touches the cost-routing seam (`dispatch_tool_call` model selection + `main.py` alias) AND a privacy-adjacent behavior (confidential-on-local). Both are in the "load-bearing seam" class the story pre-declared for MANDATORY-CR with reviewer ≠ dev ([[feedback_reviewer_model_substitution]]). Reviewer model = sonnet-5 (dev = opus-4-8). Not gate-coverage-only.

Note on the 3rd-carry `scratch/` ruff T201 (6 sites): OUT OF SCOPE for 10-6-1 — owned by story 10-6-3. Repo-wide `ruff check .` is not green ONLY due to `scratch/`; all changed source + test files are ruff-clean.
