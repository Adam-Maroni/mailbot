# Pre-Review Self-Audit — 10-5-6

**Generated:** 2026-07-11 by claude-opus-4-8 (dev model)
**Story file:** _bmad-output/implementation-artifacts/10-5-6-slash-to-plain-nl-charter-readme-rewrite.md
**Status at audit time:** review (post dev-story Tasks 1-4, pre code-review)

## 1. AC-vs-code drift scan

- **AC-1 (README charter rewrite): MATCH.** `## Slash commands` heading removed; replaced by `## Talking to MailBot` two-tier section (read/status free-NL + control-verb recognized-phrase). Write examples (§90-96 `/cancel`→`cancel`), Tier-3 delete example (`/confirm`→`confirm`), error-table cells (loop_detected §316, paused §317, degraded §313, sensitivity §307), §349 `/model` override note, §398 limitation bullet, §401 sensitive-escalation bullet, and the §19 intro all rewritten to plain NL. Interim "WITHOUT the leading `/`" note removed. Enforced by `test_recognized_phrase_dispatch.py::test_readme_has_no_slash_command_invocation_section` + `::test_readme_documents_plain_nl_intents_section` + the cross-file sweep.
- **AC-2 (recognized-phrase dispatch contract): MATCH.** SKILL.md `## Slash-command verbs` → `## Control-verb dispatch (deterministic recognized phrases)` with the exact-match phrase→verb table (cancel/confirm+yes-escalate/pause/resume/use-qwen), the explicit "never narrate a control-verb outcome without issuing the verb" prohibition, and both inherited findings (F-10-5-6-W1, F-10-5-2-W2) named. AGENTS.md Tier-2/3 flows rewritten to plain NL + cross-ref to the SKILL.md dispatch section. Enforced by 6 `test_recognized_phrase_dispatch.py` tests.
- **AC-3 (discoverability tie-in): MATCH.** SKILL.md + README document `user_facing_guidance` (RecoveryAction / Rule S) as the discoverability surface carrying the exact working phrase ("to abort this, type: cancel 14"). AGENTS.md already carried `user_facing_guidance` (Rule S); assertion `test_agents_md_documents_user_facing_guidance_discoverability` passes.
- **AC-4 (drift test): MATCH.** `tests/integration/test_recognized_phrase_dispatch.py` — 19 tests: README no-slash-table, plain-NL section present, SKILL.md recognized-phrase contract + prohibition + finding IDs + both inherited phrases, AGENTS.md plain-NL flows, cross-file dead-slash sweep (token-boundary matcher excludes prose word-lists).
- **AC-5 (MANDATORY-CR): MATCH** — see §5.12. Criterion 6 (load-bearing control-verb dispatch contract) + criterion 4 (capstone: last Cluster story of Epic 10.5) fire.
- **AC-6 (live runtime verification, Task 5): N/A at dev-pass** — Adam-hands-on Discord walk, HALT per HYBRID RUN-MODE. The persona runs an LLM this repo cannot unit-test; the drift test enforces the contract text, the walk verifies the runtime behaviour.

No drift found; no AC-text fixes required.

## 2. File-List-vs-git diff check

`git status --porcelain` (story-scoped paths):

```
 M README.md                                             TRACKED
 M hermes-config/AGENTS.md                               TRACKED
 M hermes-config/skills/mailbot/SKILL.md                 TRACKED
 M mailbot_api/chat/orchestrator.py                      TRACKED
 M mailbot_api/mcp_server.py                             TRACKED
 M tests/integration/test_draft_reply_orchestrator.py    TRACKED
 M tests/integration/test_mcp_server.py                  TRACKED
 M tests/integration/test_spend_chart_command.py         TRACKED
?? tests/integration/test_recognized_phrase_dispatch.py  UNTRACKED (pending git add — story artifact)
?? _bmad-output/implementation-artifacts/10-5-6-*.md     UNTRACKED (story file + this pre-review)
```

All File-List paths present in git output. `test_recognized_phrase_dispatch.py` untracked → staged at Step 2.6. Not-in-File-List but in git: `.claude/settings.json` + `_bmad-output/planning-artifacts/epics.md` (pre-existing session-start modifications, NOT this story — will NOT be staged) and `scratch/` (pre-existing untracked WIP, NOT staged).

## 3. Adversarial self-review

- [MEDIUM] `mcp_server.py:1036` — the `set_model_oneshot` description still contains `model_chosen_reason=OVERRIDE_SLASH_ONE_SHOT` and `set_model_persistent` `OVERRIDE_SLASH_PERSISTENT`. These are **audit-vocab enum VALUES** (closed-set from Story 9.2, `ModelChosenReason.OVERRIDE_SLASH_ONE_SHOT`), not slash invocations. Renaming the enum is out-of-scope (would touch the audit vocabulary + migrations + every consumer) and would be a false economy. Kept intentionally; the test matcher (token-boundary `/verb`) does not flag them. **Disposition §4: ACCEPT WITH RATIONALE.**
- [LOW] The `_dead_slash_offenders` matcher in the drift test uses a token-boundary regex (`(?:^|[\s`])/verb`). It would NOT catch a slash-invocation glued to a non-space punctuation (e.g. `(/pause)`). Low risk — no such form exists in the current files, and the sweep is a regression guard not a parser. **§4: ACCEPT WITH RATIONALE.**
- [LOW] `test_no_regression_of_slash_word_in_prose_is_acceptable` allows the word "slash" in README only alongside the Hermes/reserved/owns-`/` explanation. The README does keep the honest architectural explanation, so it passes; but the guard is somewhat loose (regex OR). Acceptable — the intent is to permit the honest F-10-5-1 explanation while forbidding an invocation table (which the stronger `## Slash commands`-absent test already enforces). **§4: ACCEPT WITH RATIONALE.**
- [LOW] Scope expansion beyond pure-docs: the story authoring anticipated "zero mailbot_api source changes," but the pre-review surfaced that the MCP tool `description` strings (agent-facing) and the `_SENSITIVE_ESCALATION_PROMPT` (user-facing) still emitted the dead slash forms — directly contradicting the charter. These were rewritten (string-literals only, no logic). This is in-scope for a charter rewrite (agent/user-facing dead-slash surfaces) and is documented. **§4: ACCEPT WITH RATIONALE** (justified in-scope; not a fabricated code change).
- [INFO] README anchor links `#talking-to-mailbot` and `#sensitive-and-confidential-emails` — GitHub auto-slugs headings; `## Talking to MailBot` → `#talking-to-mailbot` ✓, `### Sensitive and confidential emails` → `#sensitive-and-confidential-emails` ✓. Verified against the actual heading text.
- [INFO] The `render_spend_chart` description now says "spend month"; the `test_skill_md_documents_render_spend_chart` and `test_recognized_phrase_dispatch` both key off the plain-NL phrasing — consistent.

## 4. Self-caught issues remediated this audit

- MEDIUM (audit-vocab enum values): **ACCEPT WITH RATIONALE** — `OVERRIDE_SLASH_*` are closed-set code identifiers, not invocations; renaming is out-of-scope and cross-cutting.
- LOW (matcher token-boundary edge): **ACCEPT WITH RATIONALE** — regression guard, not a parser; no offending form exists.
- LOW (slash-word prose guard looseness): **ACCEPT WITH RATIONALE** — intentional, permits the honest F-10-5-1 explanation.
- LOW (mailbot_api string-literal scope expansion): **ACCEPT WITH RATIONALE** — agent/user-facing dead-slash surfaces are in charter scope; string-only, no logic; tests updated. Flagged for reviewer visibility.
- INFO items: no action.

## 5. Posture Audit

### 5.1 Lockfile hygiene
Non-dep-change story. `git diff --numstat -- requirements.txt` → (no output). No requirements.txt in File List. **PASS.**

### 5.2 Cross-doc pair verification
Cross-doc claims: the story cross-references two walk-evidence findings. Verified:
```
Claim: F-10-5-6-W1 "use qwen narrated-not-dispatched" (story Context)
Source: 10-5-5-walk-evidence.md § F-10-5-6-W1
  $ Grep "F-10-5-6-W1" 10-5-5-walk-evidence.md → present (line 39,43)  → MATCH
Claim: F-10-5-2-W2 "yes,escalate re-parrots refusal" (story Context)
Source: 10-5-2-walk-evidence.md § AC-4
  $ Grep "F-10-5-2-W2" 10-5-2-walk-evidence.md → present (line 92,105)  → MATCH
```
§5.2.1 schema-touching: File List contains NO migrations paths → N/A. **PASS.**

### 5.3 Lifecycle string-uniqueness
Story added zero i18n keys (no i18n system; persona docs + README prose). **N/A.**

### 5.4 Multi-consumer impact scan
Modified shared module: `mailbot_api/mcp_server.py` `_TOOL_DESCRIPTIONS` dict values + `mailbot_api/chat/orchestrator.py` `_SENSITIVE_ESCALATION_PROMPT`. These are consumed by: the MCP server tool registration (descriptions read by Hermes) and `handle_draft_reply` (the defender message). Change is string-literal-only; behaviour unchanged. Consumers verified:
```
$ Grep "_SENSITIVE_ESCALATION_PROMPT" mailbot_api/ → orchestrator.py (def + 1 use)
$ Grep "_TOOL_DESCRIPTIONS|description" mcp_server.py → single build_mcp_server consumer
Test consumers: test_mcp_server.py (description asserts), test_draft_reply_orchestrator.py (message assert) — both updated + green.
```
**PASS** — 2 string surfaces, all consumers enumerated + tests updated.

### 5.5 Screenshot-based perception check
No AC asserts "visible / user sees / appears" in a graphical sense. The persona/README are text; Discord rendering is not a MailBot-repo surface. AC-6 runtime verification is Adam's Discord walk (Task 5), not a screenshot claim. **N/A — no graphical frontend.**

### 5.6 Upstream-contract spec coverage
Story consumes no upstream-stripped/projection field. `_SENSITIVE_ESCALATION_PROMPT` change is a constant string; no projection-contract dependency. **N/A.**

### 5.7 Module-level mutable container (Python-stack overlay)
Modified `.py`: `orchestrator.py`, `mcp_server.py`. Added lines are exclusively comments + string-literal edits inside existing structures:
```
$ git diff -- orchestrator.py mcp_server.py | grep '^+' | grep -v (comment|string) → (empty)
```
No new module-level `dict`/`list`/`set`/counter introduced. `_SENSITIVE_ESCALATION_PROMPT` / `_CONFIDENTIAL_REFUSAL` are pre-existing module constants (tuple-concatenated str, immutable) — content edited, not shape. **PASS — no new mutable container.**

### 5.8 Dev-fixture seed-vs-production-shape parity
Story introduced one new test file (`test_recognized_phrase_dispatch.py`) — it reads live persona/README files via `Path.read_text` (the real producer), NOT a hand-crafted fixture. No JSON/ORM-payload fixture introduced. **N/A — no producer-shape fixture; the test reads the real artifacts.**

### 5.9 grep-verify-cited-figures
Cited figures (dev-pass values; post-CR values in the note below):
```
Cite: "suite 1853+2+3, +19 net vs 1834 baseline" (dev-pass, pre-CR)
  $ pytest -q → "1853 passed, 2 skipped, 3 deselected in 239.29s"  → MATCH
  Baseline 1834 (10-5-5 close, sprint-status) + 19 new drift tests = 1853  → MATCH
Cite: "19 tests in test_recognized_phrase_dispatch.py" (dev-pass, pre-CR)
  $ pytest test_recognized_phrase_dispatch.py -q → "19 passed"  → MATCH
Cite: "mypy 134 source files"
  $ mypy --strict mailbot_api → "Success: no issues found in 134 source files"  → MATCH
```
**Post-CR update (2026-07-11):** the MANDATORY-CR round added 6 matcher-regression tests → drift gate is now **25 tests**, suite **1859+2+3** (+25 net vs 1834). Re-verified: `pytest -q → 1859 passed, 2 skipped, 3 deselected`; `pytest test_recognized_phrase_dispatch.py -q → 25 passed`. **PASS.**

### 5.10 Producer-boundary contract enforcement
No typed-column producer touched; no new HTTP response shape; no third-party JSON ingestion. The two `.py` edits are user/agent-facing string constants, not column writes or response projections. §5.10.a/b/c/d all N/A. **N/A — story writes no typed columns and emits no new response shape.**

### 5.11 Git-evidence consistency check
**5.11.a** File-List-vs-working-tree: all File-List paths appear in `git status --porcelain` (§2). `.claude/settings.json` + `epics.md` present in git but pre-existing (session-start, not staged); `scratch/` untracked WIP (not staged). No silent scope-creep. **PASS.**
**5.11.b** Production-only test ratio:
```
$ git diff --numstat (tracked File List):
  testAdded (test_*.py): draft_orch 4 + mcp 20 + spend 16 = 40; + new test file 213 (untracked, counts as test)
  docsAdded (.md): README 45  (SKILL.md/AGENTS.md are persona-contract docs → docs)
  prodAddedExcludingDocs: orchestrator 5 + mcp_server 31 = 36  (string-literal edits)
  prodOnlyTestRatio: (40+213) / 36 = 7.0
```
Ratio 7.0 ≫ 0.30. **PASS** (test-heavy; the prod delta is string-literal-only).
**5.11.c** No-later-commits-under-attribution: single-session dev pass; `git log` since status-flip touching File List = none (same-session). **PASS.**

### 5.12 CR-cadence-mandatory surface classification
```
Criterion 1 (boundary-introducing): NO — no new writer monopoly / lint boundary / check_boundaries allowlist.
Criterion 2 (dep-introducing): NO — no new deps.
Criterion 3 (dev-self-flagged): NO — §4 has ACCEPT-WITH-RATIONALE items only, no ESCALATE-TO-REVIEWER blocker.
Criterion 4 (capstone): YES — 10-5-6 is the LAST cluster story of Epic 10.5 (done-flip clause 1: 10-5-1..10-5-6 done); charter-level surface.
Criterion 5 (privacy-invariant): PARTIAL — touches the sensitive-escalation user-facing prompt (_SENSITIVE_ESCALATION_PROMPT) which sits on the NFR-PRIV-1/2 handshake surface; the change is wording-only (no gate logic), but the surface is privacy-adjacent.
Criterion 6 (load-bearing-dispatch): YES — the recognized-phrase control-verb dispatch contract is the primary integration surface the Hermes persona reads to dispatch cancel/confirm/pause/resume/escalate; other flows (10-5-2 escalation, 10-5-1 kill-switch) depend on it being reliably honored.

Cadence verdict: MANDATORY-CR (criteria 4 + 6 fire; 5 partial). Full scope, reviewer model ≠ dev model.
```

## Posture Audit summary table

| Check | Status |
| --- | --- |
| 5.1 Lockfile hygiene | ✅ PASS |
| 5.2 Cross-doc pair verification | ✅ PASS |
| 5.3 Lifecycle string-uniqueness | N/A — no i18n keys |
| 5.4 Multi-consumer impact scan | ✅ PASS |
| 5.5 Screenshot-based perception check | N/A — no graphical frontend |
| 5.6 Upstream-contract spec coverage | N/A — no projection dependency |
| 5.7 Module-level mutable container | ✅ PASS — no new container |
| 5.8 Dev-fixture seed-vs-production-shape parity | N/A — test reads real artifacts |
| 5.9 grep-verify-cited-figures | ✅ PASS |
| 5.10 Producer-boundary contract enforcement | N/A — no typed column / response shape |
| 5.11 Git-evidence consistency check | ✅ PASS |
| 5.12 CR-cadence classification | MANDATORY-CR (crit 4 + 6) |
