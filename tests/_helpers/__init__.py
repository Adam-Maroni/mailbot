"""Cross-test helpers (Story 9-3 CR-F6).

Shared fixtures/utilities pulled out of test files to avoid the
private-symbol cross-module import antipattern (`from tests.unit.router.
test_router import _FakeAdapter`).
"""
